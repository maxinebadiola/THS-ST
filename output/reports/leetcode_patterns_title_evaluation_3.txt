====================================================================================================
TITLE EVALUATION REPORT: leetcode_patterns
Variant 3: BERT ONLY (reference_length = 6)
====================================================================================================

OVERALL MODEL PERFORMANCE
----------------------------------------------------------------------------------------------------
gemma2-9b            Average Score: 0.3333
llama3.1-8b          Average Score: 0.3147
mistral-7b-v0.3      Average Score: 0.3012
qwen2-7b             Average Score: 0.2403
tinyllama            Average Score: 0.2797


--- SEGMENT 01 ---
Start Time: 0s (00:00)
Transcript Length: 94 characters
Total Titles Evaluated: 50

Rank   Avg Score    Bert Score   Cosine Score   Penalty    Model                Title
----------------------------------------------------------------------------------------------------
1      0.6261       0.6261       0.0000         1.0000     mistral-7b-v0.3      Eight Key Patterns for Coding Interviews (4)
2      0.5665       0.6692       0.0000         0.8465     tinyllama            Eight Most Important Patterns for Coding Interviews (4)
3      0.5531       0.5531       0.0000         1.0000     mistral-7b-v0.3      Eight Key Coding Interview Patterns (2)
4      0.5459       0.5459       0.0000         1.0000     llama3.1-8b          Coding Interview Secrets Backed by Data
5      0.5428       0.5428       0.0000         1.0000     mistral-7b-v0.3      Eight Essential Coding Interview Patterns (3)
6      0.5258       0.5258       0.0000         1.0000     llama3.1-8b          Cracking Coding Interviews with Data-Driven Patterns (4)
7      0.4939       0.4939       0.0000         1.0000     llama3.1-8b          Mastering Coding Interviews through Data-Backed Strategies
8      0.4873       0.6800       0.0000         0.7165     tinyllama            The 8 Most Important Patterns for Coding Interviews (3)
9      0.4819       0.6725       0.0000         0.7165     tinyllama            The 7 Most Important Patterns for Coding Interviews
10     0.4752       0.4752       0.0000         1.0000     gemma2-9b            Mastering Coding Interview Patterns (5)
11     0.4741       0.4741       0.0000         1.0000     llama3.1-8b          The Science of Acing Coding Interviews
12     0.4705       0.4705       0.0000         1.0000     llama3.1-8b          Patterns That Crack Coding Interviews
13     0.4634       0.6467       0.0000         0.7165     mistral-7b-v0.3      Top 8 Coding Interview Patterns Backed by Data
14     0.4557       0.5384       0.0000         0.8465     tinyllama            The Ultimate Guide to Coding Interview Patterns
15     0.4488       0.5302       0.0000         0.8465     qwen2-7b             Mastering Essential Coding Interview Patterns: Data-Backed Strategies
16     0.4242       0.4242       0.0000         1.0000     llama3.1-8b          Unlocking Interview Success with Proven Patterns
17     0.4151       0.4151       0.0000         1.0000     gemma2-9b            Cracking the Coding Interview Code (5)
18     0.4012       0.5599       0.0000         0.7165     qwen2-7b             Master 8 Essential Coding Interview Patterns: Data-Backed Strategies (3)
19     0.4012       0.4012       0.0000         1.0000     llama3.1-8b          Unlocking Interview Secrets with Proven Methods
20     0.3980       0.5554       0.0000         0.7165     qwen2-7b             Master 8 Critical Coding Interview Patterns: Data-Backed Strategies (2)
21     0.3974       0.5546       0.0000         0.7165     qwen2-7b             Master 8 Essential Coding Interview Patterns: Data-Backed Strategy (2)
22     0.3952       0.5515       0.0000         0.7165     qwen2-7b             Master 8 Critical Coding Interview Patterns: Data-Backed Strategy (2)
23     0.1820       0.4948       0.0000         0.3679     tinyllama            The Ultimate Guide to Coding Interviews: 8 Patterns You Need to Know

Model Averages for this Segment:
  gemma2-9b           : 0.4451
  llama3.1-8b         : 0.4913
  mistral-7b-v0.3     : 0.5702
  qwen2-7b            : 0.4033
  tinyllama           : 0.4847

====================================================================================================

--- SEGMENT 02 ---
Start Time: 3s (00:03)
Transcript Length: 5,662 characters
Total Titles Evaluated: 50

Rank   Avg Score    Bert Score   Cosine Score   Penalty    Model                Title
----------------------------------------------------------------------------------------------------
1      0.3237       0.3237       0.0000         1.0000     llama3.1-8b          Mastering Essential Coding Patterns for Interviews
2      0.3196       0.3196       0.0000         1.0000     llama3.1-8b          Efficient Algorithms for Coding Interviews
3      0.3042       0.3042       0.0000         1.0000     llama3.1-8b          Mastering 5 Essential Coding Interview Patterns
4      0.3026       0.3026       0.0000         1.0000     llama3.1-8b          Mastering 8 Essential Coding Interview Patterns
5      0.2963       0.3500       0.0000         0.8465     llama3.1-8b          Mastering 5 Essential Patterns for Coding Interviews (2)
6      0.2904       0.2904       0.0000         1.0000     tinyllama            Understanding Leak Code Patterns (3)
7      0.2858       0.3376       0.0000         0.8465     llama3.1-8b          Mastering 5 Essential Coding Patterns for Interviews (2)
8      0.2832       0.3346       0.0000         0.8465     tinyllama            Understanding Leak Code Patterns for Coding Interviews (5)
9      0.2827       0.2827       0.0000         1.0000     gemma2-9b            Mastering Coding Interview Patterns (10)
10     0.2696       0.3763       0.0000         0.7165     tinyllama            The 8 Most Important Patterns for Coding Interviews (2)
11     0.2590       0.2590       0.0000         1.0000     llama3.1-8b          Essential LeetCode Strategies for Success
12     0.2223       0.3665       0.0000         0.6065     mistral-7b-v0.3      Mastering Two Pointers and Sliding Windows for Coding Interviews
13     0.1962       0.3821       0.0000         0.5134     mistral-7b-v0.3      Mastering Two Pointers and Sliding Windows Patterns for Coding Interviews
14     0.1950       0.3798       0.0000         0.5134     mistral-7b-v0.3      Mastering Two Pointers & Sliding Windows Patterns for Coding Interviews (7)
15     0.1936       0.3770       0.0000         0.5134     qwen2-7b             Master Linear & Non-Linear Data Structures: Essential Coding Interview Patterns
16     0.1916       0.3731       0.0000         0.5134     qwen2-7b             Master Linear & Non-Linear Data Structures Patterns for Coding Interviews (3)
17     0.1914       0.3728       0.0000         0.5134     qwen2-7b             Master Linear & Non-Linear Data Structure Patterns for Coding Interviews (3)
18     0.1671       0.3846       0.0000         0.4346     mistral-7b-v0.3      Mastering Two Pointers, Sliding Windows, and Binary Search for Coding Interviews
19     0.1598       0.3676       0.0000         0.4346     qwen2-7b             Master Linear & Non-Linear Data Structures with Essential Coding Interview Patterns
20     0.1409       0.3830       0.0000         0.3679     qwen2-7b             Master Linear & Non-Linear Data Structures with 8 Essential Coding Interview Patterns
21     0.1395       0.3791       0.0000         0.3679     qwen2-7b             Master 8 Essential Coding Interview Patterns: Two Pointers, Sliding Windows & More
22     0.0936       0.3549       0.0000         0.2636     llama3.1-8b          Mastering 5 Essential Patterns for Coding Interviews Next Steps: LeetCode Solutions & Advanced Techniques

Model Averages for this Segment:
  gemma2-9b           : 0.2827
  llama3.1-8b         : 0.2767
  mistral-7b-v0.3     : 0.1950
  qwen2-7b            : 0.1782
  tinyllama           : 0.2826

====================================================================================================

--- SEGMENT 03 ---
Start Time: 342s (05:42)
Transcript Length: 13,934 characters
Total Titles Evaluated: 50

Rank   Avg Score    Bert Score   Cosine Score   Penalty    Model                Title
----------------------------------------------------------------------------------------------------
1      0.3344       0.3344       0.0000         1.0000     llama3.1-8b          "Exploring Trees and Graphs: Breadth-First Search
2      0.2878       0.2878       0.0000         1.0000     qwen2-7b             "Binary Search and Beyond: Uncovering Patterns
3      0.2746       0.3832       0.0000         0.7165     llama3.1-8b          Navigating Non-Linear Structures: Breadth-First Search, Depth-FirstSearch, and Backtracking
4      0.2588       0.2588       0.0000         1.0000     gemma2-9b            Exploring Trees & Graphs
5      0.2586       0.2586       0.0000         1.0000     gemma2-9b            **Mastering Depth-First Search
6      0.2561       0.3026       0.0000         0.8465     gemma2-9b            **Binary Search Mastery: Finding Patterns Beyond Sorting**
7      0.2384       0.2384       0.0000         1.0000     gemma2-9b            Binary Search Mastery
8      0.2332       0.3845       0.0000         0.6065     llama3.1-8b          Navigating Non-Linear Structures: Breadth-First Search, Depth-First Traversal, and Backtracking
9      0.2293       0.3200       0.0000         0.7165     gemma2-9b            The Art of Exploration: Depth-First Search & Breadth
10     0.2276       0.2276       0.0000         1.0000     gemma2-9b            *Descriptive & Concise:**
11     0.2200       0.2200       0.0000         1.0000     gemma2-9b            *Intriguing & Conceptual:**
12     0.2194       0.3617       0.0000         0.6065     qwen2-7b             Use a path list to accumulate the current combination
13     0.2181       0.2181       0.0000         1.0000     gemma2-9b            *Short & Punchy:**
14     0.2154       0.2154       0.0000         1.0000     gemma2-9b            *Short & Catchy:**
15     0.2039       0.3972       0.0000         0.5134     llama3.1-8b          Navigating Non-Linear Structures: Binary Search, Breadth-First Search, Depth-FirstSearch, and Backtracking
16     0.0000       1.0000       0.0000         0.0000     tinyllama            For example, if we had a sorted list of only true and false, we can actually use binary search to find the first true occurrence. The binary search would work like this. You look at the middle of the list. If the middle value is true, you know that the first true must either be here or somewhere to the left, so you move your search to the left half. If the middle value is false, you know that the first true must be to the right, so you focus on the right half. This type of searching, based on where a condition is true and where it is false, can be reused for every single binary search problem. In fact, we actually have this free template for this on our website, Algamonster. All we need to do is find out what makes something feasible. For example, the common lead code problem, minimum in rotated sorted array, doesn't appear to follow binary search at first. After all, it's not sorted. However, there's a monotonic condition we can use to apply binary search. The condition is that any element that is less than the last element of the array is part of the rotated section, while any element greater than the last element is part of the original sorted section. We can mentally convert this to trues and falses, and just like that, we have the same true and false question we just solve. This can be applied to a ton of different questions. For more practice and an in-depth explanation, check out algo.monster. Now we'll take a look at non-linear structure problems which work with trees and graphs. In fact, instead of treating trees and graphs as two entirely different structures, you should imagine that a tree is just a graph with no cycles. The only difference between the two is that for a graph that could have cycles, you need some visited set to keep track of nodes you've already visited. This makes the patterns we will talk about applicable to both. For non-linear problems, we'll start with breadth-first search, as it's the easiest algorithm to understand and build off of. breadth-first search is an algorithm used to explore nodes in a graph or tree level by level. The idea behind this is that it starts at a given node, commonly called the root, and explores all its immediate neighbors first before moving on to the neighbors' neighbors, and so on. To keep track of the nodes that have already been visited so that we don't revisit them, BFS uses a queue, a data structure that works like a line. First in, first out. This ensures that nodes are visited in the correct order, starting from the closest to the root and moving outward. BFS is such a common algorithm that you will use all the time. Luckily for you, instead of having to derive it from scratch each time, we have this template that we can reuse. If you want to see more about how we got this template, check it out at algo.monster. Now let's see BFS in action. We're going to use this template to solve a common lead code problem, level order traversal of a binary tree. The problem asks us to give all of the levels of the tree at each depth This means we need to visit the nodes of the tree level by level, starting from the root and moving down, visiting all the nodes at each level before proceeding to the next. For example, given a tree with the root node 3 and children 9 and 20, where 20 has children 15 and 7, we need to return the values of the nodes level by level in the following order. First 3, then 9, 20, and finally 15, 16th phrase which is not a kill, we can solve this problem using BFS as its ideal for level by level traversal. The idea is to use a queue to help explore the tree one level at a time. First, we add the root node to the queue. Then for each level, we process all nodes currently in the queue by removing them and adding their values to the result. For each node we process, we also add its left and right children to the queue so they can be processed in the next level. This ensures that we visit all nodes on one level before moving to the next. By repeating this process until the queue is empty, we can traverse the entire tree level by level and collect the node values in the correct order. Finally, we return the list of node values grouped by levels. Devth first search is an algorithm that builds on the foundation of BFS but explores the tree or graph in a different way. While BFS focuses on exploring nodes level by level, DFS dives deep into one path as far as possible before exploring the next path. In BFS, we use a queue to process nodes in a breadth first manner, ensuring all nodes at one level are explored before moving to the next. In contrast, DFS uses a stack to explore nodes in a depth first manner, where you keep going down one branch until you reach the end, and then you come back to explore other branches. Most often, the stack DFS uses is the call stack. As most DFS implementations use recursion, where BFS is ideal for finding the shortest path or exploring nodes layer by layer, DFS is more suited for problems where you need to explore all paths or check every possible configuration, such as finding all paths in a tree, searching for a specific condition deep in the structure or detecting cycles. The depth first nature of DFS makes it more memory efficient in some cases, but it's less suited for problems where the shortest path or shallow exploration is required, as it doesn't guarantee finding the closest solution first. Like with BFS, DFS is a common algorithm that you will use all the time, so we have a template that we can reuse. Again, if you're curious to how this works, check it out at algo.monster. Now, let's solve a leap code problem with DFS. A common leap code problem that uses this pattern is called number of islands. This problem asks us to count the number of islands in a 2D grid where one represents land and zero represents water. An island is formed by connecting adjacent ones horizontally or vertically. We need to determine how many distinct islands exist by identifying connected components of ones in the grid. For example, in a grid where someone's are grouped together, each connected group is considered an island. Our goal is to traverse the grid, find all islands, and count them. We can solve this problem using DFS to explore each island. The idea is to iterate through each cell in the grid. When we encounter a one land, it indicates the start of an island. From there, we perform DFS to visit all connected ones all parts of the same island by exploring the neighboring cells in all four directions as deep as they go. During the DFS, we mark each visited cell by changing its value to zero to ensure we don't count it again. Once all connected cells for that island are visited, we increment the island count and continue searching the rest of the grid. This process ensures that we explore each island once and count it correctly. By the end, the total number of initializing DFS calls we make, not including the recursive calls, equals the number of islands. Backtracking is a pattern that many people struggle with, but in reality, it's actually just an extension of depth first search. In DFS, we typically traverse through a prebuilt structure like a tree or graph, where the nodes and connections are already defined. With backtracking, however, you often have to build the solution tree yourself as you explore different options, especially in combinatorial problems, where the structure of the tree isn't given explicitly, but is generated dynamically as you make decisions. In a backtracking problem, you explore all possible solutions by making a series of decisions. Each decision represents a node in the tree, and each potential decision forms a branch. As you explore a decision, you either reach a valid solution, or you hit a dead end. When a decision leads to a dead end, you backtrack to the previous decision, undo it, and try another path. Just like with all of our other patterns, we also have a template we are going to use here, which is this. Now, let's walk through it by solving a lead code problem. Letter combinations of a phone number. In this problem, we're given a string of digits, typically from 2 to 9, and we need to return all the possible letter combinations that the digits could represent using the mapping of a phone keypad. For example, if we have the digits 23, each digit corresponds to a set of letters, two maps to A, B, C, and three maps to D, E, F. Our goal is to generate every possible combination of these letters. So, for 23, some outputs could be a D, A, E, A, F, B, D, B, E, B, F, and so on. Now, our goal is to find all of these combinations. Backtracking is the optimal approach for solving this problem because it allows us to explore each possible letter one at a time while pruning invalid configurations early. Rather than brute forcing through every possible combination, backtracking lets us build the solution letter by letter, discarding incomplete paths. This way, we avoid wasting time exploring unnecessary paths, and instead, we focus on building the best possible solution. Let's create a template for backtracked solutions. We will use this for all of my patterns. I hope this helps!
17     0.0000       1.0000       0.0000         0.0000     tinyllama            For example, if we had a sorted list of only true and false, we can actually use binary search to find the first true occurrence. The binary search would work like this. You look at the middle of the list. If the middle value is true, you know that the first true must either be here or somewhere to the left, so you move your search to the left half. If the middle value is false, you know that the first true must be to the right, so you focus on the right half. This type of searching, based on where a condition is true and where it is false, can be reused for every single binary search problem. In fact, we actually have this free template for this on our website, Algamonster. All we need to do is find out what makes something feasible. For example, the common lead code problem, minimum in rotated sorted array, doesn't appear to follow binary search at first. After all, it's not sorted. However, there's a monotonic condition we can use to apply binary search. The condition is that any element that is less than the last element of the array is part of the rotated section, while any element greater than the last element is part of the original sorted section. We can mentally convert this to trues and falses, and just like that, we have the same true and false question we just solve. This can be applied to a ton of different questions. For more practice and an in-depth explanation, check out algo.monster. Now we'll take a look at non-linear structure problems which work with trees and graphs. In fact, instead of treating trees and graphs as two entirely different structures, you should imagine that a tree is just a graph with no cycles. The only difference between the two is that for a graph that could have cycles, you need some visited set to keep track of nodes you've already visited. This makes the patterns we will talk about applicable to both. For non-linear problems, we'll start with breadth-first search, as it's the easiest algorithm to understand and build off of. breadth-first search is an algorithm used to explore nodes in a graph or tree level by level. The idea behind this is that it starts at a given node, commonly called the root, and explores all its immediate neighbors first before moving on to the neighbors' neighbors, and so on. To keep track of the nodes that have already been visited so that we don't revisit them, BFS uses a queue, a data structure that works like a line. First in, first out. This ensures that nodes are visited in the correct order, starting from the closest to the root and moving outward. BFS is such a common algorithm that you will use all the time. Luckily for you, instead of having to derive it from scratch each time, we have this template that we can reuse. If you want to see more about how we got this template, check it out at algo.monster. Now let's see BFS in action. We're going to use this template to solve a common lead code problem, level order traversal of a binary tree. The problem asks us to give all of the levels of the tree at each depth This means we need to visit the nodes of the tree level by level, starting from the root and moving down, visiting all the nodes at each level before proceeding to the next. For example, given a tree with the root node 3 and children 9 and 20, where 20 has children 15 and 7, we need to return the values of the nodes level by level in the following order. First 3, then 9, 20, and finally 15, 16th phrase which is not a kill, we can solve this problem using BFS as its ideal for level by level traversal. The idea is to use a queue to help explore the tree one level at a time. First, we add the root node to the queue. Then for each level, we process all nodes currently in the queue by removing them and adding their values to the result. For each node we process, we also add its left and right children to the queue so they can be processed in the next level. This ensures that we visit all nodes on one level before moving to the next. By repeating this process until the queue is empty, we can traverse the entire tree level by level and collect the node values in the correct order. Finally, we return the list of node values grouped by levels. Devth first search is an algorithm that builds on the foundation of BFS but explores the tree or graph in a different way. While BFS focuses on exploring nodes level by level, DFS dives deep into one path as far as possible before exploring the next path. In BFS, we use a queue to process nodes in a breadth first manner, ensuring all nodes at one level are explored before moving to the next. In contrast, DFS uses a stack to explore nodes in a depth first manner, where you keep going down one branch until you reach the end, and then you come back to explore other branches. Most often, the stack DFS uses is the call stack. As most DFS implementations use recursion, where BFS is ideal for finding the shortest path or exploring nodes layer by layer, DFS is more suited for problems where you need to explore all paths or check every possible configuration, such as finding all paths in a tree, searching for a specific condition deep in the structure or detecting cycles. The depth first nature of DFS makes it more memory efficient in some cases, but it's less suited for problems where the shortest path or shallow exploration is required, as it doesn't guarantee finding the closest solution first. Like with BFS, DFS is a common algorithm that you will use all the time, so we have a template that we can reuse. Again, if you're curious to how this works, check it out at algo.monster. Now, let's solve a leap code problem with DFS. A common leap code problem that uses this pattern is called number of islands. This problem asks us to count the number of islands in a 2D grid where one represents land and zero represents water. An island is formed by connecting adjacent ones horizontally or vertically. We need to determine how many distinct islands exist by identifying connected components of ones in the grid. For example, in a grid where someone's are grouped together, each connected group is considered an island. Our goal is to traverse the grid, find all islands, and count them. We can solve this problem using DFS to explore each island. The idea is to iterate through each cell in the grid. When we encounter a one land, it indicates the start of an island. From there, we perform DFS to visit all connected ones all parts of the same island by exploring the neighboring cells in all four directions as deep as they go. During the DFS, we mark each visited cell by changing its value to zero to ensure we don't count it again. Once all connected cells for that island are visited, we increment the island count and continue searching the rest of the grid. This process ensures that we explore each island once and count it correctly. By the end, the total number of initializing DFS calls we make, not including the recursive calls, equals the number of islands. Backtracking is a pattern that many people struggle with, but in reality, it's actually just an extension of depth first search. In DFS, we typically traverse through a prebuilt structure like a tree or graph, where the nodes and connections are already defined. With backtracking, however, you often have to build the solution tree yourself as you explore different options, especially in combinatorial problems, where the structure of the tree isn't given explicitly, but is generated dynamically as you make decisions. In a backtracking problem, you explore all possible solutions by making a series of decisions. Each decision represents a node in the tree, and each potential decision forms a branch. As you explore a decision, you either reach a valid solution, or you hit a dead end. When a decision leads to a dead end, you backtrack to the previous decision, undo it, and try another path. Just like with all of our other patterns, we also have a template we are going to use here, which is this. Now, let's walk through it by solving a lead code problem. Letter combinations of a phone number. In this problem, we're given a string of digits, typically from 2 to 9, and we need to return all the possible letter combinations that the digits could represent using the mapping of a phone keypad. For example, if we have the digits 23, each digit corresponds to a set of letters, two maps to A, B, C, and three maps to D, E, F. Our goal is to generate every possible combination of these letters. So, for 23, some outputs could be a D, A, E, A, F, B, D, B, E, B, F, and so on. Now, our goal is to find all of these combinations. Backtracking is the optimal approach for solving this problem because it allows us to explore each possible letter one at a time while pruning invalid configurations early. Rather than brute forcing through every possible combination, backtracking lets us build the solution letter by letter, discarding incomplete paths. We start with the first digit, 0, which represents nothing. We then explore the second digit, which could be A, 4, 8, or 12. We mark the first letter as A, and explore the remaining digits. We explore the first two digits, which correspond to A and B. We remove the first A, leaving B. Now that we've removed B, we explore the third digit, C. We now have a valid combination of letters
18     0.0000       1.0000       0.0000         0.0000     mistral-7b-v0.3      For example, if we had a sorted list of only true and false, we can actually use binary search to find the first true occurrence. The binary search would work like this. You look at the middle of the list. If the middle value is true, you know that the first true must either be here or somewhere to the left, so you move your search to the left half. If the middle value is false, you know that the first true must be to the right, so you focus on the right half. This type of searching, based on where a condition is true and where it is false, can be reused for every single binary search problem. In fact, we actually have this free template for this on our website, Algamonster. All we need to do is find out what makes something feasible. For example, the common lead code problem, minimum in rotated sorted array, doesn't appear to follow binary search at first. After all, it's not sorted. However, there's a monotonic condition we can use to apply binary search. The condition is that any element that is less than the last element of the array is part of the rotated section, while any element greater than the last element is part of the original sorted section. We can mentally convert this to trues and falses, and just like that, we have the same true and false question we just solve. This can be applied to a ton of different questions. For more practice and an in-depth explanation, check out algo.monster. Now we'll take a look at non-linear structure problems which work with trees and graphs. In fact, instead of treating trees and graphs as two entirely different structures, you should imagine that a tree is just a graph with no cycles. The only difference between the two is that for a graph that could have cycles, you need some visited set to keep track of nodes you've already visited. This makes the patterns we will talk about applicable to both. For non-linear problems, we'll start with breadth-first search, as it's the easiest algorithm to understand and build off of. breadth-first search is an algorithm used to explore nodes in a graph or tree level by level. The idea behind this is that it starts at a given node, commonly called the root, and explores all its immediate neighbors first before moving on to the neighbors' neighbors, and so on. To keep track of the nodes that have already been visited so that we don't revisit them, BFS uses a queue, a data structure that works like a line. First in, first out. This ensures that nodes are visited in the correct order, starting from the closest to the root and moving outward. BFS is such a common algorithm that you will use all the time. Luckily for you, instead of having to derive it from scratch each time, we have this template that we can reuse. If you want to see more about how we got this template, check it out at algo.monster. Now let's see BFS in action. We're going to use this template to solve a common lead code problem, level order traversal of a binary tree. The problem asks us to give all of the levels of the tree at each depth This means we need to visit the nodes of the tree level by level, starting from the root and moving down, visiting all the nodes at each level before proceeding to the next. For example, given a tree with the root node 3 and children 9 and 20, where 20 has children 15 and 7, we need to return the values of the nodes level by level in the following order. First 3, then 9, 20, and finally 15, 16th phrase which is not a kill, we can solve this problem using BFS as its ideal for level by level traversal. The idea is to use a queue to help explore the tree one level at a time. First, we add the root node to the queue. Then for each level, we process all nodes currently in the queue by removing them and adding their values to the result. For each node we process, we also add its left and right children to the queue so they can be processed in the next level. This ensures that we visit all nodes on one level before moving to the next. By repeating this process until the queue is empty, we can traverse the entire tree level by level and collect the node values in the correct order. Finally, we return the list of node values grouped by levels. Devth first search is an algorithm that builds on the foundation of BFS but explores the tree or graph in a different way. While BFS focuses on exploring nodes level by level, DFS dives deep into one path as far as possible before exploring the next path. In BFS, we use a queue to process nodes in a breadth first manner, ensuring all nodes at one level are explored before moving to the next. In contrast, DFS uses a stack to explore nodes in a depth first manner, where you keep going down one branch until you reach the end, and then you come back to explore other branches. Most often, the stack DFS uses is the call stack. As most DFS implementations use recursion, where BFS is ideal for finding the shortest path or exploring nodes layer by layer, DFS is more suited for problems where you need to explore all paths or check every possible configuration, such as finding all paths in a tree, searching for a specific condition deep in the structure or detecting cycles. The depth first nature of DFS makes it more memory efficient in some cases, but it's less suited for problems where the shortest path or shallow exploration is required, as it doesn't guarantee finding the closest solution first. Like with BFS, DFS is a common algorithm that you will use all the time, so we have a template that we can reuse. Again, if you're curious to how this works, check it out at algo.monster. Now, let's solve a leap code problem with DFS. A common leap code problem that uses this pattern is called number of islands. This problem asks us to count the number of islands in a 2D grid where one represents land and zero represents water. An island is formed by connecting adjacent ones horizontally or vertically. We need to determine how many distinct islands exist by identifying connected components of ones in the grid. For example, in a grid where someone's are grouped together, each connected group is considered an island. Our goal is to traverse the grid, find all islands, and count them. We can solve this problem using DFS to explore each island. The idea is to iterate through each cell in the grid. When we encounter a one land, it indicates the start of an island. From there, we perform DFS to visit all connected ones all parts of the same island by exploring the neighboring cells in all four directions as deep as they go. During the DFS, we mark each visited cell by changing its value to zero to ensure we don't count it again. Once all connected cells for that island are visited, we increment the island count and continue searching the rest of the grid. This process ensures that we explore each island once and count it correctly. By the end, the total number of initializing DFS calls we make, not including the recursive calls, equals the number of islands. Backtracking is a pattern that many people struggle with, but in reality, it's actually just an extension of depth first search. In DFS, we typically traverse through a prebuilt structure like a tree or graph, where the nodes and connections are already defined. With backtracking, however, you often have to build the solution tree yourself as you explore different options, especially in combinatorial problems, where the structure of the tree isn't given explicitly, but is generated dynamically as you make decisions. In a backtracking problem, you explore all possible solutions by making a series of decisions. Each decision represents a node in the tree, and each potential decision forms a branch. As you explore a decision, you either reach a valid solution, or you hit a dead end. When a decision leads to a dead end, you backtrack to the previous decision, undo it, and try another path. Just like with all of our other patterns, we also have a template we are going to use here, which is this. Now, let's walk through it by solving a lead code problem. Letter combinations of a phone number. In this problem, we're given a string of digits, typically from 2 to 9, and we need to return all the possible letter combinations that the digits could represent using the mapping of a phone keypad. For example, if we have the digits 23, each digit corresponds to a set of letters, two maps to A, B, C, and three maps to D, E, F. Our goal is to generate every possible combination of these letters. So, for 23, some outputs could be a D, A, E, A, F, B, D, B, E, B, F, and so on. Now, our goal is to find all of these combinations. Backtracking is the optimal approach for solving this problem because it allows us to explore each possible letter one at a time while pruning invalid configurations early. Rather than brute forcing through every possible combination, backtracking lets us build the solution letter by letter, discarding incomplete paths that no longer make sense. We start by defining the base case, which in this case is when we've reached the end of the input string. At this point, we simply return the current combination. Otherwise, we loop through each valid option for the current digit, add it to our current combination, and recursively call the function with the remaining digits. After generating all possible combinations for the given input, we collect them and return them
19     0.0000       1.0000       0.0000         0.0000     tinyllama            For example, if we had a sorted list of only true and false, we can actually use binary search to find the first true occurrence. The binary search would work like this. You look at the middle of the list. If the middle value is true, you know that the first true must either be here or somewhere to the left, so you move your search to the left half. If the middle value is false, you know that the first true must be to the right, so you focus on the right half. This type of searching, based on where a condition is true and where it is false, can be reused for every single binary search problem. In fact, we actually have this free template for this on our website, Algamonster. All we need to do is find out what makes something feasible. For example, the common lead code problem, minimum in rotated sorted array, doesn't appear to follow binary search at first. After all, it's not sorted. However, there's a monotonic condition we can use to apply binary search. The condition is that any element that is less than the last element of the array is part of the rotated section, while any element greater than the last element is part of the original sorted section. We can mentally convert this to trues and falses, and just like that, we have the same true and false question we just solve. This can be applied to a ton of different questions. For more practice and an in-depth explanation, check out algo.monster. Now we'll take a look at non-linear structure problems which work with trees and graphs. In fact, instead of treating trees and graphs as two entirely different structures, you should imagine that a tree is just a graph with no cycles. The only difference between the two is that for a graph that could have cycles, you need some visited set to keep track of nodes you've already visited. This makes the patterns we will talk about applicable to both. For non-linear problems, we'll start with breadth-first search, as it's the easiest algorithm to understand and build off of. breadth-first search is an algorithm used to explore nodes in a graph or tree level by level. The idea behind this is that it starts at a given node, commonly called the root, and explores all its immediate neighbors first before moving on to the neighbors' neighbors, and so on. To keep track of the nodes that have already been visited so that we don't revisit them, BFS uses a queue, a data structure that works like a line. First in, first out. This ensures that nodes are visited in the correct order, starting from the closest to the root and moving outward. BFS is such a common algorithm that you will use all the time. Luckily for you, instead of having to derive it from scratch each time, we have this template that we can reuse. If you want to see more about how we got this template, check it out at algo.monster. Now let's see BFS in action. We're going to use this template to solve a common lead code problem, level order traversal of a binary tree. The problem asks us to give all of the levels of the tree at each depth This means we need to visit the nodes of the tree level by level, starting from the root and moving down, visiting all the nodes at each level before proceeding to the next. For example, given a tree with the root node 3 and children 9 and 20, where 20 has children 15 and 7, we need to return the values of the nodes level by level in the following order. First 3, then 9, 20, and finally 15, 16th phrase which is not a kill, we can solve this problem using BFS as its ideal for level by level traversal. The idea is to use a queue to help explore the tree one level at a time. First, we add the root node to the queue. Then for each level, we process all nodes currently in the queue by removing them and adding their values to the result. For each node we process, we also add its left and right children to the queue so they can be processed in the next level. This ensures that we visit all nodes on one level before moving to the next. By repeating this process until the queue is empty, we can traverse the entire tree level by level and collect the node values in the correct order. Finally, we return the list of node values grouped by levels. Devth first search is an algorithm that builds on the foundation of BFS but explores the tree or graph in a different way. While BFS focuses on exploring nodes level by level, DFS dives deep into one path as far as possible before exploring the next path. In BFS, we use a queue to process nodes in a breadth first manner, ensuring all nodes at one level are explored before moving to the next. In contrast, DFS uses a stack to explore nodes in a depth first manner, where you keep going down one branch until you reach the end, and then you come back to explore other branches. Most often, the stack DFS uses is the call stack. As most DFS implementations use recursion, where BFS is ideal for finding the shortest path or exploring nodes layer by layer, DFS is more suited for problems where you need to explore all paths or check every possible configuration, such as finding all paths in a tree, searching for a specific condition deep in the structure or detecting cycles. The depth first nature of DFS makes it more memory efficient in some cases, but it's less suited for problems where the shortest path or shallow exploration is required, as it doesn't guarantee finding the closest solution first. Like with BFS, DFS is a common algorithm that you will use all the time, so we have a template that we can reuse. Again, if you're curious to how this works, check it out at algo.monster. Now, let's solve a leap code problem with DFS. A common leap code problem that uses this pattern is called number of islands. This problem asks us to count the number of islands in a 2D grid where one represents land and zero represents water. An island is formed by connecting adjacent ones horizontally or vertically. We need to determine how many distinct islands exist by identifying connected components of ones in the grid. For example, in a grid where someone's are grouped together, each connected group is considered an island. Our goal is to traverse the grid, find all islands, and count them. We can solve this problem using DFS to explore each island. The idea is to iterate through each cell in the grid. When we encounter a one land, it indicates the start of an island. From there, we perform DFS to visit all connected ones all parts of the same island by exploring the neighboring cells in all four directions as deep as they go. During the DFS, we mark each visited cell by changing its value to zero to ensure we don't count it again. Once all connected cells for that island are visited, we increment the island count and continue searching the rest of the grid. This process ensures that we explore each island once and count it correctly. By the end, the total number of initializing DFS calls we make, not including the recursive calls, equals the number of islands. Backtracking is a pattern that many people struggle with, but in reality, it's actually just an extension of depth first search. In DFS, we typically traverse through a prebuilt structure like a tree or graph, where the nodes and connections are already defined. With backtracking, however, you often have to build the solution tree yourself as you explore different options, especially in combinatorial problems, where the structure of the tree isn't given explicitly, but is generated dynamically as you make decisions. In a backtracking problem, you explore all possible solutions by making a series of decisions. Each decision represents a node in the tree, and each potential decision forms a branch. As you explore a decision, you either reach a valid solution, or you hit a dead end. When a decision leads to a dead end, you backtrack to the previous decision, undo it, and try another path. Just like with all of our other patterns, we also have a template we are going to use here, which is this. Now, let's walk through it by solving a lead code problem. Letter combinations of a phone number. In this problem, we're given a string of digits, typically from 2 to 9, and we need to return all the possible letter combinations that the digits could represent using the mapping of a phone keypad. For example, if we have the digits 23, each digit corresponds to a set of letters, two maps to A, B, C, and three maps to D, E, F. Our goal is to generate every possible combination of these letters. So, for 23, some outputs could be a D, A, E, A, F, B, D, B, E, B, F, and so on. Now, our goal is to find all of these combinations. Backtracking is the optimal approach for solving this problem because it allows us to explore each possible letter one at a time while pruning invalid configurations early. Rather than brute forcing through every possible combination, backtracking lets us build the solution letter by letter, discarding incomplete paths as we find them. In the template, we define a function called `generate_letter_combinations` that takes in a string representing the phone number and returns a list of all possible letter combos. The function takes advantage of the backtracked approach by iterating over all possible combinations of digits in the phone key pad. It starts with the base case, where we've reached the end of the phone numbers, and it'll return an empty list. Then, it generates
20     0.0000       1.0000       0.0000         0.0000     tinyllama            For example, if we had a sorted list of only true and false, we can actually use binary search to find the first true occurrence. The binary search would work like this. You look at the middle of the list. If the middle value is true, you know that the first true must either be here or somewhere to the left, so you move your search to the left half. If the middle value is false, you know that the first true must be to the right, so you focus on the right half. This type of searching, based on where a condition is true and where it is false, can be reused for every single binary search problem. In fact, we actually have this free template for this on our website, Algamonster. All we need to do is find out what makes something feasible. For example, the common lead code problem, minimum in rotated sorted array, doesn't appear to follow binary search at first. After all, it's not sorted. However, there's a monotonic condition we can use to apply binary search. The condition is that any element that is less than the last element of the array is part of the rotated section, while any element greater than the last element is part of the original sorted section. We can mentally convert this to trues and falses, and just like that, we have the same true and false question we just solve. This can be applied to a ton of different questions. For more practice and an in-depth explanation, check out algo.monster. Now we'll take a look at non-linear structure problems which work with trees and graphs. In fact, instead of treating trees and graphs as two entirely different structures, you should imagine that a tree is just a graph with no cycles. The only difference between the two is that for a graph that could have cycles, you need some visited set to keep track of nodes you've already visited. This makes the patterns we will talk about applicable to both. For non-linear problems, we'll start with breadth-first search, as it's the easiest algorithm to understand and build off of. breadth-first search is an algorithm used to explore nodes in a graph or tree level by level. The idea behind this is that it starts at a given node, commonly called the root, and explores all its immediate neighbors first before moving on to the neighbors' neighbors, and so on. To keep track of the nodes that have already been visited so that we don't revisit them, BFS uses a queue, a data structure that works like a line. First in, first out. This ensures that nodes are visited in the correct order, starting from the closest to the root and moving outward. BFS is such a common algorithm that you will use all the time. Luckily for you, instead of having to derive it from scratch each time, we have this template that we can reuse. If you want to see more about how we got this template, check it out at algo.monster. Now let's see BFS in action. We're going to use this template to solve a common lead code problem, level order traversal of a binary tree. The problem asks us to give all of the levels of the tree at each depth This means we need to visit the nodes of the tree level by level, starting from the root and moving down, visiting all the nodes at each level before proceeding to the next. For example, given a tree with the root node 3 and children 9 and 20, where 20 has children 15 and 7, we need to return the values of the nodes level by level in the following order. First 3, then 9, 20, and finally 15, 16th phrase which is not a kill, we can solve this problem using BFS as its ideal for level by level traversal. The idea is to use a queue to help explore the tree one level at a time. First, we add the root node to the queue. Then for each level, we process all nodes currently in the queue by removing them and adding their values to the result. For each node we process, we also add its left and right children to the queue so they can be processed in the next level. This ensures that we visit all nodes on one level before moving to the next. By repeating this process until the queue is empty, we can traverse the entire tree level by level and collect the node values in the correct order. Finally, we return the list of node values grouped by levels. Devth first search is an algorithm that builds on the foundation of BFS but explores the tree or graph in a different way. While BFS focuses on exploring nodes level by level, DFS dives deep into one path as far as possible before exploring the next path. In BFS, we use a queue to process nodes in a breadth first manner, ensuring all nodes at one level are explored before moving to the next. In contrast, DFS uses a stack to explore nodes in a depth first manner, where you keep going down one branch until you reach the end, and then you come back to explore other branches. Most often, the stack DFS uses is the call stack. As most DFS implementations use recursion, where BFS is ideal for finding the shortest path or exploring nodes layer by layer, DFS is more suited for problems where you need to explore all paths or check every possible configuration, such as finding all paths in a tree, searching for a specific condition deep in the structure or detecting cycles. The depth first nature of DFS makes it more memory efficient in some cases, but it's less suited for problems where the shortest path or shallow exploration is required, as it doesn't guarantee finding the closest solution first. Like with BFS, DFS is a common algorithm that you will use all the time, so we have a template that we can reuse. Again, if you're curious to how this works, check it out at algo.monster. Now, let's solve a leap code problem with DFS. A common leap code problem that uses this pattern is called number of islands. This problem asks us to count the number of islands in a 2D grid where one represents land and zero represents water. An island is formed by connecting adjacent ones horizontally or vertically. We need to determine how many distinct islands exist by identifying connected components of ones in the grid. For example, in a grid where someone's are grouped together, each connected group is considered an island. Our goal is to traverse the grid, find all islands, and count them. We can solve this problem using DFS to explore each island. The idea is to iterate through each cell in the grid. When we encounter a one land, it indicates the start of an island. From there, we perform DFS to visit all connected ones all parts of the same island by exploring the neighboring cells in all four directions as deep as they go. During the DFS, we mark each visited cell by changing its value to zero to ensure we don't count it again. Once all connected cells for that island are visited, we increment the island count and continue searching the rest of the grid. This process ensures that we explore each island once and count it correctly. By the end, the total number of initializing DFS calls we make, not including the recursive calls, equals the number of islands. Backtracking is a pattern that many people struggle with, but in reality, it's actually just an extension of depth first search. In DFS, we typically traverse through a prebuilt structure like a tree or graph, where the nodes and connections are already defined. With backtracking, however, you often have to build the solution tree yourself as you explore different options, especially in combinatorial problems, where the structure of the tree isn't given explicitly, but is generated dynamically as you make decisions. In a backtracking problem, you explore all possible solutions by making a series of decisions. Each decision represents a node in the tree, and each potential decision forms a branch. As you explore a decision, you either reach a valid solution, or you hit a dead end. When a decision leads to a dead end, you backtrack to the previous decision, undo it, and try another path. Just like with all of our other patterns, we also have a template we are going to use here, which is this. Now, let's walk through it by solving a lead code problem. Letter combinations of a phone number. In this problem, we're given a string of digits, typically from 2 to 9, and we need to return all the possible letter combinations that the digits could represent using the mapping of a phone keypad. For example, if we have the digits 23, each digit corresponds to a set of letters, two maps to A, B, C, and three maps to D, E, F. Our goal is to generate every possible combination of these letters. So, for 23, some outputs could be a D, A, E, A, F, B, D, B, E, B, F, and so on. Now, our goal is to find all of these combinations. Backtracking is the optimal approach for solving this problem because it allows us to explore each possible letter one at a time while pruning invalid configurations early. Rather than brute forcing through every possible combination, backtracking lets us build the solution letter by letter, discarding incomplete paths. This approach is known as a bottom up approach, meaning we explore the smallest possible subset of the input at each step. In our case, we explore all the combinations of the digits from 1 to 10, starting with the smallest subset of 2, 3. We then explore the subsets of 4, 5, and 6, and repeat this process for each digit. This is a bottom-up approach, which means we explore smaller subsets of the larger input
21     0.0000       1.0000       0.0000         0.0000     tinyllama            For example, if we had a sorted list of only true and false, we can actually use binary search to find the first true occurrence. The binary search would work like this. You look at the middle of the list. If the middle value is true, you know that the first true must either be here or somewhere to the left, so you move your search to the left half. If the middle value is false, you know that the first true must be to the right, so you focus on the right half. This type of searching, based on where a condition is true and where it is false, can be reused for every single binary search problem. In fact, we actually have this free template for this on our website, Algamonster. All we need to do is find out what makes something feasible. For example, the common lead code problem, minimum in rotated sorted array, doesn't appear to follow binary search at first. After all, it's not sorted. However, there's a monotonic condition we can use to apply binary search. The condition is that any element that is less than the last element of the array is part of the rotated section, while any element greater than the last element is part of the original sorted section. We can mentally convert this to trues and falses, and just like that, we have the same true and false question we just solve. This can be applied to a ton of different questions. For more practice and an in-depth explanation, check out algo.monster. Now we'll take a look at non-linear structure problems which work with trees and graphs. In fact, instead of treating trees and graphs as two entirely different structures, you should imagine that a tree is just a graph with no cycles. The only difference between the two is that for a graph that could have cycles, you need some visited set to keep track of nodes you've already visited. This makes the patterns we will talk about applicable to both. For non-linear problems, we'll start with breadth-first search, as it's the easiest algorithm to understand and build off of. breadth-first search is an algorithm used to explore nodes in a graph or tree level by level. The idea behind this is that it starts at a given node, commonly called the root, and explores all its immediate neighbors first before moving on to the neighbors' neighbors, and so on. To keep track of the nodes that have already been visited so that we don't revisit them, BFS uses a queue, a data structure that works like a line. First in, first out. This ensures that nodes are visited in the correct order, starting from the closest to the root and moving outward. BFS is such a common algorithm that you will use all the time. Luckily for you, instead of having to derive it from scratch each time, we have this template that we can reuse. If you want to see more about how we got this template, check it out at algo.monster. Now let's see BFS in action. We're going to use this template to solve a common lead code problem, level order traversal of a binary tree. The problem asks us to give all of the levels of the tree at each depth This means we need to visit the nodes of the tree level by level, starting from the root and moving down, visiting all the nodes at each level before proceeding to the next. For example, given a tree with the root node 3 and children 9 and 20, where 20 has children 15 and 7, we need to return the values of the nodes level by level in the following order. First 3, then 9, 20, and finally 15, 16th phrase which is not a kill, we can solve this problem using BFS as its ideal for level by level traversal. The idea is to use a queue to help explore the tree one level at a time. First, we add the root node to the queue. Then for each level, we process all nodes currently in the queue by removing them and adding their values to the result. For each node we process, we also add its left and right children to the queue so they can be processed in the next level. This ensures that we visit all nodes on one level before moving to the next. By repeating this process until the queue is empty, we can traverse the entire tree level by level and collect the node values in the correct order. Finally, we return the list of node values grouped by levels. Devth first search is an algorithm that builds on the foundation of BFS but explores the tree or graph in a different way. While BFS focuses on exploring nodes level by level, DFS dives deep into one path as far as possible before exploring the next path. In BFS, we use a queue to process nodes in a breadth first manner, ensuring all nodes at one level are explored before moving to the next. In contrast, DFS uses a stack to explore nodes in a depth first manner, where you keep going down one branch until you reach the end, and then you come back to explore other branches. Most often, the stack DFS uses is the call stack. As most DFS implementations use recursion, where BFS is ideal for finding the shortest path or exploring nodes layer by layer, DFS is more suited for problems where you need to explore all paths or check every possible configuration, such as finding all paths in a tree, searching for a specific condition deep in the structure or detecting cycles. The depth first nature of DFS makes it more memory efficient in some cases, but it's less suited for problems where the shortest path or shallow exploration is required, as it doesn't guarantee finding the closest solution first. Like with BFS, DFS is a common algorithm that you will use all the time, so we have a template that we can reuse. Again, if you're curious to how this works, check it out at algo.monster. Now, let's solve a leap code problem with DFS. A common leap code problem that uses this pattern is called number of islands. This problem asks us to count the number of islands in a 2D grid where one represents land and zero represents water. An island is formed by connecting adjacent ones horizontally or vertically. We need to determine how many distinct islands exist by identifying connected components of ones in the grid. For example, in a grid where someone's are grouped together, each connected group is considered an island. Our goal is to traverse the grid, find all islands, and count them. We can solve this problem using DFS to explore each island. The idea is to iterate through each cell in the grid. When we encounter a one land, it indicates the start of an island. From there, we perform DFS to visit all connected ones all parts of the same island by exploring the neighboring cells in all four directions as deep as they go. During the DFS, we mark each visited cell by changing its value to zero to ensure we don't count it again. Once all connected cells for that island are visited, we increment the island count and continue searching the rest of the grid. This process ensures that we explore each island once and count it correctly. By the end, the total number of initializing DFS calls we make, not including the recursive calls, equals the number of islands. Backtracking is a pattern that many people struggle with, but in reality, it's actually just an extension of depth first search. In DFS, we typically traverse through a prebuilt structure like a tree or graph, where the nodes and connections are already defined. With backtracking, however, you often have to build the solution tree yourself as you explore different options, especially in combinatorial problems, where the structure of the tree isn't given explicitly, but is generated dynamically as you make decisions. In a backtracking problem, you explore all possible solutions by making a series of decisions. Each decision represents a node in the tree, and each potential decision forms a branch. As you explore a decision, you either reach a valid solution, or you hit a dead end. When a decision leads to a dead end, you backtrack to the previous decision, undo it, and try another path. Just like with all of our other patterns, we also have a template we are going to use here, which is this. Now, let's walk through it by solving a lead code problem. Letter combinations of a phone number. In this problem, we're given a string of digits, typically from 2 to 9, and we need to return all the possible letter combinations that the digits could represent using the mapping of a phone keypad. For example, if we have the digits 23, each digit corresponds to a set of letters, two maps to A, B, C, and three maps to D, E, F. Our goal is to generate every possible combination of these letters. So, for 23, some outputs could be a D, A, E, A, F, B, D, B, E, B, F, and so on. Now, our goal is to find all of these combinations. Backtracking is the optimal approach for solving this problem because it allows us to explore each possible letter one at a time while pruning invalid configurations early. Rather than brute forcing through every possible combination, backtracking lets us build the solution letter by letter, discarding incomplete paths. By doing this, we save space and time, and can efficiently explore all possibilities without wasting too much time on invalid paths. Backtracker is a template for solving a back tracking problem. It's an algorithmic framework that allows us write code that explores multiple possible solutions to a problem, one at the time by one at first sight. Backtrace is a simple algorithm that we've seen before. It works by iteratively building up a solution tree by backtrackning through
22     0.0000       1.0000       0.0000         0.0000     tinyllama            For example, if we had a sorted list of only true and false, we can actually use binary search to find the first true occurrence. The binary search would work like this. You look at the middle of the list. If the middle value is true, you know that the first true must either be here or somewhere to the left, so you move your search to the left half. If the middle value is false, you know that the first true must be to the right, so you focus on the right half. This type of searching, based on where a condition is true and where it is false, can be reused for every single binary search problem. In fact, we actually have this free template for this on our website, Algamonster. All we need to do is find out what makes something feasible. For example, the common lead code problem, minimum in rotated sorted array, doesn't appear to follow binary search at first. After all, it's not sorted. However, there's a monotonic condition we can use to apply binary search. The condition is that any element that is less than the last element of the array is part of the rotated section, while any element greater than the last element is part of the original sorted section. We can mentally convert this to trues and falses, and just like that, we have the same true and false question we just solve. This can be applied to a ton of different questions. For more practice and an in-depth explanation, check out algo.monster. Now we'll take a look at non-linear structure problems which work with trees and graphs. In fact, instead of treating trees and graphs as two entirely different structures, you should imagine that a tree is just a graph with no cycles. The only difference between the two is that for a graph that could have cycles, you need some visited set to keep track of nodes you've already visited. This makes the patterns we will talk about applicable to both. For non-linear problems, we'll start with breadth-first search, as it's the easiest algorithm to understand and build off of. breadth-first search is an algorithm used to explore nodes in a graph or tree level by level. The idea behind this is that it starts at a given node, commonly called the root, and explores all its immediate neighbors first before moving on to the neighbors' neighbors, and so on. To keep track of the nodes that have already been visited so that we don't revisit them, BFS uses a queue, a data structure that works like a line. First in, first out. This ensures that nodes are visited in the correct order, starting from the closest to the root and moving outward. BFS is such a common algorithm that you will use all the time. Luckily for you, instead of having to derive it from scratch each time, we have this template that we can reuse. If you want to see more about how we got this template, check it out at algo.monster. Now let's see BFS in action. We're going to use this template to solve a common lead code problem, level order traversal of a binary tree. The problem asks us to give all of the levels of the tree at each depth This means we need to visit the nodes of the tree level by level, starting from the root and moving down, visiting all the nodes at each level before proceeding to the next. For example, given a tree with the root node 3 and children 9 and 20, where 20 has children 15 and 7, we need to return the values of the nodes level by level in the following order. First 3, then 9, 20, and finally 15, 16th phrase which is not a kill, we can solve this problem using BFS as its ideal for level by level traversal. The idea is to use a queue to help explore the tree one level at a time. First, we add the root node to the queue. Then for each level, we process all nodes currently in the queue by removing them and adding their values to the result. For each node we process, we also add its left and right children to the queue so they can be processed in the next level. This ensures that we visit all nodes on one level before moving to the next. By repeating this process until the queue is empty, we can traverse the entire tree level by level and collect the node values in the correct order. Finally, we return the list of node values grouped by levels. Devth first search is an algorithm that builds on the foundation of BFS but explores the tree or graph in a different way. While BFS focuses on exploring nodes level by level, DFS dives deep into one path as far as possible before exploring the next path. In BFS, we use a queue to process nodes in a breadth first manner, ensuring all nodes at one level are explored before moving to the next. In contrast, DFS uses a stack to explore nodes in a depth first manner, where you keep going down one branch until you reach the end, and then you come back to explore other branches. Most often, the stack DFS uses is the call stack. As most DFS implementations use recursion, where BFS is ideal for finding the shortest path or exploring nodes layer by layer, DFS is more suited for problems where you need to explore all paths or check every possible configuration, such as finding all paths in a tree, searching for a specific condition deep in the structure or detecting cycles. The depth first nature of DFS makes it more memory efficient in some cases, but it's less suited for problems where the shortest path or shallow exploration is required, as it doesn't guarantee finding the closest solution first. Like with BFS, DFS is a common algorithm that you will use all the time, so we have a template that we can reuse. Again, if you're curious to how this works, check it out at algo.monster. Now, let's solve a leap code problem with DFS. A common leap code problem that uses this pattern is called number of islands. This problem asks us to count the number of islands in a 2D grid where one represents land and zero represents water. An island is formed by connecting adjacent ones horizontally or vertically. We need to determine how many distinct islands exist by identifying connected components of ones in the grid. For example, in a grid where someone's are grouped together, each connected group is considered an island. Our goal is to traverse the grid, find all islands, and count them. We can solve this problem using DFS to explore each island. The idea is to iterate through each cell in the grid. When we encounter a one land, it indicates the start of an island. From there, we perform DFS to visit all connected ones all parts of the same island by exploring the neighboring cells in all four directions as deep as they go. During the DFS, we mark each visited cell by changing its value to zero to ensure we don't count it again. Once all connected cells for that island are visited, we increment the island count and continue searching the rest of the grid. This process ensures that we explore each island once and count it correctly. By the end, the total number of initializing DFS calls we make, not including the recursive calls, equals the number of islands. Backtracking is a pattern that many people struggle with, but in reality, it's actually just an extension of depth first search. In DFS, we typically traverse through a prebuilt structure like a tree or graph, where the nodes and connections are already defined. With backtracking, however, you often have to build the solution tree yourself as you explore different options, especially in combinatorial problems, where the structure of the tree isn't given explicitly, but is generated dynamically as you make decisions. In a backtracking problem, you explore all possible solutions by making a series of decisions. Each decision represents a node in the tree, and each potential decision forms a branch. As you explore a decision, you either reach a valid solution, or you hit a dead end. When a decision leads to a dead end, you backtrack to the previous decision, undo it, and try another path. Just like with all of our other patterns, we also have a template we are going to use here, which is this. Now, let's walk through it by solving a lead code problem. Letter combinations of a phone number. In this problem, we're given a string of digits, typically from 2 to 9, and we need to return all the possible letter combinations that the digits could represent using the mapping of a phone keypad. For example, if we have the digits 23, each digit corresponds to a set of letters, two maps to A, B, C, and three maps to D, E, F. Our goal is to generate every possible combination of these letters. So, for 23, some outputs could be a D, A, E, A, F, B, D, B, E, B, F, and so on. Now, our goal is to find all of these combinations. Backtracking is the optimal approach for solving this problem because it allows us to explore each possible letter one at a time while pruning invalid configurations early. Rather than brute forcing through every possible combination, backtracking lets us build the solution letter by letter, discarding incomplete paths and returning the best possible solution. We use a stack, which keeps track of all possible letter combos, to build up the solution. At each step, we remove the top element from the stack, and if it'd be a valid letter, we push it onto the stack. If it'll cause a deadend, we pop the top elements from the bottom of the stack and try a new combination. This way, we never hit a situation where we've explored all possibilities
23     0.0000       1.0000       0.0000         0.0000     tinyllama            For example, if we had a sorted list of only true and false, we can actually use binary search to find the first true occurrence. The binary search would work like this. You look at the middle of the list. If the middle value is true, you know that the first true must either be here or somewhere to the left, so you move your search to the left half. If the middle value is false, you know that the first true must be to the right, so you focus on the right half. This type of searching, based on where a condition is true and where it is false, can be reused for every single binary search problem. In fact, we actually have this free template for this on our website, Algamonster. All we need to do is find out what makes something feasible. For example, the common lead code problem, minimum in rotated sorted array, doesn't appear to follow binary search at first. After all, it's not sorted. However, there's a monotonic condition we can use to apply binary search. The condition is that any element that is less than the last element of the array is part of the rotated section, while any element greater than the last element is part of the original sorted section. We can mentally convert this to trues and falses, and just like that, we have the same true and false question we just solve. This can be applied to a ton of different questions. For more practice and an in-depth explanation, check out algo.monster. Now we'll take a look at non-linear structure problems which work with trees and graphs. In fact, instead of treating trees and graphs as two entirely different structures, you should imagine that a tree is just a graph with no cycles. The only difference between the two is that for a graph that could have cycles, you need some visited set to keep track of nodes you've already visited. This makes the patterns we will talk about applicable to both. For non-linear problems, we'll start with breadth-first search, as it's the easiest algorithm to understand and build off of. breadth-first search is an algorithm used to explore nodes in a graph or tree level by level. The idea behind this is that it starts at a given node, commonly called the root, and explores all its immediate neighbors first before moving on to the neighbors' neighbors, and so on. To keep track of the nodes that have already been visited so that we don't revisit them, BFS uses a queue, a data structure that works like a line. First in, first out. This ensures that nodes are visited in the correct order, starting from the closest to the root and moving outward. BFS is such a common algorithm that you will use all the time. Luckily for you, instead of having to derive it from scratch each time, we have this template that we can reuse. If you want to see more about how we got this template, check it out at algo.monster. Now let's see BFS in action. We're going to use this template to solve a common lead code problem, level order traversal of a binary tree. The problem asks us to give all of the levels of the tree at each depth This means we need to visit the nodes of the tree level by level, starting from the root and moving down, visiting all the nodes at each level before proceeding to the next. For example, given a tree with the root node 3 and children 9 and 20, where 20 has children 15 and 7, we need to return the values of the nodes level by level in the following order. First 3, then 9, 20, and finally 15, 16th phrase which is not a kill, we can solve this problem using BFS as its ideal for level by level traversal. The idea is to use a queue to help explore the tree one level at a time. First, we add the root node to the queue. Then for each level, we process all nodes currently in the queue by removing them and adding their values to the result. For each node we process, we also add its left and right children to the queue so they can be processed in the next level. This ensures that we visit all nodes on one level before moving to the next. By repeating this process until the queue is empty, we can traverse the entire tree level by level and collect the node values in the correct order. Finally, we return the list of node values grouped by levels. Devth first search is an algorithm that builds on the foundation of BFS but explores the tree or graph in a different way. While BFS focuses on exploring nodes level by level, DFS dives deep into one path as far as possible before exploring the next path. In BFS, we use a queue to process nodes in a breadth first manner, ensuring all nodes at one level are explored before moving to the next. In contrast, DFS uses a stack to explore nodes in a depth first manner, where you keep going down one branch until you reach the end, and then you come back to explore other branches. Most often, the stack DFS uses is the call stack. As most DFS implementations use recursion, where BFS is ideal for finding the shortest path or exploring nodes layer by layer, DFS is more suited for problems where you need to explore all paths or check every possible configuration, such as finding all paths in a tree, searching for a specific condition deep in the structure or detecting cycles. The depth first nature of DFS makes it more memory efficient in some cases, but it's less suited for problems where the shortest path or shallow exploration is required, as it doesn't guarantee finding the closest solution first. Like with BFS, DFS is a common algorithm that you will use all the time, so we have a template that we can reuse. Again, if you're curious to how this works, check it out at algo.monster. Now, let's solve a leap code problem with DFS. A common leap code problem that uses this pattern is called number of islands. This problem asks us to count the number of islands in a 2D grid where one represents land and zero represents water. An island is formed by connecting adjacent ones horizontally or vertically. We need to determine how many distinct islands exist by identifying connected components of ones in the grid. For example, in a grid where someone's are grouped together, each connected group is considered an island. Our goal is to traverse the grid, find all islands, and count them. We can solve this problem using DFS to explore each island. The idea is to iterate through each cell in the grid. When we encounter a one land, it indicates the start of an island. From there, we perform DFS to visit all connected ones all parts of the same island by exploring the neighboring cells in all four directions as deep as they go. During the DFS, we mark each visited cell by changing its value to zero to ensure we don't count it again. Once all connected cells for that island are visited, we increment the island count and continue searching the rest of the grid. This process ensures that we explore each island once and count it correctly. By the end, the total number of initializing DFS calls we make, not including the recursive calls, equals the number of islands. Backtracking is a pattern that many people struggle with, but in reality, it's actually just an extension of depth first search. In DFS, we typically traverse through a prebuilt structure like a tree or graph, where the nodes and connections are already defined. With backtracking, however, you often have to build the solution tree yourself as you explore different options, especially in combinatorial problems, where the structure of the tree isn't given explicitly, but is generated dynamically as you make decisions. In a backtracking problem, you explore all possible solutions by making a series of decisions. Each decision represents a node in the tree, and each potential decision forms a branch. As you explore a decision, you either reach a valid solution, or you hit a dead end. When a decision leads to a dead end, you backtrack to the previous decision, undo it, and try another path. Just like with all of our other patterns, we also have a template we are going to use here, which is this. Now, let's walk through it by solving a lead code problem. Letter combinations of a phone number. In this problem, we're given a string of digits, typically from 2 to 9, and we need to return all the possible letter combinations that the digits could represent using the mapping of a phone keypad. For example, if we have the digits 23, each digit corresponds to a set of letters, two maps to A, B, C, and three maps to D, E, F. Our goal is to generate every possible combination of these letters. So, for 23, some outputs could be a D, A, E, A, F, B, D, B, E, B, F, and so on. Now, our goal is to find all of these combinations. Backtracking is the optimal approach for solving this problem because it allows us to explore each possible letter one at a time while pruning invalid configurations early. Rather than brute forcing through every possible combination, backtracking lets us build the solution letter by letter, discarding incomplete paths as we find them. This helps us avoid dead ends and find the best possible solution quickly. By using backtrack, we only need to consider the first few possible combinations, and the remaining combinations can be found by exploration of the remaining possibilities. This is why backtrack is a powerful technique in combinatorics, and it'll save you hours of time when working on combinatorial optimization problems. Let's take a closer look at how we can implement backtrack in our code
24     0.0000       1.0000       0.0000         0.0000     tinyllama            For example, if we had a sorted list of only true and false, we can actually use binary search to find the first true occurrence. The binary search would work like this. You look at the middle of the list. If the middle value is true, you know that the first true must either be here or somewhere to the left, so you move your search to the left half. If the middle value is false, you know that the first true must be to the right, so you focus on the right half. This type of searching, based on where a condition is true and where it is false, can be reused for every single binary search problem. In fact, we actually have this free template for this on our website, Algamonster. All we need to do is find out what makes something feasible. For example, the common lead code problem, minimum in rotated sorted array, doesn't appear to follow binary search at first. After all, it's not sorted. However, there's a monotonic condition we can use to apply binary search. The condition is that any element that is less than the last element of the array is part of the rotated section, while any element greater than the last element is part of the original sorted section. We can mentally convert this to trues and falses, and just like that, we have the same true and false question we just solve. This can be applied to a ton of different questions. For more practice and an in-depth explanation, check out algo.monster. Now we'll take a look at non-linear structure problems which work with trees and graphs. In fact, instead of treating trees and graphs as two entirely different structures, you should imagine that a tree is just a graph with no cycles. The only difference between the two is that for a graph that could have cycles, you need some visited set to keep track of nodes you've already visited. This makes the patterns we will talk about applicable to both. For non-linear problems, we'll start with breadth-first search, as it's the easiest algorithm to understand and build off of. breadth-first search is an algorithm used to explore nodes in a graph or tree level by level. The idea behind this is that it starts at a given node, commonly called the root, and explores all its immediate neighbors first before moving on to the neighbors' neighbors, and so on. To keep track of the nodes that have already been visited so that we don't revisit them, BFS uses a queue, a data structure that works like a line. First in, first out. This ensures that nodes are visited in the correct order, starting from the closest to the root and moving outward. BFS is such a common algorithm that you will use all the time. Luckily for you, instead of having to derive it from scratch each time, we have this template that we can reuse. If you want to see more about how we got this template, check it out at algo.monster. Now let's see BFS in action. We're going to use this template to solve a common lead code problem, level order traversal of a binary tree. The problem asks us to give all of the levels of the tree at each depth This means we need to visit the nodes of the tree level by level, starting from the root and moving down, visiting all the nodes at each level before proceeding to the next. For example, given a tree with the root node 3 and children 9 and 20, where 20 has children 15 and 7, we need to return the values of the nodes level by level in the following order. First 3, then 9, 20, and finally 15, 16th phrase which is not a kill, we can solve this problem using BFS as its ideal for level by level traversal. The idea is to use a queue to help explore the tree one level at a time. First, we add the root node to the queue. Then for each level, we process all nodes currently in the queue by removing them and adding their values to the result. For each node we process, we also add its left and right children to the queue so they can be processed in the next level. This ensures that we visit all nodes on one level before moving to the next. By repeating this process until the queue is empty, we can traverse the entire tree level by level and collect the node values in the correct order. Finally, we return the list of node values grouped by levels. Devth first search is an algorithm that builds on the foundation of BFS but explores the tree or graph in a different way. While BFS focuses on exploring nodes level by level, DFS dives deep into one path as far as possible before exploring the next path. In BFS, we use a queue to process nodes in a breadth first manner, ensuring all nodes at one level are explored before moving to the next. In contrast, DFS uses a stack to explore nodes in a depth first manner, where you keep going down one branch until you reach the end, and then you come back to explore other branches. Most often, the stack DFS uses is the call stack. As most DFS implementations use recursion, where BFS is ideal for finding the shortest path or exploring nodes layer by layer, DFS is more suited for problems where you need to explore all paths or check every possible configuration, such as finding all paths in a tree, searching for a specific condition deep in the structure or detecting cycles. The depth first nature of DFS makes it more memory efficient in some cases, but it's less suited for problems where the shortest path or shallow exploration is required, as it doesn't guarantee finding the closest solution first. Like with BFS, DFS is a common algorithm that you will use all the time, so we have a template that we can reuse. Again, if you're curious to how this works, check it out at algo.monster. Now, let's solve a leap code problem with DFS. A common leap code problem that uses this pattern is called number of islands. This problem asks us to count the number of islands in a 2D grid where one represents land and zero represents water. An island is formed by connecting adjacent ones horizontally or vertically. We need to determine how many distinct islands exist by identifying connected components of ones in the grid. For example, in a grid where someone's are grouped together, each connected group is considered an island. Our goal is to traverse the grid, find all islands, and count them. We can solve this problem using DFS to explore each island. The idea is to iterate through each cell in the grid. When we encounter a one land, it indicates the start of an island. From there, we perform DFS to visit all connected ones all parts of the same island by exploring the neighboring cells in all four directions as deep as they go. During the DFS, we mark each visited cell by changing its value to zero to ensure we don't count it again. Once all connected cells for that island are visited, we increment the island count and continue searching the rest of the grid. This process ensures that we explore each island once and count it correctly. By the end, the total number of initializing DFS calls we make, not including the recursive calls, equals the number of islands. Backtracking is a pattern that many people struggle with, but in reality, it's actually just an extension of depth first search. In DFS, we typically traverse through a prebuilt structure like a tree or graph, where the nodes and connections are already defined. With backtracking, however, you often have to build the solution tree yourself as you explore different options, especially in combinatorial problems, where the structure of the tree isn't given explicitly, but is generated dynamically as you make decisions. In a backtracking problem, you explore all possible solutions by making a series of decisions. Each decision represents a node in the tree, and each potential decision forms a branch. As you explore a decision, you either reach a valid solution, or you hit a dead end. When a decision leads to a dead end, you backtrack to the previous decision, undo it, and try another path. Just like with all of our other patterns, we also have a template we are going to use here, which is this. Now, let's walk through it by solving a lead code problem. Letter combinations of a phone number. In this problem, we're given a string of digits, typically from 2 to 9, and we need to return all the possible letter combinations that the digits could represent using the mapping of a phone keypad. For example, if we have the digits 23, each digit corresponds to a set of letters, two maps to A, B, C, and three maps to D, E, F. Our goal is to generate every possible combination of these letters. So, for 23, some outputs could be a D, A, E, A, F, B, D, B, E, B, F, and so on. Now, our goal is to find all of these combinations. Backtracking is the optimal approach for solving this problem because it allows us to explore each possible letter one at a time while pruning invalid configurations early. Rather than brute forcing through every possible combination, backtracking lets us build the solution letter by letter, discarding incomplete paths along the way. We start with the first letter, which we can assume is valid. We then explore all the remaining letters in turn, checking whether they form a valid combination. If they do, we move on to explore the next letter. If we run out of letters or reach a dead-end, we backtrack and try a different path. This pattern is known as a backtracker. We use a stack, a simple data structure, to store all the valid combinations we've
25     0.0000       1.0000       0.0000         0.0000     tinyllama            For example, if we had a sorted list of only true and false, we can actually use binary search to find the first true occurrence. The binary search would work like this. You look at the middle of the list. If the middle value is true, you know that the first true must either be here or somewhere to the left, so you move your search to the left half. If the middle value is false, you know that the first true must be to the right, so you focus on the right half. This type of searching, based on where a condition is true and where it is false, can be reused for every single binary search problem. In fact, we actually have this free template for this on our website, Algamonster. All we need to do is find out what makes something feasible. For example, the common lead code problem, minimum in rotated sorted array, doesn't appear to follow binary search at first. After all, it's not sorted. However, there's a monotonic condition we can use to apply binary search. The condition is that any element that is less than the last element of the array is part of the rotated section, while any element greater than the last element is part of the original sorted section. We can mentally convert this to trues and falses, and just like that, we have the same true and false question we just solve. This can be applied to a ton of different questions. For more practice and an in-depth explanation, check out algo.monster. Now we'll take a look at non-linear structure problems which work with trees and graphs. In fact, instead of treating trees and graphs as two entirely different structures, you should imagine that a tree is just a graph with no cycles. The only difference between the two is that for a graph that could have cycles, you need some visited set to keep track of nodes you've already visited. This makes the patterns we will talk about applicable to both. For non-linear problems, we'll start with breadth-first search, as it's the easiest algorithm to understand and build off of. breadth-first search is an algorithm used to explore nodes in a graph or tree level by level. The idea behind this is that it starts at a given node, commonly called the root, and explores all its immediate neighbors first before moving on to the neighbors' neighbors, and so on. To keep track of the nodes that have already been visited so that we don't revisit them, BFS uses a queue, a data structure that works like a line. First in, first out. This ensures that nodes are visited in the correct order, starting from the closest to the root and moving outward. BFS is such a common algorithm that you will use all the time. Luckily for you, instead of having to derive it from scratch each time, we have this template that we can reuse. If you want to see more about how we got this template, check it out at algo.monster. Now let's see BFS in action. We're going to use this template to solve a common lead code problem, level order traversal of a binary tree. The problem asks us to give all of the levels of the tree at each depth This means we need to visit the nodes of the tree level by level, starting from the root and moving down, visiting all the nodes at each level before proceeding to the next. For example, given a tree with the root node 3 and children 9 and 20, where 20 has children 15 and 7, we need to return the values of the nodes level by level in the following order. First 3, then 9, 20, and finally 15, 16th phrase which is not a kill, we can solve this problem using BFS as its ideal for level by level traversal. The idea is to use a queue to help explore the tree one level at a time. First, we add the root node to the queue. Then for each level, we process all nodes currently in the queue by removing them and adding their values to the result. For each node we process, we also add its left and right children to the queue so they can be processed in the next level. This ensures that we visit all nodes on one level before moving to the next. By repeating this process until the queue is empty, we can traverse the entire tree level by level and collect the node values in the correct order. Finally, we return the list of node values grouped by levels. Devth first search is an algorithm that builds on the foundation of BFS but explores the tree or graph in a different way. While BFS focuses on exploring nodes level by level, DFS dives deep into one path as far as possible before exploring the next path. In BFS, we use a queue to process nodes in a breadth first manner, ensuring all nodes at one level are explored before moving to the next. In contrast, DFS uses a stack to explore nodes in a depth first manner, where you keep going down one branch until you reach the end, and then you come back to explore other branches. Most often, the stack DFS uses is the call stack. As most DFS implementations use recursion, where BFS is ideal for finding the shortest path or exploring nodes layer by layer, DFS is more suited for problems where you need to explore all paths or check every possible configuration, such as finding all paths in a tree, searching for a specific condition deep in the structure or detecting cycles. The depth first nature of DFS makes it more memory efficient in some cases, but it's less suited for problems where the shortest path or shallow exploration is required, as it doesn't guarantee finding the closest solution first. Like with BFS, DFS is a common algorithm that you will use all the time, so we have a template that we can reuse. Again, if you're curious to how this works, check it out at algo.monster. Now, let's solve a leap code problem with DFS. A common leap code problem that uses this pattern is called number of islands. This problem asks us to count the number of islands in a 2D grid where one represents land and zero represents water. An island is formed by connecting adjacent ones horizontally or vertically. We need to determine how many distinct islands exist by identifying connected components of ones in the grid. For example, in a grid where someone's are grouped together, each connected group is considered an island. Our goal is to traverse the grid, find all islands, and count them. We can solve this problem using DFS to explore each island. The idea is to iterate through each cell in the grid. When we encounter a one land, it indicates the start of an island. From there, we perform DFS to visit all connected ones all parts of the same island by exploring the neighboring cells in all four directions as deep as they go. During the DFS, we mark each visited cell by changing its value to zero to ensure we don't count it again. Once all connected cells for that island are visited, we increment the island count and continue searching the rest of the grid. This process ensures that we explore each island once and count it correctly. By the end, the total number of initializing DFS calls we make, not including the recursive calls, equals the number of islands. Backtracking is a pattern that many people struggle with, but in reality, it's actually just an extension of depth first search. In DFS, we typically traverse through a prebuilt structure like a tree or graph, where the nodes and connections are already defined. With backtracking, however, you often have to build the solution tree yourself as you explore different options, especially in combinatorial problems, where the structure of the tree isn't given explicitly, but is generated dynamically as you make decisions. In a backtracking problem, you explore all possible solutions by making a series of decisions. Each decision represents a node in the tree, and each potential decision forms a branch. As you explore a decision, you either reach a valid solution, or you hit a dead end. When a decision leads to a dead end, you backtrack to the previous decision, undo it, and try another path. Just like with all of our other patterns, we also have a template we are going to use here, which is this. Now, let's walk through it by solving a lead code problem. Letter combinations of a phone number. In this problem, we're given a string of digits, typically from 2 to 9, and we need to return all the possible letter combinations that the digits could represent using the mapping of a phone keypad. For example, if we have the digits 23, each digit corresponds to a set of letters, two maps to A, B, C, and three maps to D, E, F. Our goal is to generate every possible combination of these letters. So, for 23, some outputs could be a D, A, E, A, F, B, D, B, E, B, F, and so on. Now, our goal is to find all of these combinations. Backtracking is the optimal approach for solving this problem because it allows us to explore each possible letter one at a time while pruning invalid configurations early. Rather than brute forcing through every possible combination, backtracking lets us build the solution letter by letter, discarding incomplete paths and returning the best possible solution. We create a dictionary of all possible letter mappings, and for each digit, we create a new dictionary of possible letter combos. We start by creating a dictionary for the first digit, and fill it with the letter combinations for that digit. Then, for each subsequent digit, recursively call ourselves with the current digit and the remaining digits. For every letter combo we found, we update the corresponding letter mapping in the dictionary. At the end of the function
26     0.0000       1.0000       0.0000         0.0000     tinyllama            For example, if we had a sorted list of only true and false, we can actually use binary search to find the first true occurrence. The binary search would work like this. You look at the middle of the list. If the middle value is true, you know that the first true must either be here or somewhere to the left, so you move your search to the left half. If the middle value is false, you know that the first true must be to the right, so you focus on the right half. This type of searching, based on where a condition is true and where it is false, can be reused for every single binary search problem. In fact, we actually have this free template for this on our website, Algamonster. All we need to do is find out what makes something feasible. For example, the common lead code problem, minimum in rotated sorted array, doesn't appear to follow binary search at first. After all, it's not sorted. However, there's a monotonic condition we can use to apply binary search. The condition is that any element that is less than the last element of the array is part of the rotated section, while any element greater than the last element is part of the original sorted section. We can mentally convert this to trues and falses, and just like that, we have the same true and false question we just solve. This can be applied to a ton of different questions. For more practice and an in-depth explanation, check out algo.monster. Now we'll take a look at non-linear structure problems which work with trees and graphs. In fact, instead of treating trees and graphs as two entirely different structures, you should imagine that a tree is just a graph with no cycles. The only difference between the two is that for a graph that could have cycles, you need some visited set to keep track of nodes you've already visited. This makes the patterns we will talk about applicable to both. For non-linear problems, we'll start with breadth-first search, as it's the easiest algorithm to understand and build off of. breadth-first search is an algorithm used to explore nodes in a graph or tree level by level. The idea behind this is that it starts at a given node, commonly called the root, and explores all its immediate neighbors first before moving on to the neighbors' neighbors, and so on. To keep track of the nodes that have already been visited so that we don't revisit them, BFS uses a queue, a data structure that works like a line. First in, first out. This ensures that nodes are visited in the correct order, starting from the closest to the root and moving outward. BFS is such a common algorithm that you will use all the time. Luckily for you, instead of having to derive it from scratch each time, we have this template that we can reuse. If you want to see more about how we got this template, check it out at algo.monster. Now let's see BFS in action. We're going to use this template to solve a common lead code problem, level order traversal of a binary tree. The problem asks us to give all of the levels of the tree at each depth This means we need to visit the nodes of the tree level by level, starting from the root and moving down, visiting all the nodes at each level before proceeding to the next. For example, given a tree with the root node 3 and children 9 and 20, where 20 has children 15 and 7, we need to return the values of the nodes level by level in the following order. First 3, then 9, 20, and finally 15, 16th phrase which is not a kill, we can solve this problem using BFS as its ideal for level by level traversal. The idea is to use a queue to help explore the tree one level at a time. First, we add the root node to the queue. Then for each level, we process all nodes currently in the queue by removing them and adding their values to the result. For each node we process, we also add its left and right children to the queue so they can be processed in the next level. This ensures that we visit all nodes on one level before moving to the next. By repeating this process until the queue is empty, we can traverse the entire tree level by level and collect the node values in the correct order. Finally, we return the list of node values grouped by levels. Devth first search is an algorithm that builds on the foundation of BFS but explores the tree or graph in a different way. While BFS focuses on exploring nodes level by level, DFS dives deep into one path as far as possible before exploring the next path. In BFS, we use a queue to process nodes in a breadth first manner, ensuring all nodes at one level are explored before moving to the next. In contrast, DFS uses a stack to explore nodes in a depth first manner, where you keep going down one branch until you reach the end, and then you come back to explore other branches. Most often, the stack DFS uses is the call stack. As most DFS implementations use recursion, where BFS is ideal for finding the shortest path or exploring nodes layer by layer, DFS is more suited for problems where you need to explore all paths or check every possible configuration, such as finding all paths in a tree, searching for a specific condition deep in the structure or detecting cycles. The depth first nature of DFS makes it more memory efficient in some cases, but it's less suited for problems where the shortest path or shallow exploration is required, as it doesn't guarantee finding the closest solution first. Like with BFS, DFS is a common algorithm that you will use all the time, so we have a template that we can reuse. Again, if you're curious to how this works, check it out at algo.monster. Now, let's solve a leap code problem with DFS. A common leap code problem that uses this pattern is called number of islands. This problem asks us to count the number of islands in a 2D grid where one represents land and zero represents water. An island is formed by connecting adjacent ones horizontally or vertically. We need to determine how many distinct islands exist by identifying connected components of ones in the grid. For example, in a grid where someone's are grouped together, each connected group is considered an island. Our goal is to traverse the grid, find all islands, and count them. We can solve this problem using DFS to explore each island. The idea is to iterate through each cell in the grid. When we encounter a one land, it indicates the start of an island. From there, we perform DFS to visit all connected ones all parts of the same island by exploring the neighboring cells in all four directions as deep as they go. During the DFS, we mark each visited cell by changing its value to zero to ensure we don't count it again. Once all connected cells for that island are visited, we increment the island count and continue searching the rest of the grid. This process ensures that we explore each island once and count it correctly. By the end, the total number of initializing DFS calls we make, not including the recursive calls, equals the number of islands. Backtracking is a pattern that many people struggle with, but in reality, it's actually just an extension of depth first search. In DFS, we typically traverse through a prebuilt structure like a tree or graph, where the nodes and connections are already defined. With backtracking, however, you often have to build the solution tree yourself as you explore different options, especially in combinatorial problems, where the structure of the tree isn't given explicitly, but is generated dynamically as you make decisions. In a backtracking problem, you explore all possible solutions by making a series of decisions. Each decision represents a node in the tree, and each potential decision forms a branch. As you explore a decision, you either reach a valid solution, or you hit a dead end. When a decision leads to a dead end, you backtrack to the previous decision, undo it, and try another path. Just like with all of our other patterns, we also have a template we are going to use here, which is this. Now, let's walk through it by solving a lead code problem. Letter combinations of a phone number. In this problem, we're given a string of digits, typically from 2 to 9, and we need to return all the possible letter combinations that the digits could represent using the mapping of a phone keypad. For example, if we have the digits 23, each digit corresponds to a set of letters, two maps to A, B, C, and three maps to D, E, F. Our goal is to generate every possible combination of these letters. So, for 23, some outputs could be a D, A, E, A, F, B, D, B, E, B, F, and so on. Now, our goal is to find all of these combinations. Backtracking is the optimal approach for solving this problem because it allows us to explore each possible letter one at a time while pruning invalid configurations early. Rather than brute forcing through every possible combination, backtracking lets us build the solution letter by letter, discarding incomplete paths. This way, we only need to consider the valid paths, and the final answer is the sum of all valid paths. We have a solution template that includes this template for backtrack, which we can modify to fit the problem at hand. Now that we have solved the lead code lead code problems, let me show you how to apply this template across other problems. For instance, let us say we want to find a maximum subarray sum problem. We want to calculate the maximum sub-array
27     0.0000       1.0000       0.0000         0.0000     mistral-7b-v0.3      For example, if we had a sorted list of only true and false, we can actually use binary search to find the first true occurrence. The binary search would work like this. You look at the middle of the list. If the middle value is true, you know that the first true must either be here or somewhere to the left, so you move your search to the left half. If the middle value is false, you know that the first true must be to the right, so you focus on the right half. This type of searching, based on where a condition is true and where it is false, can be reused for every single binary search problem. In fact, we actually have this free template for this on our website, Algamonster. All we need to do is find out what makes something feasible. For example, the common lead code problem, minimum in rotated sorted array, doesn't appear to follow binary search at first. After all, it's not sorted. However, there's a monotonic condition we can use to apply binary search. The condition is that any element that is less than the last element of the array is part of the rotated section, while any element greater than the last element is part of the original sorted section. We can mentally convert this to trues and falses, and just like that, we have the same true and false question we just solve. This can be applied to a ton of different questions. For more practice and an in-depth explanation, check out algo.monster. Now we'll take a look at non-linear structure problems which work with trees and graphs. In fact, instead of treating trees and graphs as two entirely different structures, you should imagine that a tree is just a graph with no cycles. The only difference between the two is that for a graph that could have cycles, you need some visited set to keep track of nodes you've already visited. This makes the patterns we will talk about applicable to both. For non-linear problems, we'll start with breadth-first search, as it's the easiest algorithm to understand and build off of. breadth-first search is an algorithm used to explore nodes in a graph or tree level by level. The idea behind this is that it starts at a given node, commonly called the root, and explores all its immediate neighbors first before moving on to the neighbors' neighbors, and so on. To keep track of the nodes that have already been visited so that we don't revisit them, BFS uses a queue, a data structure that works like a line. First in, first out. This ensures that nodes are visited in the correct order, starting from the closest to the root and moving outward. BFS is such a common algorithm that you will use all the time. Luckily for you, instead of having to derive it from scratch each time, we have this template that we can reuse. If you want to see more about how we got this template, check it out at algo.monster. Now let's see BFS in action. We're going to use this template to solve a common lead code problem, level order traversal of a binary tree. The problem asks us to give all of the levels of the tree at each depth This means we need to visit the nodes of the tree level by level, starting from the root and moving down, visiting all the nodes at each level before proceeding to the next. For example, given a tree with the root node 3 and children 9 and 20, where 20 has children 15 and 7, we need to return the values of the nodes level by level in the following order. First 3, then 9, 20, and finally 15, 16th phrase which is not a kill, we can solve this problem using BFS as its ideal for level by level traversal. The idea is to use a queue to help explore the tree one level at a time. First, we add the root node to the queue. Then for each level, we process all nodes currently in the queue by removing them and adding their values to the result. For each node we process, we also add its left and right children to the queue so they can be processed in the next level. This ensures that we visit all nodes on one level before moving to the next. By repeating this process until the queue is empty, we can traverse the entire tree level by level and collect the node values in the correct order. Finally, we return the list of node values grouped by levels. Devth first search is an algorithm that builds on the foundation of BFS but explores the tree or graph in a different way. While BFS focuses on exploring nodes level by level, DFS dives deep into one path as far as possible before exploring the next path. In BFS, we use a queue to process nodes in a breadth first manner, ensuring all nodes at one level are explored before moving to the next. In contrast, DFS uses a stack to explore nodes in a depth first manner, where you keep going down one branch until you reach the end, and then you come back to explore other branches. Most often, the stack DFS uses is the call stack. As most DFS implementations use recursion, where BFS is ideal for finding the shortest path or exploring nodes layer by layer, DFS is more suited for problems where you need to explore all paths or check every possible configuration, such as finding all paths in a tree, searching for a specific condition deep in the structure or detecting cycles. The depth first nature of DFS makes it more memory efficient in some cases, but it's less suited for problems where the shortest path or shallow exploration is required, as it doesn't guarantee finding the closest solution first. Like with BFS, DFS is a common algorithm that you will use all the time, so we have a template that we can reuse. Again, if you're curious to how this works, check it out at algo.monster. Now, let's solve a leap code problem with DFS. A common leap code problem that uses this pattern is called number of islands. This problem asks us to count the number of islands in a 2D grid where one represents land and zero represents water. An island is formed by connecting adjacent ones horizontally or vertically. We need to determine how many distinct islands exist by identifying connected components of ones in the grid. For example, in a grid where someone's are grouped together, each connected group is considered an island. Our goal is to traverse the grid, find all islands, and count them. We can solve this problem using DFS to explore each island. The idea is to iterate through each cell in the grid. When we encounter a one land, it indicates the start of an island. From there, we perform DFS to visit all connected ones all parts of the same island by exploring the neighboring cells in all four directions as deep as they go. During the DFS, we mark each visited cell by changing its value to zero to ensure we don't count it again. Once all connected cells for that island are visited, we increment the island count and continue searching the rest of the grid. This process ensures that we explore each island once and count it correctly. By the end, the total number of initializing DFS calls we make, not including the recursive calls, equals the number of islands. Backtracking is a pattern that many people struggle with, but in reality, it's actually just an extension of depth first search. In DFS, we typically traverse through a prebuilt structure like a tree or graph, where the nodes and connections are already defined. With backtracking, however, you often have to build the solution tree yourself as you explore different options, especially in combinatorial problems, where the structure of the tree isn't given explicitly, but is generated dynamically as you make decisions. In a backtracking problem, you explore all possible solutions by making a series of decisions. Each decision represents a node in the tree, and each potential decision forms a branch. As you explore a decision, you either reach a valid solution, or you hit a dead end. When a decision leads to a dead end, you backtrack to the previous decision, undo it, and try another path. Just like with all of our other patterns, we also have a template we are going to use here, which is this. Now, let's walk through it by solving a lead code problem. Letter combinations of a phone number. In this problem, we're given a string of digits, typically from 2 to 9, and we need to return all the possible letter combinations that the digits could represent using the mapping of a phone keypad. For example, if we have the digits 23, each digit corresponds to a set of letters, two maps to A, B, C, and three maps to D, E, F. Our goal is to generate every possible combination of these letters. So, for 23, some outputs could be a D, A, E, A, F, B, D, B, E, B, F, and so on. Now, our goal is to find all of these combinations. Backtracking is the optimal approach for solving this problem because it allows us to explore each possible letter one at a time while pruning invalid configurations early. Rather than brute forcing through every possible combination, backtracking lets us build the solution letter by letter, discarding incomplete paths that no longer make sense. We start by defining our base case, which in this case is when we have exhausted all digits. At that point, we simply append the current combination to our results. Otherwise, we loop through each possible character for the current digit, recursively calling ourselves with the remaining digits and updated combination. If we ever reach a dead-end, where we cannot form a valid combination, we backtrack by undoing the current choice and trying another option. By doing this, we systematically explore every possible
28     0.0000       1.0000       0.0000         0.0000     mistral-7b-v0.3      For example, if we had a sorted list of only true and false, we can actually use binary search to find the first true occurrence. The binary search would work like this. You look at the middle of the list. If the middle value is true, you know that the first true must either be here or somewhere to the left, so you move your search to the left half. If the middle value is false, you know that the first true must be to the right, so you focus on the right half. This type of searching, based on where a condition is true and where it is false, can be reused for every single binary search problem. In fact, we actually have this free template for this on our website, Algamonster. All we need to do is find out what makes something feasible. For example, the common lead code problem, minimum in rotated sorted array, doesn't appear to follow binary search at first. After all, it's not sorted. However, there's a monotonic condition we can use to apply binary search. The condition is that any element that is less than the last element of the array is part of the rotated section, while any element greater than the last element is part of the original sorted section. We can mentally convert this to trues and falses, and just like that, we have the same true and false question we just solve. This can be applied to a ton of different questions. For more practice and an in-depth explanation, check out algo.monster. Now we'll take a look at non-linear structure problems which work with trees and graphs. In fact, instead of treating trees and graphs as two entirely different structures, you should imagine that a tree is just a graph with no cycles. The only difference between the two is that for a graph that could have cycles, you need some visited set to keep track of nodes you've already visited. This makes the patterns we will talk about applicable to both. For non-linear problems, we'll start with breadth-first search, as it's the easiest algorithm to understand and build off of. breadth-first search is an algorithm used to explore nodes in a graph or tree level by level. The idea behind this is that it starts at a given node, commonly called the root, and explores all its immediate neighbors first before moving on to the neighbors' neighbors, and so on. To keep track of the nodes that have already been visited so that we don't revisit them, BFS uses a queue, a data structure that works like a line. First in, first out. This ensures that nodes are visited in the correct order, starting from the closest to the root and moving outward. BFS is such a common algorithm that you will use all the time. Luckily for you, instead of having to derive it from scratch each time, we have this template that we can reuse. If you want to see more about how we got this template, check it out at algo.monster. Now let's see BFS in action. We're going to use this template to solve a common lead code problem, level order traversal of a binary tree. The problem asks us to give all of the levels of the tree at each depth This means we need to visit the nodes of the tree level by level, starting from the root and moving down, visiting all the nodes at each level before proceeding to the next. For example, given a tree with the root node 3 and children 9 and 20, where 20 has children 15 and 7, we need to return the values of the nodes level by level in the following order. First 3, then 9, 20, and finally 15, 16th phrase which is not a kill, we can solve this problem using BFS as its ideal for level by level traversal. The idea is to use a queue to help explore the tree one level at a time. First, we add the root node to the queue. Then for each level, we process all nodes currently in the queue by removing them and adding their values to the result. For each node we process, we also add its left and right children to the queue so they can be processed in the next level. This ensures that we visit all nodes on one level before moving to the next. By repeating this process until the queue is empty, we can traverse the entire tree level by level and collect the node values in the correct order. Finally, we return the list of node values grouped by levels. Devth first search is an algorithm that builds on the foundation of BFS but explores the tree or graph in a different way. While BFS focuses on exploring nodes level by level, DFS dives deep into one path as far as possible before exploring the next path. In BFS, we use a queue to process nodes in a breadth first manner, ensuring all nodes at one level are explored before moving to the next. In contrast, DFS uses a stack to explore nodes in a depth first manner, where you keep going down one branch until you reach the end, and then you come back to explore other branches. Most often, the stack DFS uses is the call stack. As most DFS implementations use recursion, where BFS is ideal for finding the shortest path or exploring nodes layer by layer, DFS is more suited for problems where you need to explore all paths or check every possible configuration, such as finding all paths in a tree, searching for a specific condition deep in the structure or detecting cycles. The depth first nature of DFS makes it more memory efficient in some cases, but it's less suited for problems where the shortest path or shallow exploration is required, as it doesn't guarantee finding the closest solution first. Like with BFS, DFS is a common algorithm that you will use all the time, so we have a template that we can reuse. Again, if you're curious to how this works, check it out at algo.monster. Now, let's solve a leap code problem with DFS. A common leap code problem that uses this pattern is called number of islands. This problem asks us to count the number of islands in a 2D grid where one represents land and zero represents water. An island is formed by connecting adjacent ones horizontally or vertically. We need to determine how many distinct islands exist by identifying connected components of ones in the grid. For example, in a grid where someone's are grouped together, each connected group is considered an island. Our goal is to traverse the grid, find all islands, and count them. We can solve this problem using DFS to explore each island. The idea is to iterate through each cell in the grid. When we encounter a one land, it indicates the start of an island. From there, we perform DFS to visit all connected ones all parts of the same island by exploring the neighboring cells in all four directions as deep as they go. During the DFS, we mark each visited cell by changing its value to zero to ensure we don't count it again. Once all connected cells for that island are visited, we increment the island count and continue searching the rest of the grid. This process ensures that we explore each island once and count it correctly. By the end, the total number of initializing DFS calls we make, not including the recursive calls, equals the number of islands. Backtracking is a pattern that many people struggle with, but in reality, it's actually just an extension of depth first search. In DFS, we typically traverse through a prebuilt structure like a tree or graph, where the nodes and connections are already defined. With backtracking, however, you often have to build the solution tree yourself as you explore different options, especially in combinatorial problems, where the structure of the tree isn't given explicitly, but is generated dynamically as you make decisions. In a backtracking problem, you explore all possible solutions by making a series of decisions. Each decision represents a node in the tree, and each potential decision forms a branch. As you explore a decision, you either reach a valid solution, or you hit a dead end. When a decision leads to a dead end, you backtrack to the previous decision, undo it, and try another path. Just like with all of our other patterns, we also have a template we are going to use here, which is this. Now, let's walk through it by solving a lead code problem. Letter combinations of a phone number. In this problem, we're given a string of digits, typically from 2 to 9, and we need to return all the possible letter combinations that the digits could represent using the mapping of a phone keypad. For example, if we have the digits 23, each digit corresponds to a set of letters, two maps to A, B, C, and three maps to D, E, F. Our goal is to generate every possible combination of these letters. So, for 23, some outputs could be a D, A, E, A, F, B, D, B, E, B, F, and so on. Now, our goal is to find all of these combinations. Backtracking is the optimal approach for solving this problem because it allows us to explore each possible letter one at a time while pruning invalid configurations early. Rather than brute forcing through every possible combination, backtracking lets us build the solution letter by letter, discarding incomplete paths that no longer make sense. We start by defining our base case, which in this case is when we've exhausted all the digitals. At that point, we simply append the current combination to our output list and return. Otherwise, we loop through each possible option for the current digit, recursively calling ourselves for each option. During each recursive call, we update our current combination with the new letter and continue exploring the remaining digits. This continues until we'e exhausted all possibilities for the given digits and returned the final list
29     0.0000       1.0000       0.0000         0.0000     mistral-7b-v0.3      For example, if we had a sorted list of only true and false, we can actually use binary search to find the first true occurrence. The binary search would work like this. You look at the middle of the list. If the middle value is true, you know that the first true must either be here or somewhere to the left, so you move your search to the left half. If the middle value is false, you know that the first true must be to the right, so you focus on the right half. This type of searching, based on where a condition is true and where it is false, can be reused for every single binary search problem. In fact, we actually have this free template for this on our website, Algamonster. All we need to do is find out what makes something feasible. For example, the common lead code problem, minimum in rotated sorted array, doesn't appear to follow binary search at first. After all, it's not sorted. However, there's a monotonic condition we can use to apply binary search. The condition is that any element that is less than the last element of the array is part of the rotated section, while any element greater than the last element is part of the original sorted section. We can mentally convert this to trues and falses, and just like that, we have the same true and false question we just solve. This can be applied to a ton of different questions. For more practice and an in-depth explanation, check out algo.monster. Now we'll take a look at non-linear structure problems which work with trees and graphs. In fact, instead of treating trees and graphs as two entirely different structures, you should imagine that a tree is just a graph with no cycles. The only difference between the two is that for a graph that could have cycles, you need some visited set to keep track of nodes you've already visited. This makes the patterns we will talk about applicable to both. For non-linear problems, we'll start with breadth-first search, as it's the easiest algorithm to understand and build off of. breadth-first search is an algorithm used to explore nodes in a graph or tree level by level. The idea behind this is that it starts at a given node, commonly called the root, and explores all its immediate neighbors first before moving on to the neighbors' neighbors, and so on. To keep track of the nodes that have already been visited so that we don't revisit them, BFS uses a queue, a data structure that works like a line. First in, first out. This ensures that nodes are visited in the correct order, starting from the closest to the root and moving outward. BFS is such a common algorithm that you will use all the time. Luckily for you, instead of having to derive it from scratch each time, we have this template that we can reuse. If you want to see more about how we got this template, check it out at algo.monster. Now let's see BFS in action. We're going to use this template to solve a common lead code problem, level order traversal of a binary tree. The problem asks us to give all of the levels of the tree at each depth This means we need to visit the nodes of the tree level by level, starting from the root and moving down, visiting all the nodes at each level before proceeding to the next. For example, given a tree with the root node 3 and children 9 and 20, where 20 has children 15 and 7, we need to return the values of the nodes level by level in the following order. First 3, then 9, 20, and finally 15, 16th phrase which is not a kill, we can solve this problem using BFS as its ideal for level by level traversal. The idea is to use a queue to help explore the tree one level at a time. First, we add the root node to the queue. Then for each level, we process all nodes currently in the queue by removing them and adding their values to the result. For each node we process, we also add its left and right children to the queue so they can be processed in the next level. This ensures that we visit all nodes on one level before moving to the next. By repeating this process until the queue is empty, we can traverse the entire tree level by level and collect the node values in the correct order. Finally, we return the list of node values grouped by levels. Devth first search is an algorithm that builds on the foundation of BFS but explores the tree or graph in a different way. While BFS focuses on exploring nodes level by level, DFS dives deep into one path as far as possible before exploring the next path. In BFS, we use a queue to process nodes in a breadth first manner, ensuring all nodes at one level are explored before moving to the next. In contrast, DFS uses a stack to explore nodes in a depth first manner, where you keep going down one branch until you reach the end, and then you come back to explore other branches. Most often, the stack DFS uses is the call stack. As most DFS implementations use recursion, where BFS is ideal for finding the shortest path or exploring nodes layer by layer, DFS is more suited for problems where you need to explore all paths or check every possible configuration, such as finding all paths in a tree, searching for a specific condition deep in the structure or detecting cycles. The depth first nature of DFS makes it more memory efficient in some cases, but it's less suited for problems where the shortest path or shallow exploration is required, as it doesn't guarantee finding the closest solution first. Like with BFS, DFS is a common algorithm that you will use all the time, so we have a template that we can reuse. Again, if you're curious to how this works, check it out at algo.monster. Now, let's solve a leap code problem with DFS. A common leap code problem that uses this pattern is called number of islands. This problem asks us to count the number of islands in a 2D grid where one represents land and zero represents water. An island is formed by connecting adjacent ones horizontally or vertically. We need to determine how many distinct islands exist by identifying connected components of ones in the grid. For example, in a grid where someone's are grouped together, each connected group is considered an island. Our goal is to traverse the grid, find all islands, and count them. We can solve this problem using DFS to explore each island. The idea is to iterate through each cell in the grid. When we encounter a one land, it indicates the start of an island. From there, we perform DFS to visit all connected ones all parts of the same island by exploring the neighboring cells in all four directions as deep as they go. During the DFS, we mark each visited cell by changing its value to zero to ensure we don't count it again. Once all connected cells for that island are visited, we increment the island count and continue searching the rest of the grid. This process ensures that we explore each island once and count it correctly. By the end, the total number of initializing DFS calls we make, not including the recursive calls, equals the number of islands. Backtracking is a pattern that many people struggle with, but in reality, it's actually just an extension of depth first search. In DFS, we typically traverse through a prebuilt structure like a tree or graph, where the nodes and connections are already defined. With backtracking, however, you often have to build the solution tree yourself as you explore different options, especially in combinatorial problems, where the structure of the tree isn't given explicitly, but is generated dynamically as you make decisions. In a backtracking problem, you explore all possible solutions by making a series of decisions. Each decision represents a node in the tree, and each potential decision forms a branch. As you explore a decision, you either reach a valid solution, or you hit a dead end. When a decision leads to a dead end, you backtrack to the previous decision, undo it, and try another path. Just like with all of our other patterns, we also have a template we are going to use here, which is this. Now, let's walk through it by solving a lead code problem. Letter combinations of a phone number. In this problem, we're given a string of digits, typically from 2 to 9, and we need to return all the possible letter combinations that the digits could represent using the mapping of a phone keypad. For example, if we have the digits 23, each digit corresponds to a set of letters, two maps to A, B, C, and three maps to D, E, F. Our goal is to generate every possible combination of these letters. So, for 23, some outputs could be a D, A, E, A, F, B, D, B, E, B, F, and so on. Now, our goal is to find all of these combinations. Backtracking is the optimal approach for solving this problem because it allows us to explore each possible letter one at a time while pruning invalid configurations early. Rather than brute forcing through every possible combination, backtracking lets us build the solution letter by letter, discarding incomplete paths that no longer make sense. We start by defining the base case, which in this case is when we've exhausted all digits and found a valid combination. At this point, we simply append the combination to our results. Next, we define the recursion step, which involves choosing a letter for the current digit. Since each digit has multiple possible letters, we loop through all of them, checking whether the chosen letter is valid. If it is, we move on to choose the next letter for our next digit. If not
30     0.0000       1.0000       0.0000         0.0000     mistral-7b-v0.3      For example, if we had a sorted list of only true and false, we can actually use binary search to find the first true occurrence. The binary search would work like this. You look at the middle of the list. If the middle value is true, you know that the first true must either be here or somewhere to the left, so you move your search to the left half. If the middle value is false, you know that the first true must be to the right, so you focus on the right half. This type of searching, based on where a condition is true and where it is false, can be reused for every single binary search problem. In fact, we actually have this free template for this on our website, Algamonster. All we need to do is find out what makes something feasible. For example, the common lead code problem, minimum in rotated sorted array, doesn't appear to follow binary search at first. After all, it's not sorted. However, there's a monotonic condition we can use to apply binary search. The condition is that any element that is less than the last element of the array is part of the rotated section, while any element greater than the last element is part of the original sorted section. We can mentally convert this to trues and falses, and just like that, we have the same true and false question we just solve. This can be applied to a ton of different questions. For more practice and an in-depth explanation, check out algo.monster. Now we'll take a look at non-linear structure problems which work with trees and graphs. In fact, instead of treating trees and graphs as two entirely different structures, you should imagine that a tree is just a graph with no cycles. The only difference between the two is that for a graph that could have cycles, you need some visited set to keep track of nodes you've already visited. This makes the patterns we will talk about applicable to both. For non-linear problems, we'll start with breadth-first search, as it's the easiest algorithm to understand and build off of. breadth-first search is an algorithm used to explore nodes in a graph or tree level by level. The idea behind this is that it starts at a given node, commonly called the root, and explores all its immediate neighbors first before moving on to the neighbors' neighbors, and so on. To keep track of the nodes that have already been visited so that we don't revisit them, BFS uses a queue, a data structure that works like a line. First in, first out. This ensures that nodes are visited in the correct order, starting from the closest to the root and moving outward. BFS is such a common algorithm that you will use all the time. Luckily for you, instead of having to derive it from scratch each time, we have this template that we can reuse. If you want to see more about how we got this template, check it out at algo.monster. Now let's see BFS in action. We're going to use this template to solve a common lead code problem, level order traversal of a binary tree. The problem asks us to give all of the levels of the tree at each depth This means we need to visit the nodes of the tree level by level, starting from the root and moving down, visiting all the nodes at each level before proceeding to the next. For example, given a tree with the root node 3 and children 9 and 20, where 20 has children 15 and 7, we need to return the values of the nodes level by level in the following order. First 3, then 9, 20, and finally 15, 16th phrase which is not a kill, we can solve this problem using BFS as its ideal for level by level traversal. The idea is to use a queue to help explore the tree one level at a time. First, we add the root node to the queue. Then for each level, we process all nodes currently in the queue by removing them and adding their values to the result. For each node we process, we also add its left and right children to the queue so they can be processed in the next level. This ensures that we visit all nodes on one level before moving to the next. By repeating this process until the queue is empty, we can traverse the entire tree level by level and collect the node values in the correct order. Finally, we return the list of node values grouped by levels. Devth first search is an algorithm that builds on the foundation of BFS but explores the tree or graph in a different way. While BFS focuses on exploring nodes level by level, DFS dives deep into one path as far as possible before exploring the next path. In BFS, we use a queue to process nodes in a breadth first manner, ensuring all nodes at one level are explored before moving to the next. In contrast, DFS uses a stack to explore nodes in a depth first manner, where you keep going down one branch until you reach the end, and then you come back to explore other branches. Most often, the stack DFS uses is the call stack. As most DFS implementations use recursion, where BFS is ideal for finding the shortest path or exploring nodes layer by layer, DFS is more suited for problems where you need to explore all paths or check every possible configuration, such as finding all paths in a tree, searching for a specific condition deep in the structure or detecting cycles. The depth first nature of DFS makes it more memory efficient in some cases, but it's less suited for problems where the shortest path or shallow exploration is required, as it doesn't guarantee finding the closest solution first. Like with BFS, DFS is a common algorithm that you will use all the time, so we have a template that we can reuse. Again, if you're curious to how this works, check it out at algo.monster. Now, let's solve a leap code problem with DFS. A common leap code problem that uses this pattern is called number of islands. This problem asks us to count the number of islands in a 2D grid where one represents land and zero represents water. An island is formed by connecting adjacent ones horizontally or vertically. We need to determine how many distinct islands exist by identifying connected components of ones in the grid. For example, in a grid where someone's are grouped together, each connected group is considered an island. Our goal is to traverse the grid, find all islands, and count them. We can solve this problem using DFS to explore each island. The idea is to iterate through each cell in the grid. When we encounter a one land, it indicates the start of an island. From there, we perform DFS to visit all connected ones all parts of the same island by exploring the neighboring cells in all four directions as deep as they go. During the DFS, we mark each visited cell by changing its value to zero to ensure we don't count it again. Once all connected cells for that island are visited, we increment the island count and continue searching the rest of the grid. This process ensures that we explore each island once and count it correctly. By the end, the total number of initializing DFS calls we make, not including the recursive calls, equals the number of islands. Backtracking is a pattern that many people struggle with, but in reality, it's actually just an extension of depth first search. In DFS, we typically traverse through a prebuilt structure like a tree or graph, where the nodes and connections are already defined. With backtracking, however, you often have to build the solution tree yourself as you explore different options, especially in combinatorial problems, where the structure of the tree isn't given explicitly, but is generated dynamically as you make decisions. In a backtracking problem, you explore all possible solutions by making a series of decisions. Each decision represents a node in the tree, and each potential decision forms a branch. As you explore a decision, you either reach a valid solution, or you hit a dead end. When a decision leads to a dead end, you backtrack to the previous decision, undo it, and try another path. Just like with all of our other patterns, we also have a template we are going to use here, which is this. Now, let's walk through it by solving a lead code problem. Letter combinations of a phone number. In this problem, we're given a string of digits, typically from 2 to 9, and we need to return all the possible letter combinations that the digits could represent using the mapping of a phone keypad. For example, if we have the digits 23, each digit corresponds to a set of letters, two maps to A, B, C, and three maps to D, E, F. Our goal is to generate every possible combination of these letters. So, for 23, some outputs could be a D, A, E, A, F, B, D, B, E, B, F, and so on. Now, our goal is to find all of these combinations. Backtracking is the optimal approach for solving this problem because it allows us to explore each possible letter one at a time while pruning invalid configurations early. Rather than brute forcing through every possible combination, backtracking lets us build the solution letter by letter, discarding incomplete paths that no longer make sense. We start by defining our base case, which in this case is when we've reached the end of the input string. At this point, we simply return the current combination. Otherwise, we pick the next digit in the input, map it to its corresponding letters, and recursively call the function for each letter. For every letter we try, we append it to the current solution and recursive call the remaining digits. When the recursion ends, we remove the last appended letter and try the next
31     0.0000       1.0000       0.0000         0.0000     mistral-7b-v0.3      For example, if we had a sorted list of only true and false, we can actually use binary search to find the first true occurrence. The binary search would work like this. You look at the middle of the list. If the middle value is true, you know that the first true must either be here or somewhere to the left, so you move your search to the left half. If the middle value is false, you know that the first true must be to the right, so you focus on the right half. This type of searching, based on where a condition is true and where it is false, can be reused for every single binary search problem. In fact, we actually have this free template for this on our website, Algamonster. All we need to do is find out what makes something feasible. For example, the common lead code problem, minimum in rotated sorted array, doesn't appear to follow binary search at first. After all, it's not sorted. However, there's a monotonic condition we can use to apply binary search. The condition is that any element that is less than the last element of the array is part of the rotated section, while any element greater than the last element is part of the original sorted section. We can mentally convert this to trues and falses, and just like that, we have the same true and false question we just solve. This can be applied to a ton of different questions. For more practice and an in-depth explanation, check out algo.monster. Now we'll take a look at non-linear structure problems which work with trees and graphs. In fact, instead of treating trees and graphs as two entirely different structures, you should imagine that a tree is just a graph with no cycles. The only difference between the two is that for a graph that could have cycles, you need some visited set to keep track of nodes you've already visited. This makes the patterns we will talk about applicable to both. For non-linear problems, we'll start with breadth-first search, as it's the easiest algorithm to understand and build off of. breadth-first search is an algorithm used to explore nodes in a graph or tree level by level. The idea behind this is that it starts at a given node, commonly called the root, and explores all its immediate neighbors first before moving on to the neighbors' neighbors, and so on. To keep track of the nodes that have already been visited so that we don't revisit them, BFS uses a queue, a data structure that works like a line. First in, first out. This ensures that nodes are visited in the correct order, starting from the closest to the root and moving outward. BFS is such a common algorithm that you will use all the time. Luckily for you, instead of having to derive it from scratch each time, we have this template that we can reuse. If you want to see more about how we got this template, check it out at algo.monster. Now let's see BFS in action. We're going to use this template to solve a common lead code problem, level order traversal of a binary tree. The problem asks us to give all of the levels of the tree at each depth This means we need to visit the nodes of the tree level by level, starting from the root and moving down, visiting all the nodes at each level before proceeding to the next. For example, given a tree with the root node 3 and children 9 and 20, where 20 has children 15 and 7, we need to return the values of the nodes level by level in the following order. First 3, then 9, 20, and finally 15, 16th phrase which is not a kill, we can solve this problem using BFS as its ideal for level by level traversal. The idea is to use a queue to help explore the tree one level at a time. First, we add the root node to the queue. Then for each level, we process all nodes currently in the queue by removing them and adding their values to the result. For each node we process, we also add its left and right children to the queue so they can be processed in the next level. This ensures that we visit all nodes on one level before moving to the next. By repeating this process until the queue is empty, we can traverse the entire tree level by level and collect the node values in the correct order. Finally, we return the list of node values grouped by levels. Devth first search is an algorithm that builds on the foundation of BFS but explores the tree or graph in a different way. While BFS focuses on exploring nodes level by level, DFS dives deep into one path as far as possible before exploring the next path. In BFS, we use a queue to process nodes in a breadth first manner, ensuring all nodes at one level are explored before moving to the next. In contrast, DFS uses a stack to explore nodes in a depth first manner, where you keep going down one branch until you reach the end, and then you come back to explore other branches. Most often, the stack DFS uses is the call stack. As most DFS implementations use recursion, where BFS is ideal for finding the shortest path or exploring nodes layer by layer, DFS is more suited for problems where you need to explore all paths or check every possible configuration, such as finding all paths in a tree, searching for a specific condition deep in the structure or detecting cycles. The depth first nature of DFS makes it more memory efficient in some cases, but it's less suited for problems where the shortest path or shallow exploration is required, as it doesn't guarantee finding the closest solution first. Like with BFS, DFS is a common algorithm that you will use all the time, so we have a template that we can reuse. Again, if you're curious to how this works, check it out at algo.monster. Now, let's solve a leap code problem with DFS. A common leap code problem that uses this pattern is called number of islands. This problem asks us to count the number of islands in a 2D grid where one represents land and zero represents water. An island is formed by connecting adjacent ones horizontally or vertically. We need to determine how many distinct islands exist by identifying connected components of ones in the grid. For example, in a grid where someone's are grouped together, each connected group is considered an island. Our goal is to traverse the grid, find all islands, and count them. We can solve this problem using DFS to explore each island. The idea is to iterate through each cell in the grid. When we encounter a one land, it indicates the start of an island. From there, we perform DFS to visit all connected ones all parts of the same island by exploring the neighboring cells in all four directions as deep as they go. During the DFS, we mark each visited cell by changing its value to zero to ensure we don't count it again. Once all connected cells for that island are visited, we increment the island count and continue searching the rest of the grid. This process ensures that we explore each island once and count it correctly. By the end, the total number of initializing DFS calls we make, not including the recursive calls, equals the number of islands. Backtracking is a pattern that many people struggle with, but in reality, it's actually just an extension of depth first search. In DFS, we typically traverse through a prebuilt structure like a tree or graph, where the nodes and connections are already defined. With backtracking, however, you often have to build the solution tree yourself as you explore different options, especially in combinatorial problems, where the structure of the tree isn't given explicitly, but is generated dynamically as you make decisions. In a backtracking problem, you explore all possible solutions by making a series of decisions. Each decision represents a node in the tree, and each potential decision forms a branch. As you explore a decision, you either reach a valid solution, or you hit a dead end. When a decision leads to a dead end, you backtrack to the previous decision, undo it, and try another path. Just like with all of our other patterns, we also have a template we are going to use here, which is this. Now, let's walk through it by solving a lead code problem. Letter combinations of a phone number. In this problem, we're given a string of digits, typically from 2 to 9, and we need to return all the possible letter combinations that the digits could represent using the mapping of a phone keypad. For example, if we have the digits 23, each digit corresponds to a set of letters, two maps to A, B, C, and three maps to D, E, F. Our goal is to generate every possible combination of these letters. So, for 23, some outputs could be a D, A, E, A, F, B, D, B, E, B, F, and so on. Now, our goal is to find all of these combinations. Backtracking is the optimal approach for solving this problem because it allows us to explore each possible letter one at a time while pruning invalid configurations early. Rather than brute forcing through every possible combination, backtracking lets us build the solution letter by letter, discarding incomplete paths that no longer make sense. We start by defining our base case, which in this case is when we have exhausted all digits. At this point, we simply return our current combination. Otherwise, we pick up the next digit, map it to its corresponding letters, and loop through each letter. For every letter, we recursively call the function with the remaining digits and the updated combination. If we find a valid combination, we store it and continue exploring other possibilities. If none of the remaining combinations are valid, we backtrack
32     0.0000       1.0000       0.0000         0.0000     mistral-7b-v0.3      For example, if we had a sorted list of only true and false, we can actually use binary search to find the first true occurrence. The binary search would work like this. You look at the middle of the list. If the middle value is true, you know that the first true must either be here or somewhere to the left, so you move your search to the left half. If the middle value is false, you know that the first true must be to the right, so you focus on the right half. This type of searching, based on where a condition is true and where it is false, can be reused for every single binary search problem. In fact, we actually have this free template for this on our website, Algamonster. All we need to do is find out what makes something feasible. For example, the common lead code problem, minimum in rotated sorted array, doesn't appear to follow binary search at first. After all, it's not sorted. However, there's a monotonic condition we can use to apply binary search. The condition is that any element that is less than the last element of the array is part of the rotated section, while any element greater than the last element is part of the original sorted section. We can mentally convert this to trues and falses, and just like that, we have the same true and false question we just solve. This can be applied to a ton of different questions. For more practice and an in-depth explanation, check out algo.monster. Now we'll take a look at non-linear structure problems which work with trees and graphs. In fact, instead of treating trees and graphs as two entirely different structures, you should imagine that a tree is just a graph with no cycles. The only difference between the two is that for a graph that could have cycles, you need some visited set to keep track of nodes you've already visited. This makes the patterns we will talk about applicable to both. For non-linear problems, we'll start with breadth-first search, as it's the easiest algorithm to understand and build off of. breadth-first search is an algorithm used to explore nodes in a graph or tree level by level. The idea behind this is that it starts at a given node, commonly called the root, and explores all its immediate neighbors first before moving on to the neighbors' neighbors, and so on. To keep track of the nodes that have already been visited so that we don't revisit them, BFS uses a queue, a data structure that works like a line. First in, first out. This ensures that nodes are visited in the correct order, starting from the closest to the root and moving outward. BFS is such a common algorithm that you will use all the time. Luckily for you, instead of having to derive it from scratch each time, we have this template that we can reuse. If you want to see more about how we got this template, check it out at algo.monster. Now let's see BFS in action. We're going to use this template to solve a common lead code problem, level order traversal of a binary tree. The problem asks us to give all of the levels of the tree at each depth This means we need to visit the nodes of the tree level by level, starting from the root and moving down, visiting all the nodes at each level before proceeding to the next. For example, given a tree with the root node 3 and children 9 and 20, where 20 has children 15 and 7, we need to return the values of the nodes level by level in the following order. First 3, then 9, 20, and finally 15, 16th phrase which is not a kill, we can solve this problem using BFS as its ideal for level by level traversal. The idea is to use a queue to help explore the tree one level at a time. First, we add the root node to the queue. Then for each level, we process all nodes currently in the queue by removing them and adding their values to the result. For each node we process, we also add its left and right children to the queue so they can be processed in the next level. This ensures that we visit all nodes on one level before moving to the next. By repeating this process until the queue is empty, we can traverse the entire tree level by level and collect the node values in the correct order. Finally, we return the list of node values grouped by levels. Devth first search is an algorithm that builds on the foundation of BFS but explores the tree or graph in a different way. While BFS focuses on exploring nodes level by level, DFS dives deep into one path as far as possible before exploring the next path. In BFS, we use a queue to process nodes in a breadth first manner, ensuring all nodes at one level are explored before moving to the next. In contrast, DFS uses a stack to explore nodes in a depth first manner, where you keep going down one branch until you reach the end, and then you come back to explore other branches. Most often, the stack DFS uses is the call stack. As most DFS implementations use recursion, where BFS is ideal for finding the shortest path or exploring nodes layer by layer, DFS is more suited for problems where you need to explore all paths or check every possible configuration, such as finding all paths in a tree, searching for a specific condition deep in the structure or detecting cycles. The depth first nature of DFS makes it more memory efficient in some cases, but it's less suited for problems where the shortest path or shallow exploration is required, as it doesn't guarantee finding the closest solution first. Like with BFS, DFS is a common algorithm that you will use all the time, so we have a template that we can reuse. Again, if you're curious to how this works, check it out at algo.monster. Now, let's solve a leap code problem with DFS. A common leap code problem that uses this pattern is called number of islands. This problem asks us to count the number of islands in a 2D grid where one represents land and zero represents water. An island is formed by connecting adjacent ones horizontally or vertically. We need to determine how many distinct islands exist by identifying connected components of ones in the grid. For example, in a grid where someone's are grouped together, each connected group is considered an island. Our goal is to traverse the grid, find all islands, and count them. We can solve this problem using DFS to explore each island. The idea is to iterate through each cell in the grid. When we encounter a one land, it indicates the start of an island. From there, we perform DFS to visit all connected ones all parts of the same island by exploring the neighboring cells in all four directions as deep as they go. During the DFS, we mark each visited cell by changing its value to zero to ensure we don't count it again. Once all connected cells for that island are visited, we increment the island count and continue searching the rest of the grid. This process ensures that we explore each island once and count it correctly. By the end, the total number of initializing DFS calls we make, not including the recursive calls, equals the number of islands. Backtracking is a pattern that many people struggle with, but in reality, it's actually just an extension of depth first search. In DFS, we typically traverse through a prebuilt structure like a tree or graph, where the nodes and connections are already defined. With backtracking, however, you often have to build the solution tree yourself as you explore different options, especially in combinatorial problems, where the structure of the tree isn't given explicitly, but is generated dynamically as you make decisions. In a backtracking problem, you explore all possible solutions by making a series of decisions. Each decision represents a node in the tree, and each potential decision forms a branch. As you explore a decision, you either reach a valid solution, or you hit a dead end. When a decision leads to a dead end, you backtrack to the previous decision, undo it, and try another path. Just like with all of our other patterns, we also have a template we are going to use here, which is this. Now, let's walk through it by solving a lead code problem. Letter combinations of a phone number. In this problem, we're given a string of digits, typically from 2 to 9, and we need to return all the possible letter combinations that the digits could represent using the mapping of a phone keypad. For example, if we have the digits 23, each digit corresponds to a set of letters, two maps to A, B, C, and three maps to D, E, F. Our goal is to generate every possible combination of these letters. So, for 23, some outputs could be a D, A, E, A, F, B, D, B, E, B, F, and so on. Now, our goal is to find all of these combinations. Backtracking is the optimal approach for solving this problem because it allows us to explore each possible letter one at a time while pruning invalid configurations early. Rather than brute forcing through every possible combination, backtracking lets us build the solution letter by letter, discarding incomplete paths that no longer make sense. We start by defining a function that takes a list of digites as input, and returns a list containing all possible combinations. Inside the function, we initialize an empty list to store the final results. Next, we define a helper function that generates all possible letter choices for a given digit. For the sake of simplicity, we assume that the digit is between 2 and 9. We then define a recursive function that performs the actual backtrack. This function takes three arguments: the current index, the current
33     0.0000       1.0000       0.0000         0.0000     mistral-7b-v0.3      For example, if we had a sorted list of only true and false, we can actually use binary search to find the first true occurrence. The binary search would work like this. You look at the middle of the list. If the middle value is true, you know that the first true must either be here or somewhere to the left, so you move your search to the left half. If the middle value is false, you know that the first true must be to the right, so you focus on the right half. This type of searching, based on where a condition is true and where it is false, can be reused for every single binary search problem. In fact, we actually have this free template for this on our website, Algamonster. All we need to do is find out what makes something feasible. For example, the common lead code problem, minimum in rotated sorted array, doesn't appear to follow binary search at first. After all, it's not sorted. However, there's a monotonic condition we can use to apply binary search. The condition is that any element that is less than the last element of the array is part of the rotated section, while any element greater than the last element is part of the original sorted section. We can mentally convert this to trues and falses, and just like that, we have the same true and false question we just solve. This can be applied to a ton of different questions. For more practice and an in-depth explanation, check out algo.monster. Now we'll take a look at non-linear structure problems which work with trees and graphs. In fact, instead of treating trees and graphs as two entirely different structures, you should imagine that a tree is just a graph with no cycles. The only difference between the two is that for a graph that could have cycles, you need some visited set to keep track of nodes you've already visited. This makes the patterns we will talk about applicable to both. For non-linear problems, we'll start with breadth-first search, as it's the easiest algorithm to understand and build off of. breadth-first search is an algorithm used to explore nodes in a graph or tree level by level. The idea behind this is that it starts at a given node, commonly called the root, and explores all its immediate neighbors first before moving on to the neighbors' neighbors, and so on. To keep track of the nodes that have already been visited so that we don't revisit them, BFS uses a queue, a data structure that works like a line. First in, first out. This ensures that nodes are visited in the correct order, starting from the closest to the root and moving outward. BFS is such a common algorithm that you will use all the time. Luckily for you, instead of having to derive it from scratch each time, we have this template that we can reuse. If you want to see more about how we got this template, check it out at algo.monster. Now let's see BFS in action. We're going to use this template to solve a common lead code problem, level order traversal of a binary tree. The problem asks us to give all of the levels of the tree at each depth This means we need to visit the nodes of the tree level by level, starting from the root and moving down, visiting all the nodes at each level before proceeding to the next. For example, given a tree with the root node 3 and children 9 and 20, where 20 has children 15 and 7, we need to return the values of the nodes level by level in the following order. First 3, then 9, 20, and finally 15, 16th phrase which is not a kill, we can solve this problem using BFS as its ideal for level by level traversal. The idea is to use a queue to help explore the tree one level at a time. First, we add the root node to the queue. Then for each level, we process all nodes currently in the queue by removing them and adding their values to the result. For each node we process, we also add its left and right children to the queue so they can be processed in the next level. This ensures that we visit all nodes on one level before moving to the next. By repeating this process until the queue is empty, we can traverse the entire tree level by level and collect the node values in the correct order. Finally, we return the list of node values grouped by levels. Devth first search is an algorithm that builds on the foundation of BFS but explores the tree or graph in a different way. While BFS focuses on exploring nodes level by level, DFS dives deep into one path as far as possible before exploring the next path. In BFS, we use a queue to process nodes in a breadth first manner, ensuring all nodes at one level are explored before moving to the next. In contrast, DFS uses a stack to explore nodes in a depth first manner, where you keep going down one branch until you reach the end, and then you come back to explore other branches. Most often, the stack DFS uses is the call stack. As most DFS implementations use recursion, where BFS is ideal for finding the shortest path or exploring nodes layer by layer, DFS is more suited for problems where you need to explore all paths or check every possible configuration, such as finding all paths in a tree, searching for a specific condition deep in the structure or detecting cycles. The depth first nature of DFS makes it more memory efficient in some cases, but it's less suited for problems where the shortest path or shallow exploration is required, as it doesn't guarantee finding the closest solution first. Like with BFS, DFS is a common algorithm that you will use all the time, so we have a template that we can reuse. Again, if you're curious to how this works, check it out at algo.monster. Now, let's solve a leap code problem with DFS. A common leap code problem that uses this pattern is called number of islands. This problem asks us to count the number of islands in a 2D grid where one represents land and zero represents water. An island is formed by connecting adjacent ones horizontally or vertically. We need to determine how many distinct islands exist by identifying connected components of ones in the grid. For example, in a grid where someone's are grouped together, each connected group is considered an island. Our goal is to traverse the grid, find all islands, and count them. We can solve this problem using DFS to explore each island. The idea is to iterate through each cell in the grid. When we encounter a one land, it indicates the start of an island. From there, we perform DFS to visit all connected ones all parts of the same island by exploring the neighboring cells in all four directions as deep as they go. During the DFS, we mark each visited cell by changing its value to zero to ensure we don't count it again. Once all connected cells for that island are visited, we increment the island count and continue searching the rest of the grid. This process ensures that we explore each island once and count it correctly. By the end, the total number of initializing DFS calls we make, not including the recursive calls, equals the number of islands. Backtracking is a pattern that many people struggle with, but in reality, it's actually just an extension of depth first search. In DFS, we typically traverse through a prebuilt structure like a tree or graph, where the nodes and connections are already defined. With backtracking, however, you often have to build the solution tree yourself as you explore different options, especially in combinatorial problems, where the structure of the tree isn't given explicitly, but is generated dynamically as you make decisions. In a backtracking problem, you explore all possible solutions by making a series of decisions. Each decision represents a node in the tree, and each potential decision forms a branch. As you explore a decision, you either reach a valid solution, or you hit a dead end. When a decision leads to a dead end, you backtrack to the previous decision, undo it, and try another path. Just like with all of our other patterns, we also have a template we are going to use here, which is this. Now, let's walk through it by solving a lead code problem. Letter combinations of a phone number. In this problem, we're given a string of digits, typically from 2 to 9, and we need to return all the possible letter combinations that the digits could represent using the mapping of a phone keypad. For example, if we have the digits 23, each digit corresponds to a set of letters, two maps to A, B, C, and three maps to D, E, F. Our goal is to generate every possible combination of these letters. So, for 23, some outputs could be a D, A, E, A, F, B, D, B, E, B, F, and so on. Now, our goal is to find all of these combinations. Backtracking is the optimal approach for solving this problem because it allows us to explore each possible letter one at a time while pruning invalid configurations early. Rather than brute forcing through every possible combination, backtracking lets us build the solution letter by letter, discarding incomplete paths that no longer make sense. We start by defining a helper function that takes a current index and a partial solution. It then iterates through each remaining digit, checks if the digit is valid for the current position, and if so, adds the corresponding letters to the partial solution and recursively calls itself for the next position. If we reach the last position without hitting a dead-end, we append the final solution to our results. If a deadend is reached, we backtrack by undoing the last decision and trying a new path
34     0.0000       1.0000       0.0000         0.0000     mistral-7b-v0.3      For example, if we had a sorted list of only true and false, we can actually use binary search to find the first true occurrence. The binary search would work like this. You look at the middle of the list. If the middle value is true, you know that the first true must either be here or somewhere to the left, so you move your search to the left half. If the middle value is false, you know that the first true must be to the right, so you focus on the right half. This type of searching, based on where a condition is true and where it is false, can be reused for every single binary search problem. In fact, we actually have this free template for this on our website, Algamonster. All we need to do is find out what makes something feasible. For example, the common lead code problem, minimum in rotated sorted array, doesn't appear to follow binary search at first. After all, it's not sorted. However, there's a monotonic condition we can use to apply binary search. The condition is that any element that is less than the last element of the array is part of the rotated section, while any element greater than the last element is part of the original sorted section. We can mentally convert this to trues and falses, and just like that, we have the same true and false question we just solve. This can be applied to a ton of different questions. For more practice and an in-depth explanation, check out algo.monster. Now we'll take a look at non-linear structure problems which work with trees and graphs. In fact, instead of treating trees and graphs as two entirely different structures, you should imagine that a tree is just a graph with no cycles. The only difference between the two is that for a graph that could have cycles, you need some visited set to keep track of nodes you've already visited. This makes the patterns we will talk about applicable to both. For non-linear problems, we'll start with breadth-first search, as it's the easiest algorithm to understand and build off of. breadth-first search is an algorithm used to explore nodes in a graph or tree level by level. The idea behind this is that it starts at a given node, commonly called the root, and explores all its immediate neighbors first before moving on to the neighbors' neighbors, and so on. To keep track of the nodes that have already been visited so that we don't revisit them, BFS uses a queue, a data structure that works like a line. First in, first out. This ensures that nodes are visited in the correct order, starting from the closest to the root and moving outward. BFS is such a common algorithm that you will use all the time. Luckily for you, instead of having to derive it from scratch each time, we have this template that we can reuse. If you want to see more about how we got this template, check it out at algo.monster. Now let's see BFS in action. We're going to use this template to solve a common lead code problem, level order traversal of a binary tree. The problem asks us to give all of the levels of the tree at each depth This means we need to visit the nodes of the tree level by level, starting from the root and moving down, visiting all the nodes at each level before proceeding to the next. For example, given a tree with the root node 3 and children 9 and 20, where 20 has children 15 and 7, we need to return the values of the nodes level by level in the following order. First 3, then 9, 20, and finally 15, 16th phrase which is not a kill, we can solve this problem using BFS as its ideal for level by level traversal. The idea is to use a queue to help explore the tree one level at a time. First, we add the root node to the queue. Then for each level, we process all nodes currently in the queue by removing them and adding their values to the result. For each node we process, we also add its left and right children to the queue so they can be processed in the next level. This ensures that we visit all nodes on one level before moving to the next. By repeating this process until the queue is empty, we can traverse the entire tree level by level and collect the node values in the correct order. Finally, we return the list of node values grouped by levels. Devth first search is an algorithm that builds on the foundation of BFS but explores the tree or graph in a different way. While BFS focuses on exploring nodes level by level, DFS dives deep into one path as far as possible before exploring the next path. In BFS, we use a queue to process nodes in a breadth first manner, ensuring all nodes at one level are explored before moving to the next. In contrast, DFS uses a stack to explore nodes in a depth first manner, where you keep going down one branch until you reach the end, and then you come back to explore other branches. Most often, the stack DFS uses is the call stack. As most DFS implementations use recursion, where BFS is ideal for finding the shortest path or exploring nodes layer by layer, DFS is more suited for problems where you need to explore all paths or check every possible configuration, such as finding all paths in a tree, searching for a specific condition deep in the structure or detecting cycles. The depth first nature of DFS makes it more memory efficient in some cases, but it's less suited for problems where the shortest path or shallow exploration is required, as it doesn't guarantee finding the closest solution first. Like with BFS, DFS is a common algorithm that you will use all the time, so we have a template that we can reuse. Again, if you're curious to how this works, check it out at algo.monster. Now, let's solve a leap code problem with DFS. A common leap code problem that uses this pattern is called number of islands. This problem asks us to count the number of islands in a 2D grid where one represents land and zero represents water. An island is formed by connecting adjacent ones horizontally or vertically. We need to determine how many distinct islands exist by identifying connected components of ones in the grid. For example, in a grid where someone's are grouped together, each connected group is considered an island. Our goal is to traverse the grid, find all islands, and count them. We can solve this problem using DFS to explore each island. The idea is to iterate through each cell in the grid. When we encounter a one land, it indicates the start of an island. From there, we perform DFS to visit all connected ones all parts of the same island by exploring the neighboring cells in all four directions as deep as they go. During the DFS, we mark each visited cell by changing its value to zero to ensure we don't count it again. Once all connected cells for that island are visited, we increment the island count and continue searching the rest of the grid. This process ensures that we explore each island once and count it correctly. By the end, the total number of initializing DFS calls we make, not including the recursive calls, equals the number of islands. Backtracking is a pattern that many people struggle with, but in reality, it's actually just an extension of depth first search. In DFS, we typically traverse through a prebuilt structure like a tree or graph, where the nodes and connections are already defined. With backtracking, however, you often have to build the solution tree yourself as you explore different options, especially in combinatorial problems, where the structure of the tree isn't given explicitly, but is generated dynamically as you make decisions. In a backtracking problem, you explore all possible solutions by making a series of decisions. Each decision represents a node in the tree, and each potential decision forms a branch. As you explore a decision, you either reach a valid solution, or you hit a dead end. When a decision leads to a dead end, you backtrack to the previous decision, undo it, and try another path. Just like with all of our other patterns, we also have a template we are going to use here, which is this. Now, let's walk through it by solving a lead code problem. Letter combinations of a phone number. In this problem, we're given a string of digits, typically from 2 to 9, and we need to return all the possible letter combinations that the digits could represent using the mapping of a phone keypad. For example, if we have the digits 23, each digit corresponds to a set of letters, two maps to A, B, C, and three maps to D, E, F. Our goal is to generate every possible combination of these letters. So, for 23, some outputs could be a D, A, E, A, F, B, D, B, E, B, F, and so on. Now, our goal is to find all of these combinations. Backtracking is the optimal approach for solving this problem because it allows us to explore each possible letter one at a time while pruning invalid configurations early. Rather than brute forcing through every possible combination, backtracking lets us build the solution letter by letter, discarding incomplete paths that no longer make sense. We start by defining our base case, which in this case is when we have exhausted all digits. At this point, we simply append the current combination to our results and return. Otherwise, we loop through each possible option for the current digit, recursively calling ourselves with the updated combination and the remaining digits until we reach the base case. This pattern ensures that every valid combination is found and that we avoid duplicates. One important thing to note is that we need a way to prune invalid combinations
35     0.0000       1.0000       0.0000         0.0000     mistral-7b-v0.3      For example, if we had a sorted list of only true and false, we can actually use binary search to find the first true occurrence. The binary search would work like this. You look at the middle of the list. If the middle value is true, you know that the first true must either be here or somewhere to the left, so you move your search to the left half. If the middle value is false, you know that the first true must be to the right, so you focus on the right half. This type of searching, based on where a condition is true and where it is false, can be reused for every single binary search problem. In fact, we actually have this free template for this on our website, Algamonster. All we need to do is find out what makes something feasible. For example, the common lead code problem, minimum in rotated sorted array, doesn't appear to follow binary search at first. After all, it's not sorted. However, there's a monotonic condition we can use to apply binary search. The condition is that any element that is less than the last element of the array is part of the rotated section, while any element greater than the last element is part of the original sorted section. We can mentally convert this to trues and falses, and just like that, we have the same true and false question we just solve. This can be applied to a ton of different questions. For more practice and an in-depth explanation, check out algo.monster. Now we'll take a look at non-linear structure problems which work with trees and graphs. In fact, instead of treating trees and graphs as two entirely different structures, you should imagine that a tree is just a graph with no cycles. The only difference between the two is that for a graph that could have cycles, you need some visited set to keep track of nodes you've already visited. This makes the patterns we will talk about applicable to both. For non-linear problems, we'll start with breadth-first search, as it's the easiest algorithm to understand and build off of. breadth-first search is an algorithm used to explore nodes in a graph or tree level by level. The idea behind this is that it starts at a given node, commonly called the root, and explores all its immediate neighbors first before moving on to the neighbors' neighbors, and so on. To keep track of the nodes that have already been visited so that we don't revisit them, BFS uses a queue, a data structure that works like a line. First in, first out. This ensures that nodes are visited in the correct order, starting from the closest to the root and moving outward. BFS is such a common algorithm that you will use all the time. Luckily for you, instead of having to derive it from scratch each time, we have this template that we can reuse. If you want to see more about how we got this template, check it out at algo.monster. Now let's see BFS in action. We're going to use this template to solve a common lead code problem, level order traversal of a binary tree. The problem asks us to give all of the levels of the tree at each depth This means we need to visit the nodes of the tree level by level, starting from the root and moving down, visiting all the nodes at each level before proceeding to the next. For example, given a tree with the root node 3 and children 9 and 20, where 20 has children 15 and 7, we need to return the values of the nodes level by level in the following order. First 3, then 9, 20, and finally 15, 16th phrase which is not a kill, we can solve this problem using BFS as its ideal for level by level traversal. The idea is to use a queue to help explore the tree one level at a time. First, we add the root node to the queue. Then for each level, we process all nodes currently in the queue by removing them and adding their values to the result. For each node we process, we also add its left and right children to the queue so they can be processed in the next level. This ensures that we visit all nodes on one level before moving to the next. By repeating this process until the queue is empty, we can traverse the entire tree level by level and collect the node values in the correct order. Finally, we return the list of node values grouped by levels. Devth first search is an algorithm that builds on the foundation of BFS but explores the tree or graph in a different way. While BFS focuses on exploring nodes level by level, DFS dives deep into one path as far as possible before exploring the next path. In BFS, we use a queue to process nodes in a breadth first manner, ensuring all nodes at one level are explored before moving to the next. In contrast, DFS uses a stack to explore nodes in a depth first manner, where you keep going down one branch until you reach the end, and then you come back to explore other branches. Most often, the stack DFS uses is the call stack. As most DFS implementations use recursion, where BFS is ideal for finding the shortest path or exploring nodes layer by layer, DFS is more suited for problems where you need to explore all paths or check every possible configuration, such as finding all paths in a tree, searching for a specific condition deep in the structure or detecting cycles. The depth first nature of DFS makes it more memory efficient in some cases, but it's less suited for problems where the shortest path or shallow exploration is required, as it doesn't guarantee finding the closest solution first. Like with BFS, DFS is a common algorithm that you will use all the time, so we have a template that we can reuse. Again, if you're curious to how this works, check it out at algo.monster. Now, let's solve a leap code problem with DFS. A common leap code problem that uses this pattern is called number of islands. This problem asks us to count the number of islands in a 2D grid where one represents land and zero represents water. An island is formed by connecting adjacent ones horizontally or vertically. We need to determine how many distinct islands exist by identifying connected components of ones in the grid. For example, in a grid where someone's are grouped together, each connected group is considered an island. Our goal is to traverse the grid, find all islands, and count them. We can solve this problem using DFS to explore each island. The idea is to iterate through each cell in the grid. When we encounter a one land, it indicates the start of an island. From there, we perform DFS to visit all connected ones all parts of the same island by exploring the neighboring cells in all four directions as deep as they go. During the DFS, we mark each visited cell by changing its value to zero to ensure we don't count it again. Once all connected cells for that island are visited, we increment the island count and continue searching the rest of the grid. This process ensures that we explore each island once and count it correctly. By the end, the total number of initializing DFS calls we make, not including the recursive calls, equals the number of islands. Backtracking is a pattern that many people struggle with, but in reality, it's actually just an extension of depth first search. In DFS, we typically traverse through a prebuilt structure like a tree or graph, where the nodes and connections are already defined. With backtracking, however, you often have to build the solution tree yourself as you explore different options, especially in combinatorial problems, where the structure of the tree isn't given explicitly, but is generated dynamically as you make decisions. In a backtracking problem, you explore all possible solutions by making a series of decisions. Each decision represents a node in the tree, and each potential decision forms a branch. As you explore a decision, you either reach a valid solution, or you hit a dead end. When a decision leads to a dead end, you backtrack to the previous decision, undo it, and try another path. Just like with all of our other patterns, we also have a template we are going to use here, which is this. Now, let's walk through it by solving a lead code problem. Letter combinations of a phone number. In this problem, we're given a string of digits, typically from 2 to 9, and we need to return all the possible letter combinations that the digits could represent using the mapping of a phone keypad. For example, if we have the digits 23, each digit corresponds to a set of letters, two maps to A, B, C, and three maps to D, E, F. Our goal is to generate every possible combination of these letters. So, for 23, some outputs could be a D, A, E, A, F, B, D, B, E, B, F, and so on. Now, our goal is to find all of these combinations. Backtracking is the optimal approach for solving this problem because it allows us to explore each possible letter one at a time while pruning invalid configurations early. Rather than brute forcing through every possible combination, backtracking lets us build the solution letter by letter, discarding incomplete paths that no longer make sense. We start by defining the base case, which in this case is when we have exhausted all digits and found a valid combination. Next, we define the helper function that generates the combinations for a given digit. It takes in the current index, the current partial combination, and the remaining digits. Inside this function, we loop over the allowed letters for the current digit and append them to the current combination. We then recursively call ourselves for the remaining indices and digits after the current one. Finally we return
36     0.0000       1.0000       0.0000         0.0000     gemma2-9b            For example, if we had a sorted list of only true and false, we can actually use binary search to find the first true occurrence. The binary search would work like this. You look at the middle of the list. If the middle value is true, you know that the first true must either be here or somewhere to the left, so you move your search to the left half. If the middle value is false, you know that the first true must be to the right, so you focus on the right half. This type of searching, based on where a condition is true and where it is false, can be reused for every single binary search problem. In fact, we actually have this free template for this on our website, Algamonster. All we need to do is find out what makes something feasible. For example, the common lead code problem, minimum in rotated sorted array, doesn't appear to follow binary search at first. After all, it's not sorted. However, there's a monotonic condition we can use to apply binary search. The condition is that any element that is less than the last element of the array is part of the rotated section, while any element greater than the last element is part of the original sorted section. We can mentally convert this to trues and falses, and just like that, we have the same true and false question we just solve. This can be applied to a ton of different questions. For more practice and an in-depth explanation, check out algo.monster. Now we'll take a look at non-linear structure problems which work with trees and graphs. In fact, instead of treating trees and graphs as two entirely different structures, you should imagine that a tree is just a graph with no cycles. The only difference between the two is that for a graph that could have cycles, you need some visited set to keep track of nodes you've already visited. This makes the patterns we will talk about applicable to both. For non-linear problems, we'll start with breadth-first search, as it's the easiest algorithm to understand and build off of. breadth-first search is an algorithm used to explore nodes in a graph or tree level by level. The idea behind this is that it starts at a given node, commonly called the root, and explores all its immediate neighbors first before moving on to the neighbors' neighbors, and so on. To keep track of the nodes that have already been visited so that we don't revisit them, BFS uses a queue, a data structure that works like a line. First in, first out. This ensures that nodes are visited in the correct order, starting from the closest to the root and moving outward. BFS is such a common algorithm that you will use all the time. Luckily for you, instead of having to derive it from scratch each time, we have this template that we can reuse. If you want to see more about how we got this template, check it out at algo.monster. Now let's see BFS in action. We're going to use this template to solve a common lead code problem, level order traversal of a binary tree. The problem asks us to give all of the levels of the tree at each depth This means we need to visit the nodes of the tree level by level, starting from the root and moving down, visiting all the nodes at each level before proceeding to the next. For example, given a tree with the root node 3 and children 9 and 20, where 20 has children 15 and 7, we need to return the values of the nodes level by level in the following order. First 3, then 9, 20, and finally 15, 16th phrase which is not a kill, we can solve this problem using BFS as its ideal for level by level traversal. The idea is to use a queue to help explore the tree one level at a time. First, we add the root node to the queue. Then for each level, we process all nodes currently in the queue by removing them and adding their values to the result. For each node we process, we also add its left and right children to the queue so they can be processed in the next level. This ensures that we visit all nodes on one level before moving to the next. By repeating this process until the queue is empty, we can traverse the entire tree level by level and collect the node values in the correct order. Finally, we return the list of node values grouped by levels. Devth first search is an algorithm that builds on the foundation of BFS but explores the tree or graph in a different way. While BFS focuses on exploring nodes level by level, DFS dives deep into one path as far as possible before exploring the next path. In BFS, we use a queue to process nodes in a breadth first manner, ensuring all nodes at one level are explored before moving to the next. In contrast, DFS uses a stack to explore nodes in a depth first manner, where you keep going down one branch until you reach the end, and then you come back to explore other branches. Most often, the stack DFS uses is the call stack. As most DFS implementations use recursion, where BFS is ideal for finding the shortest path or exploring nodes layer by layer, DFS is more suited for problems where you need to explore all paths or check every possible configuration, such as finding all paths in a tree, searching for a specific condition deep in the structure or detecting cycles. The depth first nature of DFS makes it more memory efficient in some cases, but it's less suited for problems where the shortest path or shallow exploration is required, as it doesn't guarantee finding the closest solution first. Like with BFS, DFS is a common algorithm that you will use all the time, so we have a template that we can reuse. Again, if you're curious to how this works, check it out at algo.monster. Now, let's solve a leap code problem with DFS. A common leap code problem that uses this pattern is called number of islands. This problem asks us to count the number of islands in a 2D grid where one represents land and zero represents water. An island is formed by connecting adjacent ones horizontally or vertically. We need to determine how many distinct islands exist by identifying connected components of ones in the grid. For example, in a grid where someone's are grouped together, each connected group is considered an island. Our goal is to traverse the grid, find all islands, and count them. We can solve this problem using DFS to explore each island. The idea is to iterate through each cell in the grid. When we encounter a one land, it indicates the start of an island. From there, we perform DFS to visit all connected ones all parts of the same island by exploring the neighboring cells in all four directions as deep as they go. During the DFS, we mark each visited cell by changing its value to zero to ensure we don't count it again. Once all connected cells for that island are visited, we increment the island count and continue searching the rest of the grid. This process ensures that we explore each island once and count it correctly. By the end, the total number of initializing DFS calls we make, not including the recursive calls, equals the number of islands. Backtracking is a pattern that many people struggle with, but in reality, it's actually just an extension of depth first search. In DFS, we typically traverse through a prebuilt structure like a tree or graph, where the nodes and connections are already defined. With backtracking, however, you often have to build the solution tree yourself as you explore different options, especially in combinatorial problems, where the structure of the tree isn't given explicitly, but is generated dynamically as you make decisions. In a backtracking problem, you explore all possible solutions by making a series of decisions. Each decision represents a node in the tree, and each potential decision forms a branch. As you explore a decision, you either reach a valid solution, or you hit a dead end. When a decision leads to a dead end, you backtrack to the previous decision, undo it, and try another path. Just like with all of our other patterns, we also have a template we are going to use here, which is this. Now, let's walk through it by solving a lead code problem. Letter combinations of a phone number. In this problem, we're given a string of digits, typically from 2 to 9, and we need to return all the possible letter combinations that the digits could represent using the mapping of a phone keypad. For example, if we have the digits 23, each digit corresponds to a set of letters, two maps to A, B, C, and three maps to D, E, F. Our goal is to generate every possible combination of these letters. So, for 23, some outputs could be a D, A, E, A, F, B, D, B, E, B, F, and so on. Now, our goal is to find all of these combinations. Backtracking is the optimal approach for solving this problem because it allows us to explore each possible letter one at a time while pruning invalid configurations early. Rather than brute forcing through every possible combination, backtracking lets us build the solution letter by letter, discarding incomplete paths that no longer make sense. We start by taking the input digits and starts by checking if the input is empty, returning an empty list if so. It initializes an empty list ains to store the results and defines a recursive helper function, DFS, to perform depth first search, DFS. The DFS function tracks the current position in the digits string using start index and builds combinations step by step by adding letters to the path list. If the start index reaches the length of the digits, the path is complete, so it adds the current combination to the results list. Otherwise, it iterates over the corresponding letters for the digit at the start position and recursively calls DFS for each letter, appending the letter to the current path. After exploring all possibilities for a particular digit, it removes the last added letter from the path to backtrack and consider alternative choices
37     0.0000       1.0000       0.0000         0.0000     qwen2-7b             For example, if we had a sorted list of only true and false, we can actually use binary search to find the first true occurrence. The binary search would work like this. You look at the middle of the list. If the middle value is true, you know that the first true must either be here or somewhere to the left, so you move your search to the left half. If the middle value is false, you know that the first true must be to the right, so you focus on the right half. This type of searching, based on where a condition is true and where it is false, can be reused for every single binary search problem. In fact, we actually have this free template for this on our website, Algamonster. All we need to do is find out what makes something feasible. For example, the common lead code problem, minimum in rotated sorted array, doesn't appear to follow binary search at first. After all, it's not sorted. However, there's a monotonic condition we can use to apply binary search. The condition is that any element that is less than the last element of the array is part of the rotated section, while any element greater than the last element is part of the original sorted section. We can mentally convert this to trues and falses, and just like that, we have the same true and false question we just solve. This can be applied to a ton of different questions. For more practice and an in-depth explanation, check out algo.monster. Now we'll take a look at non-linear structure problems which work with trees and graphs. In fact, instead of treating trees and graphs as two entirely different structures, you should imagine that a tree is just a graph with no cycles. The only difference between the two is that for a graph that could have cycles, you need some visited set to keep track of nodes you've already visited. This makes the patterns we will talk about applicable to both. For non-linear problems, we'll start with breadth-first search, as it's the easiest algorithm to understand and build off of. breadth-first search is an algorithm used to explore nodes in a graph or tree level by level. The idea behind this is that it starts at a given node, commonly called the root, and explores all its immediate neighbors first before moving on to the neighbors' neighbors, and so on. To keep track of the nodes that have already been visited so that we don't revisit them, BFS uses a queue, a data structure that works like a line. First in, first out. This ensures that nodes are visited in the correct order, starting from the closest to the root and moving outward. BFS is such a common algorithm that you will use all the time. Luckily for you, instead of having to derive it from scratch each time, we have this template that we can reuse. If you want to see more about how we got this template, check it out at algo.monster. Now let's see BFS in action. We're going to use this template to solve a common lead code problem, level order traversal of a binary tree. The problem asks us to give all of the levels of the tree at each depth This means we need to visit the nodes of the tree level by level, starting from the root and moving down, visiting all the nodes at each level before proceeding to the next. For example, given a tree with the root node 3 and children 9 and 20, where 20 has children 15 and 7, we need to return the values of the nodes level by level in the following order. First 3, then 9, 20, and finally 15, 16th phrase which is not a kill, we can solve this problem using BFS as its ideal for level by level traversal. The idea is to use a queue to help explore the tree one level at a time. First, we add the root node to the queue. Then for each level, we process all nodes currently in the queue by removing them and adding their values to the result. For each node we process, we also add its left and right children to the queue so they can be processed in the next level. This ensures that we visit all nodes on one level before moving to the next. By repeating this process until the queue is empty, we can traverse the entire tree level by level and collect the node values in the correct order. Finally, we return the list of node values grouped by levels. Devth first search is an algorithm that builds on the foundation of BFS but explores the tree or graph in a different way. While BFS focuses on exploring nodes level by level, DFS dives deep into one path as far as possible before exploring the next path. In BFS, we use a queue to process nodes in a breadth first manner, ensuring all nodes at one level are explored before moving to the next. In contrast, DFS uses a stack to explore nodes in a depth first manner, where you keep going down one branch until you reach the end, and then you come back to explore other branches. Most often, the stack DFS uses is the call stack. As most DFS implementations use recursion, where BFS is ideal for finding the shortest path or exploring nodes layer by layer, DFS is more suited for problems where you need to explore all paths or check every possible configuration, such as finding all paths in a tree, searching for a specific condition deep in the structure or detecting cycles. The depth first nature of DFS makes it more memory efficient in some cases, but it's less suited for problems where the shortest path or shallow exploration is required, as it doesn't guarantee finding the closest solution first. Like with BFS, DFS is a common algorithm that you will use all the time, so we have a template that we can reuse. Again, if you're curious to how this works, check it out at algo.monster. Now, let's solve a leap code problem with DFS. A common leap code problem that uses this pattern is called number of islands. This problem asks us to count the number of islands in a 2D grid where one represents land and zero represents water. An island is formed by connecting adjacent ones horizontally or vertically. We need to determine how many distinct islands exist by identifying connected components of ones in the grid. For example, in a grid where someone's are grouped together, each connected group is considered an island. Our goal is to traverse the grid, find all islands, and count them. We can solve this problem using DFS to explore each island. The idea is to iterate through each cell in the grid. When we encounter a one land, it indicates the start of an island. From there, we perform DFS to visit all connected ones all parts of the same island by exploring the neighboring cells in all four directions as deep as they go. During the DFS, we mark each visited cell by changing its value to zero to ensure we don't count it again. Once all connected cells for that island are visited, we increment the island count and continue searching the rest of the grid. This process ensures that we explore each island once and count it correctly. By the end, the total number of initializing DFS calls we make, not including the recursive calls, equals the number of islands. Backtracking is a pattern that many people struggle with, but in reality, it's actually just an extension of depth first search. In DFS, we typically traverse through a prebuilt structure like a tree or graph, where the nodes and connections are already defined. With backtracking, however, you often have to build the solution tree yourself as you explore different options, especially in combinatorial problems, where the structure of the tree isn't given explicitly, but is generated dynamically as you make decisions. In a backtracking problem, you explore all possible solutions by making a series of decisions. Each decision represents a node in the tree, and each potential decision forms a branch. As you explore a decision, you either reach a valid solution, or you hit a dead end. When a decision leads to a dead end, you backtrack to the previous decision, undo it, and try another path. Just like with all of our other patterns, we also have a template we are going to use here, which is this. Now, let's walk through it by solving a lead code problem. Letter combinations of a phone number. In this problem, we're given a string of digits, typically from 2 to 9, and we need to return all the possible letter combinations that the digits could represent using the mapping of a phone keypad. For example, if we have the digits 23, each digit corresponds to a set of letters, two maps to A, B, C, and three maps to D, E, F. Our goal is to generate every possible combination of these letters. So, for 23, some outputs could be a D, A, E, A, F, B, D, B, E, B, F, and so on. Now, our goal is to find all of these combinations. Backtracking is the optimal approach for solving this problem because it allows us to explore each possible letter one at a time while pruning invalid configurations early. Rather than brute forcing through every possible combination, backtracking lets us build the solution letter by letter, discarding incomplete paths that no longer make sense. We start by taking the input digits and starts by checking if the input is empty, returning an empty list if so. It initializes an empty list ains to store the results and defines a recursive helper function, DFS, to perform depth first search, DFS. The DFS function tracks the current position in the digits string using start index and builds combinations step by step by adding letters to the path list. If the start index reaches the length of the digits, the current path is a complete combination, so it's joined into a string and added to ANDs. For each digit encountered, the corresponding letters are retrieved from the mapping dictionary. Then, for each letter, DFS recursively calls itself, passing along the updated path and advancing the start pointer. This continues until all possibilities are exhausted. Finally returns the combined list of all found combinations. That covers our discussion on the main algorithms and patterns used in coding interviews. These include sorting and searching techniques like quicksort, mergesort, binary search, and others, as well as dynamic programming approaches like tabulation and memoization. Additionally
38     0.0000       1.0000       0.0000         0.0000     qwen2-7b             For example, if we had a sorted list of only true and false, we can actually use binary search to find the first true occurrence. The binary search would work like this. You look at the middle of the list. If the middle value is true, you know that the first true must either be here or somewhere to the left, so you move your search to the left half. If the middle value is false, you know that the first true must be to the right, so you focus on the right half. This type of searching, based on where a condition is true and where it is false, can be reused for every single binary search problem. In fact, we actually have this free template for this on our website, Algamonster. All we need to do is find out what makes something feasible. For example, the common lead code problem, minimum in rotated sorted array, doesn't appear to follow binary search at first. After all, it's not sorted. However, there's a monotonic condition we can use to apply binary search. The condition is that any element that is less than the last element of the array is part of the rotated section, while any element greater than the last element is part of the original sorted section. We can mentally convert this to trues and falses, and just like that, we have the same true and false question we just solve. This can be applied to a ton of different questions. For more practice and an in-depth explanation, check out algo.monster. Now we'll take a look at non-linear structure problems which work with trees and graphs. In fact, instead of treating trees and graphs as two entirely different structures, you should imagine that a tree is just a graph with no cycles. The only difference between the two is that for a graph that could have cycles, you need some visited set to keep track of nodes you've already visited. This makes the patterns we will talk about applicable to both. For non-linear problems, we'll start with breadth-first search, as it's the easiest algorithm to understand and build off of. breadth-first search is an algorithm used to explore nodes in a graph or tree level by level. The idea behind this is that it starts at a given node, commonly called the root, and explores all its immediate neighbors first before moving on to the neighbors' neighbors, and so on. To keep track of the nodes that have already been visited so that we don't revisit them, BFS uses a queue, a data structure that works like a line. First in, first out. This ensures that nodes are visited in the correct order, starting from the closest to the root and moving outward. BFS is such a common algorithm that you will use all the time. Luckily for you, instead of having to derive it from scratch each time, we have this template that we can reuse. If you want to see more about how we got this template, check it out at algo.monster. Now let's see BFS in action. We're going to use this template to solve a common lead code problem, level order traversal of a binary tree. The problem asks us to give all of the levels of the tree at each depth This means we need to visit the nodes of the tree level by level, starting from the root and moving down, visiting all the nodes at each level before proceeding to the next. For example, given a tree with the root node 3 and children 9 and 20, where 20 has children 15 and 7, we need to return the values of the nodes level by level in the following order. First 3, then 9, 20, and finally 15, 16th phrase which is not a kill, we can solve this problem using BFS as its ideal for level by level traversal. The idea is to use a queue to help explore the tree one level at a time. First, we add the root node to the queue. Then for each level, we process all nodes currently in the queue by removing them and adding their values to the result. For each node we process, we also add its left and right children to the queue so they can be processed in the next level. This ensures that we visit all nodes on one level before moving to the next. By repeating this process until the queue is empty, we can traverse the entire tree level by level and collect the node values in the correct order. Finally, we return the list of node values grouped by levels. Devth first search is an algorithm that builds on the foundation of BFS but explores the tree or graph in a different way. While BFS focuses on exploring nodes level by level, DFS dives deep into one path as far as possible before exploring the next path. In BFS, we use a queue to process nodes in a breadth first manner, ensuring all nodes at one level are explored before moving to the next. In contrast, DFS uses a stack to explore nodes in a depth first manner, where you keep going down one branch until you reach the end, and then you come back to explore other branches. Most often, the stack DFS uses is the call stack. As most DFS implementations use recursion, where BFS is ideal for finding the shortest path or exploring nodes layer by layer, DFS is more suited for problems where you need to explore all paths or check every possible configuration, such as finding all paths in a tree, searching for a specific condition deep in the structure or detecting cycles. The depth first nature of DFS makes it more memory efficient in some cases, but it's less suited for problems where the shortest path or shallow exploration is required, as it doesn't guarantee finding the closest solution first. Like with BFS, DFS is a common algorithm that you will use all the time, so we have a template that we can reuse. Again, if you're curious to how this works, check it out at algo.monster. Now, let's solve a leap code problem with DFS. A common leap code problem that uses this pattern is called number of islands. This problem asks us to count the number of islands in a 2D grid where one represents land and zero represents water. An island is formed by connecting adjacent ones horizontally or vertically. We need to determine how many distinct islands exist by identifying connected components of ones in the grid. For example, in a grid where someone's are grouped together, each connected group is considered an island. Our goal is to traverse the grid, find all islands, and count them. We can solve this problem using DFS to explore each island. The idea is to iterate through each cell in the grid. When we encounter a one land, it indicates the start of an island. From there, we perform DFS to visit all connected ones all parts of the same island by exploring the neighboring cells in all four directions as deep as they go. During the DFS, we mark each visited cell by changing its value to zero to ensure we don't count it again. Once all connected cells for that island are visited, we increment the island count and continue searching the rest of the grid. This process ensures that we explore each island once and count it correctly. By the end, the total number of initializing DFS calls we make, not including the recursive calls, equals the number of islands. Backtracking is a pattern that many people struggle with, but in reality, it's actually just an extension of depth first search. In DFS, we typically traverse through a prebuilt structure like a tree or graph, where the nodes and connections are already defined. With backtracking, however, you often have to build the solution tree yourself as you explore different options, especially in combinatorial problems, where the structure of the tree isn't given explicitly, but is generated dynamically as you make decisions. In a backtracking problem, you explore all possible solutions by making a series of decisions. Each decision represents a node in the tree, and each potential decision forms a branch. As you explore a decision, you either reach a valid solution, or you hit a dead end. When a decision leads to a dead end, you backtrack to the previous decision, undo it, and try another path. Just like with all of our other patterns, we also have a template we are going to use here, which is this. Now, let's walk through it by solving a lead code problem. Letter combinations of a phone number. In this problem, we're given a string of digits, typically from 2 to 9, and we need to return all the possible letter combinations that the digits could represent using the mapping of a phone keypad. For example, if we have the digits 23, each digit corresponds to a set of letters, two maps to A, B, C, and three maps to D, E, F. Our goal is to generate every possible combination of these letters. So, for 23, some outputs could be a D, A, E, A, F, B, D, B, E, B, F, and so on. Now, our goal is to find all of these combinations. Backtracking is the optimal approach for solving this problem because it allows us to explore each possible letter one at a time while pruning invalid configurations early. Rather than brute forcing through every possible combination, backtracking lets us build the solution letter by letter, discarding incomplete paths that no longer make sense. We start by taking the input digits and starts by checking if the input is empty, returning an empty list if so. It initializes an empty list ains to store the results and defines a recursive helper function, DFS, to perform depth first search, DFS. The DFS function tracks the current position in the digits string using start index and builds combinations step by step by adding letters to the path list. If the start index reaches the length of the digits, the current path is a complete combination, so it's joined into a string and added to ANDs. For each digit encountered, the corresponding letters are fetched from the phone key map dictionary. Then, for each letter, DFS recursively calls itself with the updated start index incremented by one, effectively moving to a new letter in the sequence. After processing all letters for a particular digit, the function backtracks by popping the last letter from the path and continues trying the next available option. This backtracking ensures that when a path becomes invalid (i.e., it exceeds the allowed characters), the algorithm immediately returns to the parent choice
39     0.0000       1.0000       0.0000         0.0000     llama3.1-8b          For example, if we had a sorted list of only true and false, we can actually use binary search to find the first true occurrence. The binary search would work like this. You look at the middle of the list. If the middle value is true, you know that the first true must either be here or somewhere to the left, so you move your search to the left half. If the middle value is false, you know that the first true must be to the right, so you focus on the right half. This type of searching, based on where a condition is true and where it is false, can be reused for every single binary search problem. In fact, we actually have this free template for this on our website, Algamonster. All we need to do is find out what makes something feasible. For example, the common lead code problem, minimum in rotated sorted array, doesn't appear to follow binary search at first. After all, it's not sorted. However, there's a monotonic condition we can use to apply binary search. The condition is that any element that is less than the last element of the array is part of the rotated section, while any element greater than the last element is part of the original sorted section. We can mentally convert this to trues and falses, and just like that, we have the same true and false question we just solve. This can be applied to a ton of different questions. For more practice and an in-depth explanation, check out algo.monster. Now we'll take a look at non-linear structure problems which work with trees and graphs. In fact, instead of treating trees and graphs as two entirely different structures, you should imagine that a tree is just a graph with no cycles. The only difference between the two is that for a graph that could have cycles, you need some visited set to keep track of nodes you've already visited. This makes the patterns we will talk about applicable to both. For non-linear problems, we'll start with breadth-first search, as it's the easiest algorithm to understand and build off of. breadth-first search is an algorithm used to explore nodes in a graph or tree level by level. The idea behind this is that it starts at a given node, commonly called the root, and explores all its immediate neighbors first before moving on to the neighbors' neighbors, and so on. To keep track of the nodes that have already been visited so that we don't revisit them, BFS uses a queue, a data structure that works like a line. First in, first out. This ensures that nodes are visited in the correct order, starting from the closest to the root and moving outward. BFS is such a common algorithm that you will use all the time. Luckily for you, instead of having to derive it from scratch each time, we have this template that we can reuse. If you want to see more about how we got this template, check it out at algo.monster. Now let's see BFS in action. We're going to use this template to solve a common lead code problem, level order traversal of a binary tree. The problem asks us to give all of the levels of the tree at each depth This means we need to visit the nodes of the tree level by level, starting from the root and moving down, visiting all the nodes at each level before proceeding to the next. For example, given a tree with the root node 3 and children 9 and 20, where 20 has children 15 and 7, we need to return the values of the nodes level by level in the following order. First 3, then 9, 20, and finally 15, 16th phrase which is not a kill, we can solve this problem using BFS as its ideal for level by level traversal. The idea is to use a queue to help explore the tree one level at a time. First, we add the root node to the queue. Then for each level, we process all nodes currently in the queue by removing them and adding their values to the result. For each node we process, we also add its left and right children to the queue so they can be processed in the next level. This ensures that we visit all nodes on one level before moving to the next. By repeating this process until the queue is empty, we can traverse the entire tree level by level and collect the node values in the correct order. Finally, we return the list of node values grouped by levels. Devth first search is an algorithm that builds on the foundation of BFS but explores the tree or graph in a different way. While BFS focuses on exploring nodes level by level, DFS dives deep into one path as far as possible before exploring the next path. In BFS, we use a queue to process nodes in a breadth first manner, ensuring all nodes at one level are explored before moving to the next. In contrast, DFS uses a stack to explore nodes in a depth first manner, where you keep going down one branch until you reach the end, and then you come back to explore other branches. Most often, the stack DFS uses is the call stack. As most DFS implementations use recursion, where BFS is ideal for finding the shortest path or exploring nodes layer by layer, DFS is more suited for problems where you need to explore all paths or check every possible configuration, such as finding all paths in a tree, searching for a specific condition deep in the structure or detecting cycles. The depth first nature of DFS makes it more memory efficient in some cases, but it's less suited for problems where the shortest path or shallow exploration is required, as it doesn't guarantee finding the closest solution first. Like with BFS, DFS is a common algorithm that you will use all the time, so we have a template that we can reuse. Again, if you're curious to how this works, check it out at algo.monster. Now, let's solve a leap code problem with DFS. A common leap code problem that uses this pattern is called number of islands. This problem asks us to count the number of islands in a 2D grid where one represents land and zero represents water. An island is formed by connecting adjacent ones horizontally or vertically. We need to determine how many distinct islands exist by identifying connected components of ones in the grid. For example, in a grid where someone's are grouped together, each connected group is considered an island. Our goal is to traverse the grid, find all islands, and count them. We can solve this problem using DFS to explore each island. The idea is to iterate through each cell in the grid. When we encounter a one land, it indicates the start of an island. From there, we perform DFS to visit all connected ones all parts of the same island by exploring the neighboring cells in all four directions as deep as they go. During the DFS, we mark each visited cell by changing its value to zero to ensure we don't count it again. Once all connected cells for that island are visited, we increment the island count and continue searching the rest of the grid. This process ensures that we explore each island once and count it correctly. By the end, the total number of initializing DFS calls we make, not including the recursive calls, equals the number of islands. Backtracking is a pattern that many people struggle with, but in reality, it's actually just an extension of depth first search. In DFS, we typically traverse through a prebuilt structure like a tree or graph, where the nodes and connections are already defined. With backtracking, however, you often have to build the solution tree yourself as you explore different options, especially in combinatorial problems, where the structure of the tree isn't given explicitly, but is generated dynamically as you make decisions. In a backtracking problem, you explore all possible solutions by making a series of decisions. Each decision represents a node in the tree, and each potential decision forms a branch. As you explore a decision, you either reach a valid solution, or you hit a dead end. When a decision leads to a dead end, you backtrack to the previous decision, undo it, and try another path. Just like with all of our other patterns, we also have a template we are going to use here, which is this. Now, let's walk through it by solving a lead code problem. Letter combinations of a phone number. In this problem, we're given a string of digits, typically from 2 to 9, and we need to return all the possible letter combinations that the digits could represent using the mapping of a phone keypad. For example, if we have the digits 23, each digit corresponds to a set of letters, two maps to A, B, C, and three maps to D, E, F. Our goal is to generate every possible combination of these letters. So, for 23, some outputs could be a D, A, E, A, F, B, D, B, E, B, F, and so on. Now, our goal is to find all of these combinations. Backtracking is the optimal approach for solving this problem because it allows us to explore each possible letter one at a time while pruning invalid configurations early. Rather than brute forcing through every possible combination, backtracking lets us build the solution letter by letter, discarding incomplete paths that no longer make sense. We start by taking the input digits and starts by checking if the input is empty, returning an empty list if so. It initializes an empty list ains to store the results and defines a recursive helper function, DFS, to perform depth first search, DFS. The DFS function tracks the current position in the digits string using start index and builds combinations step by step by adding letters to the path list. If the start index reaches the length of the digits, the current path is a complete combination, so it's joined into a string and added to ANDs. For each digit, the function iterates over its corresponding letters, adds each letter to the current combination, and recursively calls itself with the updated path and the next index. This continues until all possible combinations are found. At the end of the process, the results are returned. The key insight here is that we're using DFS under the hood to explore the possibilities, but we're building the solution ourselves rather than traversing a pre-existing structure. This is the essence of backtracking. Let's move on to topological sorting. Topological sorting is
40     0.0000       1.0000       0.0000         0.0000     qwen2-7b             For example, if we had a sorted list of only true and false, we can actually use binary search to find the first true occurrence. The binary search would work like this. You look at the middle of the list. If the middle value is true, you know that the first true must either be here or somewhere to the left, so you move your search to the left half. If the middle value is false, you know that the first true must be to the right, so you focus on the right half. This type of searching, based on where a condition is true and where it is false, can be reused for every single binary search problem. In fact, we actually have this free template for this on our website, Algamonster. All we need to do is find out what makes something feasible. For example, the common lead code problem, minimum in rotated sorted array, doesn't appear to follow binary search at first. After all, it's not sorted. However, there's a monotonic condition we can use to apply binary search. The condition is that any element that is less than the last element of the array is part of the rotated section, while any element greater than the last element is part of the original sorted section. We can mentally convert this to trues and falses, and just like that, we have the same true and false question we just solve. This can be applied to a ton of different questions. For more practice and an in-depth explanation, check out algo.monster. Now we'll take a look at non-linear structure problems which work with trees and graphs. In fact, instead of treating trees and graphs as two entirely different structures, you should imagine that a tree is just a graph with no cycles. The only difference between the two is that for a graph that could have cycles, you need some visited set to keep track of nodes you've already visited. This makes the patterns we will talk about applicable to both. For non-linear problems, we'll start with breadth-first search, as it's the easiest algorithm to understand and build off of. breadth-first search is an algorithm used to explore nodes in a graph or tree level by level. The idea behind this is that it starts at a given node, commonly called the root, and explores all its immediate neighbors first before moving on to the neighbors' neighbors, and so on. To keep track of the nodes that have already been visited so that we don't revisit them, BFS uses a queue, a data structure that works like a line. First in, first out. This ensures that nodes are visited in the correct order, starting from the closest to the root and moving outward. BFS is such a common algorithm that you will use all the time. Luckily for you, instead of having to derive it from scratch each time, we have this template that we can reuse. If you want to see more about how we got this template, check it out at algo.monster. Now let's see BFS in action. We're going to use this template to solve a common lead code problem, level order traversal of a binary tree. The problem asks us to give all of the levels of the tree at each depth This means we need to visit the nodes of the tree level by level, starting from the root and moving down, visiting all the nodes at each level before proceeding to the next. For example, given a tree with the root node 3 and children 9 and 20, where 20 has children 15 and 7, we need to return the values of the nodes level by level in the following order. First 3, then 9, 20, and finally 15, 16th phrase which is not a kill, we can solve this problem using BFS as its ideal for level by level traversal. The idea is to use a queue to help explore the tree one level at a time. First, we add the root node to the queue. Then for each level, we process all nodes currently in the queue by removing them and adding their values to the result. For each node we process, we also add its left and right children to the queue so they can be processed in the next level. This ensures that we visit all nodes on one level before moving to the next. By repeating this process until the queue is empty, we can traverse the entire tree level by level and collect the node values in the correct order. Finally, we return the list of node values grouped by levels. Devth first search is an algorithm that builds on the foundation of BFS but explores the tree or graph in a different way. While BFS focuses on exploring nodes level by level, DFS dives deep into one path as far as possible before exploring the next path. In BFS, we use a queue to process nodes in a breadth first manner, ensuring all nodes at one level are explored before moving to the next. In contrast, DFS uses a stack to explore nodes in a depth first manner, where you keep going down one branch until you reach the end, and then you come back to explore other branches. Most often, the stack DFS uses is the call stack. As most DFS implementations use recursion, where BFS is ideal for finding the shortest path or exploring nodes layer by layer, DFS is more suited for problems where you need to explore all paths or check every possible configuration, such as finding all paths in a tree, searching for a specific condition deep in the structure or detecting cycles. The depth first nature of DFS makes it more memory efficient in some cases, but it's less suited for problems where the shortest path or shallow exploration is required, as it doesn't guarantee finding the closest solution first. Like with BFS, DFS is a common algorithm that you will use all the time, so we have a template that we can reuse. Again, if you're curious to how this works, check it out at algo.monster. Now, let's solve a leap code problem with DFS. A common leap code problem that uses this pattern is called number of islands. This problem asks us to count the number of islands in a 2D grid where one represents land and zero represents water. An island is formed by connecting adjacent ones horizontally or vertically. We need to determine how many distinct islands exist by identifying connected components of ones in the grid. For example, in a grid where someone's are grouped together, each connected group is considered an island. Our goal is to traverse the grid, find all islands, and count them. We can solve this problem using DFS to explore each island. The idea is to iterate through each cell in the grid. When we encounter a one land, it indicates the start of an island. From there, we perform DFS to visit all connected ones all parts of the same island by exploring the neighboring cells in all four directions as deep as they go. During the DFS, we mark each visited cell by changing its value to zero to ensure we don't count it again. Once all connected cells for that island are visited, we increment the island count and continue searching the rest of the grid. This process ensures that we explore each island once and count it correctly. By the end, the total number of initializing DFS calls we make, not including the recursive calls, equals the number of islands. Backtracking is a pattern that many people struggle with, but in reality, it's actually just an extension of depth first search. In DFS, we typically traverse through a prebuilt structure like a tree or graph, where the nodes and connections are already defined. With backtracking, however, you often have to build the solution tree yourself as you explore different options, especially in combinatorial problems, where the structure of the tree isn't given explicitly, but is generated dynamically as you make decisions. In a backtracking problem, you explore all possible solutions by making a series of decisions. Each decision represents a node in the tree, and each potential decision forms a branch. As you explore a decision, you either reach a valid solution, or you hit a dead end. When a decision leads to a dead end, you backtrack to the previous decision, undo it, and try another path. Just like with all of our other patterns, we also have a template we are going to use here, which is this. Now, let's walk through it by solving a lead code problem. Letter combinations of a phone number. In this problem, we're given a string of digits, typically from 2 to 9, and we need to return all the possible letter combinations that the digits could represent using the mapping of a phone keypad. For example, if we have the digits 23, each digit corresponds to a set of letters, two maps to A, B, C, and three maps to D, E, F. Our goal is to generate every possible combination of these letters. So, for 23, some outputs could be a D, A, E, A, F, B, D, B, E, B, F, and so on. Now, our goal is to find all of these combinations. Backtracking is the optimal approach for solving this problem because it allows us to explore each possible letter one at a time while pruning invalid configurations early. Rather than brute forcing through every possible combination, backtracking lets us build the solution letter by letter, discarding incomplete paths that no longer make sense. We start by taking the input digits and starts by checking if the input is empty, returning an empty list if so. It initializes an empty list ains to store the results and defines a recursive helper function, DFS, to perform depth first search, DFS. The DFS function tracks the current position in the digits string using start index and builds combinations step by step by adding letters to the path list. If the start index reaches the length of the digits, the current path is a complete combination, so it's joined into a string and added to ANDs. For each digit encountered, the function looks up the corresponding letters in the phone keypad map and iterates over them. For every letter, it recursively calls DFS with the updated path and moves to the start plus one. This continues until all combinations are found. Finally returns the list containing all the combinations. That's it! Backtracking helps us efficiently explore the space of all possible letter sequences without wasting time on invalid paths, leading to a much faster solution compared to trying every combination individually. And that concludes our discussion
41     0.0000       1.0000       0.0000         0.0000     qwen2-7b             For example, if we had a sorted list of only true and false, we can actually use binary search to find the first true occurrence. The binary search would work like this. You look at the middle of the list. If the middle value is true, you know that the first true must either be here or somewhere to the left, so you move your search to the left half. If the middle value is false, you know that the first true must be to the right, so you focus on the right half. This type of searching, based on where a condition is true and where it is false, can be reused for every single binary search problem. In fact, we actually have this free template for this on our website, Algamonster. All we need to do is find out what makes something feasible. For example, the common lead code problem, minimum in rotated sorted array, doesn't appear to follow binary search at first. After all, it's not sorted. However, there's a monotonic condition we can use to apply binary search. The condition is that any element that is less than the last element of the array is part of the rotated section, while any element greater than the last element is part of the original sorted section. We can mentally convert this to trues and falses, and just like that, we have the same true and false question we just solve. This can be applied to a ton of different questions. For more practice and an in-depth explanation, check out algo.monster. Now we'll take a look at non-linear structure problems which work with trees and graphs. In fact, instead of treating trees and graphs as two entirely different structures, you should imagine that a tree is just a graph with no cycles. The only difference between the two is that for a graph that could have cycles, you need some visited set to keep track of nodes you've already visited. This makes the patterns we will talk about applicable to both. For non-linear problems, we'll start with breadth-first search, as it's the easiest algorithm to understand and build off of. breadth-first search is an algorithm used to explore nodes in a graph or tree level by level. The idea behind this is that it starts at a given node, commonly called the root, and explores all its immediate neighbors first before moving on to the neighbors' neighbors, and so on. To keep track of the nodes that have already been visited so that we don't revisit them, BFS uses a queue, a data structure that works like a line. First in, first out. This ensures that nodes are visited in the correct order, starting from the closest to the root and moving outward. BFS is such a common algorithm that you will use all the time. Luckily for you, instead of having to derive it from scratch each time, we have this template that we can reuse. If you want to see more about how we got this template, check it out at algo.monster. Now let's see BFS in action. We're going to use this template to solve a common lead code problem, level order traversal of a binary tree. The problem asks us to give all of the levels of the tree at each depth This means we need to visit the nodes of the tree level by level, starting from the root and moving down, visiting all the nodes at each level before proceeding to the next. For example, given a tree with the root node 3 and children 9 and 20, where 20 has children 15 and 7, we need to return the values of the nodes level by level in the following order. First 3, then 9, 20, and finally 15, 16th phrase which is not a kill, we can solve this problem using BFS as its ideal for level by level traversal. The idea is to use a queue to help explore the tree one level at a time. First, we add the root node to the queue. Then for each level, we process all nodes currently in the queue by removing them and adding their values to the result. For each node we process, we also add its left and right children to the queue so they can be processed in the next level. This ensures that we visit all nodes on one level before moving to the next. By repeating this process until the queue is empty, we can traverse the entire tree level by level and collect the node values in the correct order. Finally, we return the list of node values grouped by levels. Devth first search is an algorithm that builds on the foundation of BFS but explores the tree or graph in a different way. While BFS focuses on exploring nodes level by level, DFS dives deep into one path as far as possible before exploring the next path. In BFS, we use a queue to process nodes in a breadth first manner, ensuring all nodes at one level are explored before moving to the next. In contrast, DFS uses a stack to explore nodes in a depth first manner, where you keep going down one branch until you reach the end, and then you come back to explore other branches. Most often, the stack DFS uses is the call stack. As most DFS implementations use recursion, where BFS is ideal for finding the shortest path or exploring nodes layer by layer, DFS is more suited for problems where you need to explore all paths or check every possible configuration, such as finding all paths in a tree, searching for a specific condition deep in the structure or detecting cycles. The depth first nature of DFS makes it more memory efficient in some cases, but it's less suited for problems where the shortest path or shallow exploration is required, as it doesn't guarantee finding the closest solution first. Like with BFS, DFS is a common algorithm that you will use all the time, so we have a template that we can reuse. Again, if you're curious to how this works, check it out at algo.monster. Now, let's solve a leap code problem with DFS. A common leap code problem that uses this pattern is called number of islands. This problem asks us to count the number of islands in a 2D grid where one represents land and zero represents water. An island is formed by connecting adjacent ones horizontally or vertically. We need to determine how many distinct islands exist by identifying connected components of ones in the grid. For example, in a grid where someone's are grouped together, each connected group is considered an island. Our goal is to traverse the grid, find all islands, and count them. We can solve this problem using DFS to explore each island. The idea is to iterate through each cell in the grid. When we encounter a one land, it indicates the start of an island. From there, we perform DFS to visit all connected ones all parts of the same island by exploring the neighboring cells in all four directions as deep as they go. During the DFS, we mark each visited cell by changing its value to zero to ensure we don't count it again. Once all connected cells for that island are visited, we increment the island count and continue searching the rest of the grid. This process ensures that we explore each island once and count it correctly. By the end, the total number of initializing DFS calls we make, not including the recursive calls, equals the number of islands. Backtracking is a pattern that many people struggle with, but in reality, it's actually just an extension of depth first search. In DFS, we typically traverse through a prebuilt structure like a tree or graph, where the nodes and connections are already defined. With backtracking, however, you often have to build the solution tree yourself as you explore different options, especially in combinatorial problems, where the structure of the tree isn't given explicitly, but is generated dynamically as you make decisions. In a backtracking problem, you explore all possible solutions by making a series of decisions. Each decision represents a node in the tree, and each potential decision forms a branch. As you explore a decision, you either reach a valid solution, or you hit a dead end. When a decision leads to a dead end, you backtrack to the previous decision, undo it, and try another path. Just like with all of our other patterns, we also have a template we are going to use here, which is this. Now, let's walk through it by solving a lead code problem. Letter combinations of a phone number. In this problem, we're given a string of digits, typically from 2 to 9, and we need to return all the possible letter combinations that the digits could represent using the mapping of a phone keypad. For example, if we have the digits 23, each digit corresponds to a set of letters, two maps to A, B, C, and three maps to D, E, F. Our goal is to generate every possible combination of these letters. So, for 23, some outputs could be a D, A, E, A, F, B, D, B, E, B, F, and so on. Now, our goal is to find all of these combinations. Backtracking is the optimal approach for solving this problem because it allows us to explore each possible letter one at a time while pruning invalid configurations early. Rather than brute forcing through every possible combination, backtracking lets us build the solution letter by letter, discarding incomplete paths that no longer make sense. We start by taking the input digits and starts by checking if the input is empty, returning an empty list if so. It initializes an empty list ains to store the results and defines a recursive helper function, DFS, to perform depth first search, DFS. The DFS function tracks the current position in the digits string using start index and builds combinations step by step by adding letters to the path list. If the start index reaches the length of the digits, the current path is a complete combination, so it's joined into a string and added to ANDs. For each digit encountered, it looks up the corresponding letters in the phone keypad mapping dictionary and iterates over them. For every letter, it recursively calls DFS with the updated path and increments the start pointer. This continues until all combinations are found, and the final result list contains all possible letter strings. By using backtracking in this way, we efficiently explore all valid combinations without wasting time on invalid paths. This method ensures that every possible letter combination is generated exactly once, providing a comprehensive solution to the problem
42     0.0000       1.0000       0.0000         0.0000     qwen2-7b             For example, if we had a sorted list of only true and false, we can actually use binary search to find the first true occurrence. The binary search would work like this. You look at the middle of the list. If the middle value is true, you know that the first true must either be here or somewhere to the left, so you move your search to the left half. If the middle value is false, you know that the first true must be to the right, so you focus on the right half. This type of searching, based on where a condition is true and where it is false, can be reused for every single binary search problem. In fact, we actually have this free template for this on our website, Algamonster. All we need to do is find out what makes something feasible. For example, the common lead code problem, minimum in rotated sorted array, doesn't appear to follow binary search at first. After all, it's not sorted. However, there's a monotonic condition we can use to apply binary search. The condition is that any element that is less than the last element of the array is part of the rotated section, while any element greater than the last element is part of the original sorted section. We can mentally convert this to trues and falses, and just like that, we have the same true and false question we just solve. This can be applied to a ton of different questions. For more practice and an in-depth explanation, check out algo.monster. Now we'll take a look at non-linear structure problems which work with trees and graphs. In fact, instead of treating trees and graphs as two entirely different structures, you should imagine that a tree is just a graph with no cycles. The only difference between the two is that for a graph that could have cycles, you need some visited set to keep track of nodes you've already visited. This makes the patterns we will talk about applicable to both. For non-linear problems, we'll start with breadth-first search, as it's the easiest algorithm to understand and build off of. breadth-first search is an algorithm used to explore nodes in a graph or tree level by level. The idea behind this is that it starts at a given node, commonly called the root, and explores all its immediate neighbors first before moving on to the neighbors' neighbors, and so on. To keep track of the nodes that have already been visited so that we don't revisit them, BFS uses a queue, a data structure that works like a line. First in, first out. This ensures that nodes are visited in the correct order, starting from the closest to the root and moving outward. BFS is such a common algorithm that you will use all the time. Luckily for you, instead of having to derive it from scratch each time, we have this template that we can reuse. If you want to see more about how we got this template, check it out at algo.monster. Now let's see BFS in action. We're going to use this template to solve a common lead code problem, level order traversal of a binary tree. The problem asks us to give all of the levels of the tree at each depth This means we need to visit the nodes of the tree level by level, starting from the root and moving down, visiting all the nodes at each level before proceeding to the next. For example, given a tree with the root node 3 and children 9 and 20, where 20 has children 15 and 7, we need to return the values of the nodes level by level in the following order. First 3, then 9, 20, and finally 15, 16th phrase which is not a kill, we can solve this problem using BFS as its ideal for level by level traversal. The idea is to use a queue to help explore the tree one level at a time. First, we add the root node to the queue. Then for each level, we process all nodes currently in the queue by removing them and adding their values to the result. For each node we process, we also add its left and right children to the queue so they can be processed in the next level. This ensures that we visit all nodes on one level before moving to the next. By repeating this process until the queue is empty, we can traverse the entire tree level by level and collect the node values in the correct order. Finally, we return the list of node values grouped by levels. Devth first search is an algorithm that builds on the foundation of BFS but explores the tree or graph in a different way. While BFS focuses on exploring nodes level by level, DFS dives deep into one path as far as possible before exploring the next path. In BFS, we use a queue to process nodes in a breadth first manner, ensuring all nodes at one level are explored before moving to the next. In contrast, DFS uses a stack to explore nodes in a depth first manner, where you keep going down one branch until you reach the end, and then you come back to explore other branches. Most often, the stack DFS uses is the call stack. As most DFS implementations use recursion, where BFS is ideal for finding the shortest path or exploring nodes layer by layer, DFS is more suited for problems where you need to explore all paths or check every possible configuration, such as finding all paths in a tree, searching for a specific condition deep in the structure or detecting cycles. The depth first nature of DFS makes it more memory efficient in some cases, but it's less suited for problems where the shortest path or shallow exploration is required, as it doesn't guarantee finding the closest solution first. Like with BFS, DFS is a common algorithm that you will use all the time, so we have a template that we can reuse. Again, if you're curious to how this works, check it out at algo.monster. Now, let's solve a leap code problem with DFS. A common leap code problem that uses this pattern is called number of islands. This problem asks us to count the number of islands in a 2D grid where one represents land and zero represents water. An island is formed by connecting adjacent ones horizontally or vertically. We need to determine how many distinct islands exist by identifying connected components of ones in the grid. For example, in a grid where someone's are grouped together, each connected group is considered an island. Our goal is to traverse the grid, find all islands, and count them. We can solve this problem using DFS to explore each island. The idea is to iterate through each cell in the grid. When we encounter a one land, it indicates the start of an island. From there, we perform DFS to visit all connected ones all parts of the same island by exploring the neighboring cells in all four directions as deep as they go. During the DFS, we mark each visited cell by changing its value to zero to ensure we don't count it again. Once all connected cells for that island are visited, we increment the island count and continue searching the rest of the grid. This process ensures that we explore each island once and count it correctly. By the end, the total number of initializing DFS calls we make, not including the recursive calls, equals the number of islands. Backtracking is a pattern that many people struggle with, but in reality, it's actually just an extension of depth first search. In DFS, we typically traverse through a prebuilt structure like a tree or graph, where the nodes and connections are already defined. With backtracking, however, you often have to build the solution tree yourself as you explore different options, especially in combinatorial problems, where the structure of the tree isn't given explicitly, but is generated dynamically as you make decisions. In a backtracking problem, you explore all possible solutions by making a series of decisions. Each decision represents a node in the tree, and each potential decision forms a branch. As you explore a decision, you either reach a valid solution, or you hit a dead end. When a decision leads to a dead end, you backtrack to the previous decision, undo it, and try another path. Just like with all of our other patterns, we also have a template we are going to use here, which is this. Now, let's walk through it by solving a lead code problem. Letter combinations of a phone number. In this problem, we're given a string of digits, typically from 2 to 9, and we need to return all the possible letter combinations that the digits could represent using the mapping of a phone keypad. For example, if we have the digits 23, each digit corresponds to a set of letters, two maps to A, B, C, and three maps to D, E, F. Our goal is to generate every possible combination of these letters. So, for 23, some outputs could be a D, A, E, A, F, B, D, B, E, B, F, and so on. Now, our goal is to find all of these combinations. Backtracking is the optimal approach for solving this problem because it allows us to explore each possible letter one at a time while pruning invalid configurations early. Rather than brute forcing through every possible combination, backtracking lets us build the solution letter by letter, discarding incomplete paths that no longer make sense. We start by taking the input digits and starts by checking if the input is empty, returning an empty list if so. It initializes an empty list ains to store the results and defines a recursive helper function, DFS, to perform depth first search, DFS. The DFS function tracks the current position in the digits string using start index and builds combinations step by step by adding letters to the path list. If the start index reaches the length of the digits, the current path is a complete combination, so it's joined into a string and added to ANDs. For each digit encountered, it looks up the corresponding letters in the phone keypad map and iterates over them. For every letter, it recursively calls DFS with the updated path and the incremented start index to explore further combinations. After the recursive call returns, it removes the last letter from the path to backtrack and try the next option. This continues until all possible combinations are generated, and the final result is returned. By using backtracking in this way, we efficiently explore all valid combinations without wasting time on invalid paths
43     0.0000       1.0000       0.0000         0.0000     qwen2-7b             For example, if we had a sorted list of only true and false, we can actually use binary search to find the first true occurrence. The binary search would work like this. You look at the middle of the list. If the middle value is true, you know that the first true must either be here or somewhere to the left, so you move your search to the left half. If the middle value is false, you know that the first true must be to the right, so you focus on the right half. This type of searching, based on where a condition is true and where it is false, can be reused for every single binary search problem. In fact, we actually have this free template for this on our website, Algamonster. All we need to do is find out what makes something feasible. For example, the common lead code problem, minimum in rotated sorted array, doesn't appear to follow binary search at first. After all, it's not sorted. However, there's a monotonic condition we can use to apply binary search. The condition is that any element that is less than the last element of the array is part of the rotated section, while any element greater than the last element is part of the original sorted section. We can mentally convert this to trues and falses, and just like that, we have the same true and false question we just solve. This can be applied to a ton of different questions. For more practice and an in-depth explanation, check out algo.monster. Now we'll take a look at non-linear structure problems which work with trees and graphs. In fact, instead of treating trees and graphs as two entirely different structures, you should imagine that a tree is just a graph with no cycles. The only difference between the two is that for a graph that could have cycles, you need some visited set to keep track of nodes you've already visited. This makes the patterns we will talk about applicable to both. For non-linear problems, we'll start with breadth-first search, as it's the easiest algorithm to understand and build off of. breadth-first search is an algorithm used to explore nodes in a graph or tree level by level. The idea behind this is that it starts at a given node, commonly called the root, and explores all its immediate neighbors first before moving on to the neighbors' neighbors, and so on. To keep track of the nodes that have already been visited so that we don't revisit them, BFS uses a queue, a data structure that works like a line. First in, first out. This ensures that nodes are visited in the correct order, starting from the closest to the root and moving outward. BFS is such a common algorithm that you will use all the time. Luckily for you, instead of having to derive it from scratch each time, we have this template that we can reuse. If you want to see more about how we got this template, check it out at algo.monster. Now let's see BFS in action. We're going to use this template to solve a common lead code problem, level order traversal of a binary tree. The problem asks us to give all of the levels of the tree at each depth This means we need to visit the nodes of the tree level by level, starting from the root and moving down, visiting all the nodes at each level before proceeding to the next. For example, given a tree with the root node 3 and children 9 and 20, where 20 has children 15 and 7, we need to return the values of the nodes level by level in the following order. First 3, then 9, 20, and finally 15, 16th phrase which is not a kill, we can solve this problem using BFS as its ideal for level by level traversal. The idea is to use a queue to help explore the tree one level at a time. First, we add the root node to the queue. Then for each level, we process all nodes currently in the queue by removing them and adding their values to the result. For each node we process, we also add its left and right children to the queue so they can be processed in the next level. This ensures that we visit all nodes on one level before moving to the next. By repeating this process until the queue is empty, we can traverse the entire tree level by level and collect the node values in the correct order. Finally, we return the list of node values grouped by levels. Devth first search is an algorithm that builds on the foundation of BFS but explores the tree or graph in a different way. While BFS focuses on exploring nodes level by level, DFS dives deep into one path as far as possible before exploring the next path. In BFS, we use a queue to process nodes in a breadth first manner, ensuring all nodes at one level are explored before moving to the next. In contrast, DFS uses a stack to explore nodes in a depth first manner, where you keep going down one branch until you reach the end, and then you come back to explore other branches. Most often, the stack DFS uses is the call stack. As most DFS implementations use recursion, where BFS is ideal for finding the shortest path or exploring nodes layer by layer, DFS is more suited for problems where you need to explore all paths or check every possible configuration, such as finding all paths in a tree, searching for a specific condition deep in the structure or detecting cycles. The depth first nature of DFS makes it more memory efficient in some cases, but it's less suited for problems where the shortest path or shallow exploration is required, as it doesn't guarantee finding the closest solution first. Like with BFS, DFS is a common algorithm that you will use all the time, so we have a template that we can reuse. Again, if you're curious to how this works, check it out at algo.monster. Now, let's solve a leap code problem with DFS. A common leap code problem that uses this pattern is called number of islands. This problem asks us to count the number of islands in a 2D grid where one represents land and zero represents water. An island is formed by connecting adjacent ones horizontally or vertically. We need to determine how many distinct islands exist by identifying connected components of ones in the grid. For example, in a grid where someone's are grouped together, each connected group is considered an island. Our goal is to traverse the grid, find all islands, and count them. We can solve this problem using DFS to explore each island. The idea is to iterate through each cell in the grid. When we encounter a one land, it indicates the start of an island. From there, we perform DFS to visit all connected ones all parts of the same island by exploring the neighboring cells in all four directions as deep as they go. During the DFS, we mark each visited cell by changing its value to zero to ensure we don't count it again. Once all connected cells for that island are visited, we increment the island count and continue searching the rest of the grid. This process ensures that we explore each island once and count it correctly. By the end, the total number of initializing DFS calls we make, not including the recursive calls, equals the number of islands. Backtracking is a pattern that many people struggle with, but in reality, it's actually just an extension of depth first search. In DFS, we typically traverse through a prebuilt structure like a tree or graph, where the nodes and connections are already defined. With backtracking, however, you often have to build the solution tree yourself as you explore different options, especially in combinatorial problems, where the structure of the tree isn't given explicitly, but is generated dynamically as you make decisions. In a backtracking problem, you explore all possible solutions by making a series of decisions. Each decision represents a node in the tree, and each potential decision forms a branch. As you explore a decision, you either reach a valid solution, or you hit a dead end. When a decision leads to a dead end, you backtrack to the previous decision, undo it, and try another path. Just like with all of our other patterns, we also have a template we are going to use here, which is this. Now, let's walk through it by solving a lead code problem. Letter combinations of a phone number. In this problem, we're given a string of digits, typically from 2 to 9, and we need to return all the possible letter combinations that the digits could represent using the mapping of a phone keypad. For example, if we have the digits 23, each digit corresponds to a set of letters, two maps to A, B, C, and three maps to D, E, F. Our goal is to generate every possible combination of these letters. So, for 23, some outputs could be a D, A, E, A, F, B, D, B, E, B, F, and so on. Now, our goal is to find all of these combinations. Backtracking is the optimal approach for solving this problem because it allows us to explore each possible letter one at a time while pruning invalid configurations early. Rather than brute forcing through every possible combination, backtracking lets us build the solution letter by letter, discarding incomplete paths that no longer make sense. We start by taking the input digits and starts by checking if the input is empty, returning an empty list if so. It initializes an empty list ains to store the results and defines a recursive helper function, DFS, to perform depth first search, DFS. The DFS function tracks the current position in the digits string using start index and builds combinations step by step by adding letters to the path list. If the start index reaches the length of the digits, the current path is a complete combination, so it's joined into a string and added to ANDs. For each digit encountered, it iterates over the corresponding letters in the phone keypad map and recursively calls DFS with the updated path and start index incremented by one. This continues until all combinations are generated. By doing this, we efficiently explore all possibilities without wasting time on invalid paths. The key to backtracking is to prune the search space effectively, avoiding unnecessary computations and focusing on viable solutions. This method ensures that all possible combinations are found without redundancy, providing an elegant solution to the problem. Backtrack is a powerful
44     0.0000       1.0000       0.0000         0.0000     llama3.1-8b          For example, if we had a sorted list of only true and false, we can actually use binary search to find the first true occurrence. The binary search would work like this. You look at the middle of the list. If the middle value is true, you know that the first true must either be here or somewhere to the left, so you move your search to the left half. If the middle value is false, you know that the first true must be to the right, so you focus on the right half. This type of searching, based on where a condition is true and where it is false, can be reused for every single binary search problem. In fact, we actually have this free template for this on our website, Algamonster. All we need to do is find out what makes something feasible. For example, the common lead code problem, minimum in rotated sorted array, doesn't appear to follow binary search at first. After all, it's not sorted. However, there's a monotonic condition we can use to apply binary search. The condition is that any element that is less than the last element of the array is part of the rotated section, while any element greater than the last element is part of the original sorted section. We can mentally convert this to trues and falses, and just like that, we have the same true and false question we just solve. This can be applied to a ton of different questions. For more practice and an in-depth explanation, check out algo.monster. Now we'll take a look at non-linear structure problems which work with trees and graphs. In fact, instead of treating trees and graphs as two entirely different structures, you should imagine that a tree is just a graph with no cycles. The only difference between the two is that for a graph that could have cycles, you need some visited set to keep track of nodes you've already visited. This makes the patterns we will talk about applicable to both. For non-linear problems, we'll start with breadth-first search, as it's the easiest algorithm to understand and build off of. breadth-first search is an algorithm used to explore nodes in a graph or tree level by level. The idea behind this is that it starts at a given node, commonly called the root, and explores all its immediate neighbors first before moving on to the neighbors' neighbors, and so on. To keep track of the nodes that have already been visited so that we don't revisit them, BFS uses a queue, a data structure that works like a line. First in, first out. This ensures that nodes are visited in the correct order, starting from the closest to the root and moving outward. BFS is such a common algorithm that you will use all the time. Luckily for you, instead of having to derive it from scratch each time, we have this template that we can reuse. If you want to see more about how we got this template, check it out at algo.monster. Now let's see BFS in action. We're going to use this template to solve a common lead code problem, level order traversal of a binary tree. The problem asks us to give all of the levels of the tree at each depth This means we need to visit the nodes of the tree level by level, starting from the root and moving down, visiting all the nodes at each level before proceeding to the next. For example, given a tree with the root node 3 and children 9 and 20, where 20 has children 15 and 7, we need to return the values of the nodes level by level in the following order. First 3, then 9, 20, and finally 15, 16th phrase which is not a kill, we can solve this problem using BFS as its ideal for level by level traversal. The idea is to use a queue to help explore the tree one level at a time. First, we add the root node to the queue. Then for each level, we process all nodes currently in the queue by removing them and adding their values to the result. For each node we process, we also add its left and right children to the queue so they can be processed in the next level. This ensures that we visit all nodes on one level before moving to the next. By repeating this process until the queue is empty, we can traverse the entire tree level by level and collect the node values in the correct order. Finally, we return the list of node values grouped by levels. Devth first search is an algorithm that builds on the foundation of BFS but explores the tree or graph in a different way. While BFS focuses on exploring nodes level by level, DFS dives deep into one path as far as possible before exploring the next path. In BFS, we use a queue to process nodes in a breadth first manner, ensuring all nodes at one level are explored before moving to the next. In contrast, DFS uses a stack to explore nodes in a depth first manner, where you keep going down one branch until you reach the end, and then you come back to explore other branches. Most often, the stack DFS uses is the call stack. As most DFS implementations use recursion, where BFS is ideal for finding the shortest path or exploring nodes layer by layer, DFS is more suited for problems where you need to explore all paths or check every possible configuration, such as finding all paths in a tree, searching for a specific condition deep in the structure or detecting cycles. The depth first nature of DFS makes it more memory efficient in some cases, but it's less suited for problems where the shortest path or shallow exploration is required, as it doesn't guarantee finding the closest solution first. Like with BFS, DFS is a common algorithm that you will use all the time, so we have a template that we can reuse. Again, if you're curious to how this works, check it out at algo.monster. Now, let's solve a leap code problem with DFS. A common leap code problem that uses this pattern is called number of islands. This problem asks us to count the number of islands in a 2D grid where one represents land and zero represents water. An island is formed by connecting adjacent ones horizontally or vertically. We need to determine how many distinct islands exist by identifying connected components of ones in the grid. For example, in a grid where someone's are grouped together, each connected group is considered an island. Our goal is to traverse the grid, find all islands, and count them. We can solve this problem using DFS to explore each island. The idea is to iterate through each cell in the grid. When we encounter a one land, it indicates the start of an island. From there, we perform DFS to visit all connected ones all parts of the same island by exploring the neighboring cells in all four directions as deep as they go. During the DFS, we mark each visited cell by changing its value to zero to ensure we don't count it again. Once all connected cells for that island are visited, we increment the island count and continue searching the rest of the grid. This process ensures that we explore each island once and count it correctly. By the end, the total number of initializing DFS calls we make, not including the recursive calls, equals the number of islands. Backtracking is a pattern that many people struggle with, but in reality, it's actually just an extension of depth first search. In DFS, we typically traverse through a prebuilt structure like a tree or graph, where the nodes and connections are already defined. With backtracking, however, you often have to build the solution tree yourself as you explore different options, especially in combinatorial problems, where the structure of the tree isn't given explicitly, but is generated dynamically as you make decisions. In a backtracking problem, you explore all possible solutions by making a series of decisions. Each decision represents a node in the tree, and each potential decision forms a branch. As you explore a decision, you either reach a valid solution, or you hit a dead end. When a decision leads to a dead end, you backtrack to the previous decision, undo it, and try another path. Just like with all of our other patterns, we also have a template we are going to use here, which is this. Now, let's walk through it by solving a lead code problem. Letter combinations of a phone number. In this problem, we're given a string of digits, typically from 2 to 9, and we need to return all the possible letter combinations that the digits could represent using the mapping of a phone keypad. For example, if we have the digits 23, each digit corresponds to a set of letters, two maps to A, B, C, and three maps to D, E, F. Our goal is to generate every possible combination of these letters. So, for 23, some outputs could be a D, A, E, A, F, B, D, B, E, B, F, and so on. Now, our goal is to find all of these combinations. Backtracking is the optimal approach for solving this problem because it allows us to explore each possible letter one at a time while pruning invalid configurations early. Rather than brute forcing through every possible combination, backtracking lets us build the solution letter by letter, discarding incomplete paths that no longer make sense. We start by taking the input digits and starts by checking if the input is empty, returning an empty list if so. It initializes an empty list ains to store the results and defines a recursive helper function, DFS, to perform depth first search, DFS. The DFS function tracks the current position in the digits string using start index and builds combinations step by step by adding letters to the path list. If the start index reaches the length of the digits, the current path is a complete combination, so it's joined into a string and added to ANDs. For each digit, the function iterates over its corresponding letters, adds each letter to the current combination, and recursively calls itself with the updated start index. This effectively tries each possible combination and prunes those that are invalid. By doing so, it generates all possible combinations efficiently. Finally when the function returns, it returns the list ANDs containing all possible letter combination. This is a very powerful technique that can be used to solve many types of problems. For now, let is move on to topological sorting. Topological sorting is a linear ordering
45     0.0000       1.0000       0.0000         0.0000     llama3.1-8b          For example, if we had a sorted list of only true and false, we can actually use binary search to find the first true occurrence. The binary search would work like this. You look at the middle of the list. If the middle value is true, you know that the first true must either be here or somewhere to the left, so you move your search to the left half. If the middle value is false, you know that the first true must be to the right, so you focus on the right half. This type of searching, based on where a condition is true and where it is false, can be reused for every single binary search problem. In fact, we actually have this free template for this on our website, Algamonster. All we need to do is find out what makes something feasible. For example, the common lead code problem, minimum in rotated sorted array, doesn't appear to follow binary search at first. After all, it's not sorted. However, there's a monotonic condition we can use to apply binary search. The condition is that any element that is less than the last element of the array is part of the rotated section, while any element greater than the last element is part of the original sorted section. We can mentally convert this to trues and falses, and just like that, we have the same true and false question we just solve. This can be applied to a ton of different questions. For more practice and an in-depth explanation, check out algo.monster. Now we'll take a look at non-linear structure problems which work with trees and graphs. In fact, instead of treating trees and graphs as two entirely different structures, you should imagine that a tree is just a graph with no cycles. The only difference between the two is that for a graph that could have cycles, you need some visited set to keep track of nodes you've already visited. This makes the patterns we will talk about applicable to both. For non-linear problems, we'll start with breadth-first search, as it's the easiest algorithm to understand and build off of. breadth-first search is an algorithm used to explore nodes in a graph or tree level by level. The idea behind this is that it starts at a given node, commonly called the root, and explores all its immediate neighbors first before moving on to the neighbors' neighbors, and so on. To keep track of the nodes that have already been visited so that we don't revisit them, BFS uses a queue, a data structure that works like a line. First in, first out. This ensures that nodes are visited in the correct order, starting from the closest to the root and moving outward. BFS is such a common algorithm that you will use all the time. Luckily for you, instead of having to derive it from scratch each time, we have this template that we can reuse. If you want to see more about how we got this template, check it out at algo.monster. Now let's see BFS in action. We're going to use this template to solve a common lead code problem, level order traversal of a binary tree. The problem asks us to give all of the levels of the tree at each depth This means we need to visit the nodes of the tree level by level, starting from the root and moving down, visiting all the nodes at each level before proceeding to the next. For example, given a tree with the root node 3 and children 9 and 20, where 20 has children 15 and 7, we need to return the values of the nodes level by level in the following order. First 3, then 9, 20, and finally 15, 16th phrase which is not a kill, we can solve this problem using BFS as its ideal for level by level traversal. The idea is to use a queue to help explore the tree one level at a time. First, we add the root node to the queue. Then for each level, we process all nodes currently in the queue by removing them and adding their values to the result. For each node we process, we also add its left and right children to the queue so they can be processed in the next level. This ensures that we visit all nodes on one level before moving to the next. By repeating this process until the queue is empty, we can traverse the entire tree level by level and collect the node values in the correct order. Finally, we return the list of node values grouped by levels. Devth first search is an algorithm that builds on the foundation of BFS but explores the tree or graph in a different way. While BFS focuses on exploring nodes level by level, DFS dives deep into one path as far as possible before exploring the next path. In BFS, we use a queue to process nodes in a breadth first manner, ensuring all nodes at one level are explored before moving to the next. In contrast, DFS uses a stack to explore nodes in a depth first manner, where you keep going down one branch until you reach the end, and then you come back to explore other branches. Most often, the stack DFS uses is the call stack. As most DFS implementations use recursion, where BFS is ideal for finding the shortest path or exploring nodes layer by layer, DFS is more suited for problems where you need to explore all paths or check every possible configuration, such as finding all paths in a tree, searching for a specific condition deep in the structure or detecting cycles. The depth first nature of DFS makes it more memory efficient in some cases, but it's less suited for problems where the shortest path or shallow exploration is required, as it doesn't guarantee finding the closest solution first. Like with BFS, DFS is a common algorithm that you will use all the time, so we have a template that we can reuse. Again, if you're curious to how this works, check it out at algo.monster. Now, let's solve a leap code problem with DFS. A common leap code problem that uses this pattern is called number of islands. This problem asks us to count the number of islands in a 2D grid where one represents land and zero represents water. An island is formed by connecting adjacent ones horizontally or vertically. We need to determine how many distinct islands exist by identifying connected components of ones in the grid. For example, in a grid where someone's are grouped together, each connected group is considered an island. Our goal is to traverse the grid, find all islands, and count them. We can solve this problem using DFS to explore each island. The idea is to iterate through each cell in the grid. When we encounter a one land, it indicates the start of an island. From there, we perform DFS to visit all connected ones all parts of the same island by exploring the neighboring cells in all four directions as deep as they go. During the DFS, we mark each visited cell by changing its value to zero to ensure we don't count it again. Once all connected cells for that island are visited, we increment the island count and continue searching the rest of the grid. This process ensures that we explore each island once and count it correctly. By the end, the total number of initializing DFS calls we make, not including the recursive calls, equals the number of islands. Backtracking is a pattern that many people struggle with, but in reality, it's actually just an extension of depth first search. In DFS, we typically traverse through a prebuilt structure like a tree or graph, where the nodes and connections are already defined. With backtracking, however, you often have to build the solution tree yourself as you explore different options, especially in combinatorial problems, where the structure of the tree isn't given explicitly, but is generated dynamically as you make decisions. In a backtracking problem, you explore all possible solutions by making a series of decisions. Each decision represents a node in the tree, and each potential decision forms a branch. As you explore a decision, you either reach a valid solution, or you hit a dead end. When a decision leads to a dead end, you backtrack to the previous decision, undo it, and try another path. Just like with all of our other patterns, we also have a template we are going to use here, which is this. Now, let's walk through it by solving a lead code problem. Letter combinations of a phone number. In this problem, we're given a string of digits, typically from 2 to 9, and we need to return all the possible letter combinations that the digits could represent using the mapping of a phone keypad. For example, if we have the digits 23, each digit corresponds to a set of letters, two maps to A, B, C, and three maps to D, E, F. Our goal is to generate every possible combination of these letters. So, for 23, some outputs could be a D, A, E, A, F, B, D, B, E, B, F, and so on. Now, our goal is to find all of these combinations. Backtracking is the optimal approach for solving this problem because it allows us to explore each possible letter one at a time while pruning invalid configurations early. Rather than brute forcing through every possible combination, backtracking lets us build the solution letter by letter, discarding incomplete paths that no longer make sense. We start by taking the input digits and starts by checking if the input is empty, returning an empty list if so. It initializes an empty list ains to store the results and defines a recursive helper function, DFS, to perform depth first search, DFS. The DFS function tracks the current position in the digits string using start index and builds combinations step by step by adding letters to the path list. If the start index reaches the length of the digits, the current path is a complete combination, so it's joined into a string and added to ANDs. For each digit, the function iterates over its corresponding letters and recursively calls itself with the updated path and start index. If a letter cannot be appended to the current combination, it returns immediately without adding anything to ANDS. This prevents unnecessary computation and reduces the number complexity. The main function returns the list ANDs after the DFS completes. Let's take a closer look at how this code works. The key insight here is that we're building up combinations step-by-step, rather than trying to generate all possibilities upfront. This approach avoids exponential growth
46     0.0000       1.0000       0.0000         0.0000     qwen2-7b             For example, if we had a sorted list of only true and false, we can actually use binary search to find the first true occurrence. The binary search would work like this. You look at the middle of the list. If the middle value is true, you know that the first true must either be here or somewhere to the left, so you move your search to the left half. If the middle value is false, you know that the first true must be to the right, so you focus on the right half. This type of searching, based on where a condition is true and where it is false, can be reused for every single binary search problem. In fact, we actually have this free template for this on our website, Algamonster. All we need to do is find out what makes something feasible. For example, the common lead code problem, minimum in rotated sorted array, doesn't appear to follow binary search at first. After all, it's not sorted. However, there's a monotonic condition we can use to apply binary search. The condition is that any element that is less than the last element of the array is part of the rotated section, while any element greater than the last element is part of the original sorted section. We can mentally convert this to trues and falses, and just like that, we have the same true and false question we just solve. This can be applied to a ton of different questions. For more practice and an in-depth explanation, check out algo.monster. Now we'll take a look at non-linear structure problems which work with trees and graphs. In fact, instead of treating trees and graphs as two entirely different structures, you should imagine that a tree is just a graph with no cycles. The only difference between the two is that for a graph that could have cycles, you need some visited set to keep track of nodes you've already visited. This makes the patterns we will talk about applicable to both. For non-linear problems, we'll start with breadth-first search, as it's the easiest algorithm to understand and build off of. breadth-first search is an algorithm used to explore nodes in a graph or tree level by level. The idea behind this is that it starts at a given node, commonly called the root, and explores all its immediate neighbors first before moving on to the neighbors' neighbors, and so on. To keep track of the nodes that have already been visited so that we don't revisit them, BFS uses a queue, a data structure that works like a line. First in, first out. This ensures that nodes are visited in the correct order, starting from the closest to the root and moving outward. BFS is such a common algorithm that you will use all the time. Luckily for you, instead of having to derive it from scratch each time, we have this template that we can reuse. If you want to see more about how we got this template, check it out at algo.monster. Now let's see BFS in action. We're going to use this template to solve a common lead code problem, level order traversal of a binary tree. The problem asks us to give all of the levels of the tree at each depth This means we need to visit the nodes of the tree level by level, starting from the root and moving down, visiting all the nodes at each level before proceeding to the next. For example, given a tree with the root node 3 and children 9 and 20, where 20 has children 15 and 7, we need to return the values of the nodes level by level in the following order. First 3, then 9, 20, and finally 15, 16th phrase which is not a kill, we can solve this problem using BFS as its ideal for level by level traversal. The idea is to use a queue to help explore the tree one level at a time. First, we add the root node to the queue. Then for each level, we process all nodes currently in the queue by removing them and adding their values to the result. For each node we process, we also add its left and right children to the queue so they can be processed in the next level. This ensures that we visit all nodes on one level before moving to the next. By repeating this process until the queue is empty, we can traverse the entire tree level by level and collect the node values in the correct order. Finally, we return the list of node values grouped by levels. Devth first search is an algorithm that builds on the foundation of BFS but explores the tree or graph in a different way. While BFS focuses on exploring nodes level by level, DFS dives deep into one path as far as possible before exploring the next path. In BFS, we use a queue to process nodes in a breadth first manner, ensuring all nodes at one level are explored before moving to the next. In contrast, DFS uses a stack to explore nodes in a depth first manner, where you keep going down one branch until you reach the end, and then you come back to explore other branches. Most often, the stack DFS uses is the call stack. As most DFS implementations use recursion, where BFS is ideal for finding the shortest path or exploring nodes layer by layer, DFS is more suited for problems where you need to explore all paths or check every possible configuration, such as finding all paths in a tree, searching for a specific condition deep in the structure or detecting cycles. The depth first nature of DFS makes it more memory efficient in some cases, but it's less suited for problems where the shortest path or shallow exploration is required, as it doesn't guarantee finding the closest solution first. Like with BFS, DFS is a common algorithm that you will use all the time, so we have a template that we can reuse. Again, if you're curious to how this works, check it out at algo.monster. Now, let's solve a leap code problem with DFS. A common leap code problem that uses this pattern is called number of islands. This problem asks us to count the number of islands in a 2D grid where one represents land and zero represents water. An island is formed by connecting adjacent ones horizontally or vertically. We need to determine how many distinct islands exist by identifying connected components of ones in the grid. For example, in a grid where someone's are grouped together, each connected group is considered an island. Our goal is to traverse the grid, find all islands, and count them. We can solve this problem using DFS to explore each island. The idea is to iterate through each cell in the grid. When we encounter a one land, it indicates the start of an island. From there, we perform DFS to visit all connected ones all parts of the same island by exploring the neighboring cells in all four directions as deep as they go. During the DFS, we mark each visited cell by changing its value to zero to ensure we don't count it again. Once all connected cells for that island are visited, we increment the island count and continue searching the rest of the grid. This process ensures that we explore each island once and count it correctly. By the end, the total number of initializing DFS calls we make, not including the recursive calls, equals the number of islands. Backtracking is a pattern that many people struggle with, but in reality, it's actually just an extension of depth first search. In DFS, we typically traverse through a prebuilt structure like a tree or graph, where the nodes and connections are already defined. With backtracking, however, you often have to build the solution tree yourself as you explore different options, especially in combinatorial problems, where the structure of the tree isn't given explicitly, but is generated dynamically as you make decisions. In a backtracking problem, you explore all possible solutions by making a series of decisions. Each decision represents a node in the tree, and each potential decision forms a branch. As you explore a decision, you either reach a valid solution, or you hit a dead end. When a decision leads to a dead end, you backtrack to the previous decision, undo it, and try another path. Just like with all of our other patterns, we also have a template we are going to use here, which is this. Now, let's walk through it by solving a lead code problem. Letter combinations of a phone number. In this problem, we're given a string of digits, typically from 2 to 9, and we need to return all the possible letter combinations that the digits could represent using the mapping of a phone keypad. For example, if we have the digits 23, each digit corresponds to a set of letters, two maps to A, B, C, and three maps to D, E, F. Our goal is to generate every possible combination of these letters. So, for 23, some outputs could be a D, A, E, A, F, B, D, B, E, B, F, and so on. Now, our goal is to find all of these combinations. Backtracking is the optimal approach for solving this problem because it allows us to explore each possible letter one at a time while pruning invalid configurations early. Rather than brute forcing through every possible combination, backtracking lets us build the solution letter by letter, discarding incomplete paths that no longer make sense. We start by taking the input digits and starts by checking if the input is empty, returning an empty list if so. It initializes an empty list ains to store the results and defines a recursive helper function, DFS, to perform depth first search, DFS. The DFS function tracks the current position in the digits string using start index and builds combinations step by step by adding letters to the path list. If the start index reaches the length of the digits, the current path is a complete combination, so it's joined into a string and added to ANDs. For each digit encountered, it iterates over the corresponding letters mapped from the phone keypad dictionary and recursively calls DFS with updated parameters. This continues until all possible combinations are found, and the final result list contains all the valid combinations. The key point here is that when building up the path, we backtrack whenever necessary by removing the last character added to the current combination, allowing us to try alternative paths without reprocessing the same sequence multiple times. This dynamic generation of the solution space combined with the ability to quickly discard
47     0.0000       1.0000       0.0000         0.0000     llama3.1-8b          For example, if we had a sorted list of only true and false, we can actually use binary search to find the first true occurrence. The binary search would work like this. You look at the middle of the list. If the middle value is true, you know that the first true must either be here or somewhere to the left, so you move your search to the left half. If the middle value is false, you know that the first true must be to the right, so you focus on the right half. This type of searching, based on where a condition is true and where it is false, can be reused for every single binary search problem. In fact, we actually have this free template for this on our website, Algamonster. All we need to do is find out what makes something feasible. For example, the common lead code problem, minimum in rotated sorted array, doesn't appear to follow binary search at first. After all, it's not sorted. However, there's a monotonic condition we can use to apply binary search. The condition is that any element that is less than the last element of the array is part of the rotated section, while any element greater than the last element is part of the original sorted section. We can mentally convert this to trues and falses, and just like that, we have the same true and false question we just solve. This can be applied to a ton of different questions. For more practice and an in-depth explanation, check out algo.monster. Now we'll take a look at non-linear structure problems which work with trees and graphs. In fact, instead of treating trees and graphs as two entirely different structures, you should imagine that a tree is just a graph with no cycles. The only difference between the two is that for a graph that could have cycles, you need some visited set to keep track of nodes you've already visited. This makes the patterns we will talk about applicable to both. For non-linear problems, we'll start with breadth-first search, as it's the easiest algorithm to understand and build off of. breadth-first search is an algorithm used to explore nodes in a graph or tree level by level. The idea behind this is that it starts at a given node, commonly called the root, and explores all its immediate neighbors first before moving on to the neighbors' neighbors, and so on. To keep track of the nodes that have already been visited so that we don't revisit them, BFS uses a queue, a data structure that works like a line. First in, first out. This ensures that nodes are visited in the correct order, starting from the closest to the root and moving outward. BFS is such a common algorithm that you will use all the time. Luckily for you, instead of having to derive it from scratch each time, we have this template that we can reuse. If you want to see more about how we got this template, check it out at algo.monster. Now let's see BFS in action. We're going to use this template to solve a common lead code problem, level order traversal of a binary tree. The problem asks us to give all of the levels of the tree at each depth This means we need to visit the nodes of the tree level by level, starting from the root and moving down, visiting all the nodes at each level before proceeding to the next. For example, given a tree with the root node 3 and children 9 and 20, where 20 has children 15 and 7, we need to return the values of the nodes level by level in the following order. First 3, then 9, 20, and finally 15, 16th phrase which is not a kill, we can solve this problem using BFS as its ideal for level by level traversal. The idea is to use a queue to help explore the tree one level at a time. First, we add the root node to the queue. Then for each level, we process all nodes currently in the queue by removing them and adding their values to the result. For each node we process, we also add its left and right children to the queue so they can be processed in the next level. This ensures that we visit all nodes on one level before moving to the next. By repeating this process until the queue is empty, we can traverse the entire tree level by level and collect the node values in the correct order. Finally, we return the list of node values grouped by levels. Devth first search is an algorithm that builds on the foundation of BFS but explores the tree or graph in a different way. While BFS focuses on exploring nodes level by level, DFS dives deep into one path as far as possible before exploring the next path. In BFS, we use a queue to process nodes in a breadth first manner, ensuring all nodes at one level are explored before moving to the next. In contrast, DFS uses a stack to explore nodes in a depth first manner, where you keep going down one branch until you reach the end, and then you come back to explore other branches. Most often, the stack DFS uses is the call stack. As most DFS implementations use recursion, where BFS is ideal for finding the shortest path or exploring nodes layer by layer, DFS is more suited for problems where you need to explore all paths or check every possible configuration, such as finding all paths in a tree, searching for a specific condition deep in the structure or detecting cycles. The depth first nature of DFS makes it more memory efficient in some cases, but it's less suited for problems where the shortest path or shallow exploration is required, as it doesn't guarantee finding the closest solution first. Like with BFS, DFS is a common algorithm that you will use all the time, so we have a template that we can reuse. Again, if you're curious to how this works, check it out at algo.monster. Now, let's solve a leap code problem with DFS. A common leap code problem that uses this pattern is called number of islands. This problem asks us to count the number of islands in a 2D grid where one represents land and zero represents water. An island is formed by connecting adjacent ones horizontally or vertically. We need to determine how many distinct islands exist by identifying connected components of ones in the grid. For example, in a grid where someone's are grouped together, each connected group is considered an island. Our goal is to traverse the grid, find all islands, and count them. We can solve this problem using DFS to explore each island. The idea is to iterate through each cell in the grid. When we encounter a one land, it indicates the start of an island. From there, we perform DFS to visit all connected ones all parts of the same island by exploring the neighboring cells in all four directions as deep as they go. During the DFS, we mark each visited cell by changing its value to zero to ensure we don't count it again. Once all connected cells for that island are visited, we increment the island count and continue searching the rest of the grid. This process ensures that we explore each island once and count it correctly. By the end, the total number of initializing DFS calls we make, not including the recursive calls, equals the number of islands. Backtracking is a pattern that many people struggle with, but in reality, it's actually just an extension of depth first search. In DFS, we typically traverse through a prebuilt structure like a tree or graph, where the nodes and connections are already defined. With backtracking, however, you often have to build the solution tree yourself as you explore different options, especially in combinatorial problems, where the structure of the tree isn't given explicitly, but is generated dynamically as you make decisions. In a backtracking problem, you explore all possible solutions by making a series of decisions. Each decision represents a node in the tree, and each potential decision forms a branch. As you explore a decision, you either reach a valid solution, or you hit a dead end. When a decision leads to a dead end, you backtrack to the previous decision, undo it, and try another path. Just like with all of our other patterns, we also have a template we are going to use here, which is this. Now, let's walk through it by solving a lead code problem. Letter combinations of a phone number. In this problem, we're given a string of digits, typically from 2 to 9, and we need to return all the possible letter combinations that the digits could represent using the mapping of a phone keypad. For example, if we have the digits 23, each digit corresponds to a set of letters, two maps to A, B, C, and three maps to D, E, F. Our goal is to generate every possible combination of these letters. So, for 23, some outputs could be a D, A, E, A, F, B, D, B, E, B, F, and so on. Now, our goal is to find all of these combinations. Backtracking is the optimal approach for solving this problem because it allows us to explore each possible letter one at a time while pruning invalid configurations early. Rather than brute forcing through every possible combination, backtracking lets us build the solution letter by letter, discarding incomplete paths that no longer make sense. We start by taking the input digits and starts by checking if the input is empty, returning an empty list if so. It initializes an empty list ains to store the results and defines a recursive helper function, DFS, to perform depth first search, DFS. The DFS function tracks the current position in the digits string using start index and builds combinations step by step by adding letters to the path list. If the start index reaches the length of the digits, the current path is a complete combination, so it's joined into a string and added to ANDs. For each digit, the function iterates over its corresponding letters, adds each letter to the current combination, and recursively calls itself with the updated path and incremented start index. If a letter cannot form a valid combination, the recursion returns without adding anything to ANDS. Otherwise, after processing all letters for the current digit, it resets the path to remove the last letter added. This effectively removes the invalid choice, allowing the algorithm to explore alternative paths. This continues until all digits are exhausted, resulting in all possible combinations being stored in ANDs, which
48     0.0000       1.0000       0.0000         0.0000     llama3.1-8b          For example, if we had a sorted list of only true and false, we can actually use binary search to find the first true occurrence. The binary search would work like this. You look at the middle of the list. If the middle value is true, you know that the first true must either be here or somewhere to the left, so you move your search to the left half. If the middle value is false, you know that the first true must be to the right, so you focus on the right half. This type of searching, based on where a condition is true and where it is false, can be reused for every single binary search problem. In fact, we actually have this free template for this on our website, Algamonster. All we need to do is find out what makes something feasible. For example, the common lead code problem, minimum in rotated sorted array, doesn't appear to follow binary search at first. After all, it's not sorted. However, there's a monotonic condition we can use to apply binary search. The condition is that any element that is less than the last element of the array is part of the rotated section, while any element greater than the last element is part of the original sorted section. We can mentally convert this to trues and falses, and just like that, we have the same true and false question we just solve. This can be applied to a ton of different questions. For more practice and an in-depth explanation, check out algo.monster. Now we'll take a look at non-linear structure problems which work with trees and graphs. In fact, instead of treating trees and graphs as two entirely different structures, you should imagine that a tree is just a graph with no cycles. The only difference between the two is that for a graph that could have cycles, you need some visited set to keep track of nodes you've already visited. This makes the patterns we will talk about applicable to both. For non-linear problems, we'll start with breadth-first search, as it's the easiest algorithm to understand and build off of. breadth-first search is an algorithm used to explore nodes in a graph or tree level by level. The idea behind this is that it starts at a given node, commonly called the root, and explores all its immediate neighbors first before moving on to the neighbors' neighbors, and so on. To keep track of the nodes that have already been visited so that we don't revisit them, BFS uses a queue, a data structure that works like a line. First in, first out. This ensures that nodes are visited in the correct order, starting from the closest to the root and moving outward. BFS is such a common algorithm that you will use all the time. Luckily for you, instead of having to derive it from scratch each time, we have this template that we can reuse. If you want to see more about how we got this template, check it out at algo.monster. Now let's see BFS in action. We're going to use this template to solve a common lead code problem, level order traversal of a binary tree. The problem asks us to give all of the levels of the tree at each depth This means we need to visit the nodes of the tree level by level, starting from the root and moving down, visiting all the nodes at each level before proceeding to the next. For example, given a tree with the root node 3 and children 9 and 20, where 20 has children 15 and 7, we need to return the values of the nodes level by level in the following order. First 3, then 9, 20, and finally 15, 16th phrase which is not a kill, we can solve this problem using BFS as its ideal for level by level traversal. The idea is to use a queue to help explore the tree one level at a time. First, we add the root node to the queue. Then for each level, we process all nodes currently in the queue by removing them and adding their values to the result. For each node we process, we also add its left and right children to the queue so they can be processed in the next level. This ensures that we visit all nodes on one level before moving to the next. By repeating this process until the queue is empty, we can traverse the entire tree level by level and collect the node values in the correct order. Finally, we return the list of node values grouped by levels. Devth first search is an algorithm that builds on the foundation of BFS but explores the tree or graph in a different way. While BFS focuses on exploring nodes level by level, DFS dives deep into one path as far as possible before exploring the next path. In BFS, we use a queue to process nodes in a breadth first manner, ensuring all nodes at one level are explored before moving to the next. In contrast, DFS uses a stack to explore nodes in a depth first manner, where you keep going down one branch until you reach the end, and then you come back to explore other branches. Most often, the stack DFS uses is the call stack. As most DFS implementations use recursion, where BFS is ideal for finding the shortest path or exploring nodes layer by layer, DFS is more suited for problems where you need to explore all paths or check every possible configuration, such as finding all paths in a tree, searching for a specific condition deep in the structure or detecting cycles. The depth first nature of DFS makes it more memory efficient in some cases, but it's less suited for problems where the shortest path or shallow exploration is required, as it doesn't guarantee finding the closest solution first. Like with BFS, DFS is a common algorithm that you will use all the time, so we have a template that we can reuse. Again, if you're curious to how this works, check it out at algo.monster. Now, let's solve a leap code problem with DFS. A common leap code problem that uses this pattern is called number of islands. This problem asks us to count the number of islands in a 2D grid where one represents land and zero represents water. An island is formed by connecting adjacent ones horizontally or vertically. We need to determine how many distinct islands exist by identifying connected components of ones in the grid. For example, in a grid where someone's are grouped together, each connected group is considered an island. Our goal is to traverse the grid, find all islands, and count them. We can solve this problem using DFS to explore each island. The idea is to iterate through each cell in the grid. When we encounter a one land, it indicates the start of an island. From there, we perform DFS to visit all connected ones all parts of the same island by exploring the neighboring cells in all four directions as deep as they go. During the DFS, we mark each visited cell by changing its value to zero to ensure we don't count it again. Once all connected cells for that island are visited, we increment the island count and continue searching the rest of the grid. This process ensures that we explore each island once and count it correctly. By the end, the total number of initializing DFS calls we make, not including the recursive calls, equals the number of islands. Backtracking is a pattern that many people struggle with, but in reality, it's actually just an extension of depth first search. In DFS, we typically traverse through a prebuilt structure like a tree or graph, where the nodes and connections are already defined. With backtracking, however, you often have to build the solution tree yourself as you explore different options, especially in combinatorial problems, where the structure of the tree isn't given explicitly, but is generated dynamically as you make decisions. In a backtracking problem, you explore all possible solutions by making a series of decisions. Each decision represents a node in the tree, and each potential decision forms a branch. As you explore a decision, you either reach a valid solution, or you hit a dead end. When a decision leads to a dead end, you backtrack to the previous decision, undo it, and try another path. Just like with all of our other patterns, we also have a template we are going to use here, which is this. Now, let's walk through it by solving a lead code problem. Letter combinations of a phone number. In this problem, we're given a string of digits, typically from 2 to 9, and we need to return all the possible letter combinations that the digits could represent using the mapping of a phone keypad. For example, if we have the digits 23, each digit corresponds to a set of letters, two maps to A, B, C, and three maps to D, E, F. Our goal is to generate every possible combination of these letters. So, for 23, some outputs could be a D, A, E, A, F, B, D, B, E, B, F, and so on. Now, our goal is to find all of these combinations. Backtracking is the optimal approach for solving this problem because it allows us to explore each possible letter one at a time while pruning invalid configurations early. Rather than brute forcing through every possible combination, backtracking lets us build the solution letter by letter, discarding incomplete paths that no longer make sense. We start by taking the input digits and starts by checking if the input is empty, returning an empty list if so. It initializes an empty list ains to store the results and defines a recursive helper function, DFS, to perform depth first search, DFS. The DFS function tracks the current position in the digits string using start index and builds combinations step by step by adding letters to the path list. If the start index reaches the length of the digits, the current path is a complete combination, so it's joined into a string and added to ANDs. For each digit, the function iterates over its corresponding letters, adds each letter to the current combination, and recursively calls itself with the updated path and start index. This continues until all possible combinations are found. Finally returns the list containing all combinations. This is a very powerful technique that can be used to solve many types of problems, including those involving permutations, combinations, and subsets. Let's move on to graph algorithms now. Graph algorithms involve traversing and manipulating graphs, which consist of vertices connected by edges. There are several key concepts to understand
49     0.0000       1.0000       0.0000         0.0000     qwen2-7b             For example, if we had a sorted list of only true and false, we can actually use binary search to find the first true occurrence. The binary search would work like this. You look at the middle of the list. If the middle value is true, you know that the first true must either be here or somewhere to the left, so you move your search to the left half. If the middle value is false, you know that the first true must be to the right, so you focus on the right half. This type of searching, based on where a condition is true and where it is false, can be reused for every single binary search problem. In fact, we actually have this free template for this on our website, Algamonster. All we need to do is find out what makes something feasible. For example, the common lead code problem, minimum in rotated sorted array, doesn't appear to follow binary search at first. After all, it's not sorted. However, there's a monotonic condition we can use to apply binary search. The condition is that any element that is less than the last element of the array is part of the rotated section, while any element greater than the last element is part of the original sorted section. We can mentally convert this to trues and falses, and just like that, we have the same true and false question we just solve. This can be applied to a ton of different questions. For more practice and an in-depth explanation, check out algo.monster. Now we'll take a look at non-linear structure problems which work with trees and graphs. In fact, instead of treating trees and graphs as two entirely different structures, you should imagine that a tree is just a graph with no cycles. The only difference between the two is that for a graph that could have cycles, you need some visited set to keep track of nodes you've already visited. This makes the patterns we will talk about applicable to both. For non-linear problems, we'll start with breadth-first search, as it's the easiest algorithm to understand and build off of. breadth-first search is an algorithm used to explore nodes in a graph or tree level by level. The idea behind this is that it starts at a given node, commonly called the root, and explores all its immediate neighbors first before moving on to the neighbors' neighbors, and so on. To keep track of the nodes that have already been visited so that we don't revisit them, BFS uses a queue, a data structure that works like a line. First in, first out. This ensures that nodes are visited in the correct order, starting from the closest to the root and moving outward. BFS is such a common algorithm that you will use all the time. Luckily for you, instead of having to derive it from scratch each time, we have this template that we can reuse. If you want to see more about how we got this template, check it out at algo.monster. Now let's see BFS in action. We're going to use this template to solve a common lead code problem, level order traversal of a binary tree. The problem asks us to give all of the levels of the tree at each depth This means we need to visit the nodes of the tree level by level, starting from the root and moving down, visiting all the nodes at each level before proceeding to the next. For example, given a tree with the root node 3 and children 9 and 20, where 20 has children 15 and 7, we need to return the values of the nodes level by level in the following order. First 3, then 9, 20, and finally 15, 16th phrase which is not a kill, we can solve this problem using BFS as its ideal for level by level traversal. The idea is to use a queue to help explore the tree one level at a time. First, we add the root node to the queue. Then for each level, we process all nodes currently in the queue by removing them and adding their values to the result. For each node we process, we also add its left and right children to the queue so they can be processed in the next level. This ensures that we visit all nodes on one level before moving to the next. By repeating this process until the queue is empty, we can traverse the entire tree level by level and collect the node values in the correct order. Finally, we return the list of node values grouped by levels. Devth first search is an algorithm that builds on the foundation of BFS but explores the tree or graph in a different way. While BFS focuses on exploring nodes level by level, DFS dives deep into one path as far as possible before exploring the next path. In BFS, we use a queue to process nodes in a breadth first manner, ensuring all nodes at one level are explored before moving to the next. In contrast, DFS uses a stack to explore nodes in a depth first manner, where you keep going down one branch until you reach the end, and then you come back to explore other branches. Most often, the stack DFS uses is the call stack. As most DFS implementations use recursion, where BFS is ideal for finding the shortest path or exploring nodes layer by layer, DFS is more suited for problems where you need to explore all paths or check every possible configuration, such as finding all paths in a tree, searching for a specific condition deep in the structure or detecting cycles. The depth first nature of DFS makes it more memory efficient in some cases, but it's less suited for problems where the shortest path or shallow exploration is required, as it doesn't guarantee finding the closest solution first. Like with BFS, DFS is a common algorithm that you will use all the time, so we have a template that we can reuse. Again, if you're curious to how this works, check it out at algo.monster. Now, let's solve a leap code problem with DFS. A common leap code problem that uses this pattern is called number of islands. This problem asks us to count the number of islands in a 2D grid where one represents land and zero represents water. An island is formed by connecting adjacent ones horizontally or vertically. We need to determine how many distinct islands exist by identifying connected components of ones in the grid. For example, in a grid where someone's are grouped together, each connected group is considered an island. Our goal is to traverse the grid, find all islands, and count them. We can solve this problem using DFS to explore each island. The idea is to iterate through each cell in the grid. When we encounter a one land, it indicates the start of an island. From there, we perform DFS to visit all connected ones all parts of the same island by exploring the neighboring cells in all four directions as deep as they go. During the DFS, we mark each visited cell by changing its value to zero to ensure we don't count it again. Once all connected cells for that island are visited, we increment the island count and continue searching the rest of the grid. This process ensures that we explore each island once and count it correctly. By the end, the total number of initializing DFS calls we make, not including the recursive calls, equals the number of islands. Backtracking is a pattern that many people struggle with, but in reality, it's actually just an extension of depth first search. In DFS, we typically traverse through a prebuilt structure like a tree or graph, where the nodes and connections are already defined. With backtracking, however, you often have to build the solution tree yourself as you explore different options, especially in combinatorial problems, where the structure of the tree isn't given explicitly, but is generated dynamically as you make decisions. In a backtracking problem, you explore all possible solutions by making a series of decisions. Each decision represents a node in the tree, and each potential decision forms a branch. As you explore a decision, you either reach a valid solution, or you hit a dead end. When a decision leads to a dead end, you backtrack to the previous decision, undo it, and try another path. Just like with all of our other patterns, we also have a template we are going to use here, which is this. Now, let's walk through it by solving a lead code problem. Letter combinations of a phone number. In this problem, we're given a string of digits, typically from 2 to 9, and we need to return all the possible letter combinations that the digits could represent using the mapping of a phone keypad. For example, if we have the digits 23, each digit corresponds to a set of letters, two maps to A, B, C, and three maps to D, E, F. Our goal is to generate every possible combination of these letters. So, for 23, some outputs could be a D, A, E, A, F, B, D, B, E, B, F, and so on. Now, our goal is to find all of these combinations. Backtracking is the optimal approach for solving this problem because it allows us to explore each possible letter one at a time while pruning invalid configurations early. Rather than brute forcing through every possible combination, backtracking lets us build the solution letter by letter, discarding incomplete paths that no longer make sense. We start by taking the input digits and starts by checking if the input is empty, returning an empty list if so. It initializes an empty list ains to store the results and defines a recursive helper function, DFS, to perform depth first search, DFS. The DFS function tracks the current position in the digits string using start index and builds combinations step by step by adding letters to the path list. If the start index reaches the length of the digits, the current path is a complete combination, so it's joined into a string and added to ANDs. For each digit, it looks up the corresponding letters in the phone keypad map and iterates over them. For every letter, it adds it to the current combination and moves forward in the DFS by calling itself recursively with the updated start index plus one. After processing all letters for a digit, the function removes the last letter from the path to backtrack and explore other possibilities. Once the DFS finishes, the final result is returned containing all possible combinations of letters for the given digits. This method efficiently generates all valid combinations without
50     0.0000       1.0000       0.0000         0.0000     llama3.1-8b          For example, if we had a sorted list of only true and false, we can actually use binary search to find the first true occurrence. The binary search would work like this. You look at the middle of the list. If the middle value is true, you know that the first true must either be here or somewhere to the left, so you move your search to the left half. If the middle value is false, you know that the first true must be to the right, so you focus on the right half. This type of searching, based on where a condition is true and where it is false, can be reused for every single binary search problem. In fact, we actually have this free template for this on our website, Algamonster. All we need to do is find out what makes something feasible. For example, the common lead code problem, minimum in rotated sorted array, doesn't appear to follow binary search at first. After all, it's not sorted. However, there's a monotonic condition we can use to apply binary search. The condition is that any element that is less than the last element of the array is part of the rotated section, while any element greater than the last element is part of the original sorted section. We can mentally convert this to trues and falses, and just like that, we have the same true and false question we just solve. This can be applied to a ton of different questions. For more practice and an in-depth explanation, check out algo.monster. Now we'll take a look at non-linear structure problems which work with trees and graphs. In fact, instead of treating trees and graphs as two entirely different structures, you should imagine that a tree is just a graph with no cycles. The only difference between the two is that for a graph that could have cycles, you need some visited set to keep track of nodes you've already visited. This makes the patterns we will talk about applicable to both. For non-linear problems, we'll start with breadth-first search, as it's the easiest algorithm to understand and build off of. breadth-first search is an algorithm used to explore nodes in a graph or tree level by level. The idea behind this is that it starts at a given node, commonly called the root, and explores all its immediate neighbors first before moving on to the neighbors' neighbors, and so on. To keep track of the nodes that have already been visited so that we don't revisit them, BFS uses a queue, a data structure that works like a line. First in, first out. This ensures that nodes are visited in the correct order, starting from the closest to the root and moving outward. BFS is such a common algorithm that you will use all the time. Luckily for you, instead of having to derive it from scratch each time, we have this template that we can reuse. If you want to see more about how we got this template, check it out at algo.monster. Now let's see BFS in action. We're going to use this template to solve a common lead code problem, level order traversal of a binary tree. The problem asks us to give all of the levels of the tree at each depth This means we need to visit the nodes of the tree level by level, starting from the root and moving down, visiting all the nodes at each level before proceeding to the next. For example, given a tree with the root node 3 and children 9 and 20, where 20 has children 15 and 7, we need to return the values of the nodes level by level in the following order. First 3, then 9, 20, and finally 15, 16th phrase which is not a kill, we can solve this problem using BFS as its ideal for level by level traversal. The idea is to use a queue to help explore the tree one level at a time. First, we add the root node to the queue. Then for each level, we process all nodes currently in the queue by removing them and adding their values to the result. For each node we process, we also add its left and right children to the queue so they can be processed in the next level. This ensures that we visit all nodes on one level before moving to the next. By repeating this process until the queue is empty, we can traverse the entire tree level by level and collect the node values in the correct order. Finally, we return the list of node values grouped by levels. Devth first search is an algorithm that builds on the foundation of BFS but explores the tree or graph in a different way. While BFS focuses on exploring nodes level by level, DFS dives deep into one path as far as possible before exploring the next path. In BFS, we use a queue to process nodes in a breadth first manner, ensuring all nodes at one level are explored before moving to the next. In contrast, DFS uses a stack to explore nodes in a depth first manner, where you keep going down one branch until you reach the end, and then you come back to explore other branches. Most often, the stack DFS uses is the call stack. As most DFS implementations use recursion, where BFS is ideal for finding the shortest path or exploring nodes layer by layer, DFS is more suited for problems where you need to explore all paths or check every possible configuration, such as finding all paths in a tree, searching for a specific condition deep in the structure or detecting cycles. The depth first nature of DFS makes it more memory efficient in some cases, but it's less suited for problems where the shortest path or shallow exploration is required, as it doesn't guarantee finding the closest solution first. Like with BFS, DFS is a common algorithm that you will use all the time, so we have a template that we can reuse. Again, if you're curious to how this works, check it out at algo.monster. Now, let's solve a leap code problem with DFS. A common leap code problem that uses this pattern is called number of islands. This problem asks us to count the number of islands in a 2D grid where one represents land and zero represents water. An island is formed by connecting adjacent ones horizontally or vertically. We need to determine how many distinct islands exist by identifying connected components of ones in the grid. For example, in a grid where someone's are grouped together, each connected group is considered an island. Our goal is to traverse the grid, find all islands, and count them. We can solve this problem using DFS to explore each island. The idea is to iterate through each cell in the grid. When we encounter a one land, it indicates the start of an island. From there, we perform DFS to visit all connected ones all parts of the same island by exploring the neighboring cells in all four directions as deep as they go. During the DFS, we mark each visited cell by changing its value to zero to ensure we don't count it again. Once all connected cells for that island are visited, we increment the island count and continue searching the rest of the grid. This process ensures that we explore each island once and count it correctly. By the end, the total number of initializing DFS calls we make, not including the recursive calls, equals the number of islands. Backtracking is a pattern that many people struggle with, but in reality, it's actually just an extension of depth first search. In DFS, we typically traverse through a prebuilt structure like a tree or graph, where the nodes and connections are already defined. With backtracking, however, you often have to build the solution tree yourself as you explore different options, especially in combinatorial problems, where the structure of the tree isn't given explicitly, but is generated dynamically as you make decisions. In a backtracking problem, you explore all possible solutions by making a series of decisions. Each decision represents a node in the tree, and each potential decision forms a branch. As you explore a decision, you either reach a valid solution, or you hit a dead end. When a decision leads to a dead end, you backtrack to the previous decision, undo it, and try another path. Just like with all of our other patterns, we also have a template we are going to use here, which is this. Now, let's walk through it by solving a lead code problem. Letter combinations of a phone number. In this problem, we're given a string of digits, typically from 2 to 9, and we need to return all the possible letter combinations that the digits could represent using the mapping of a phone keypad. For example, if we have the digits 23, each digit corresponds to a set of letters, two maps to A, B, C, and three maps to D, E, F. Our goal is to generate every possible combination of these letters. So, for 23, some outputs could be a D, A, E, A, F, B, D, B, E, B, F, and so on. Now, our goal is to find all of these combinations. Backtracking is the optimal approach for solving this problem because it allows us to explore each possible letter one at a time while pruning invalid configurations early. Rather than brute forcing through every possible combination, backtracking lets us build the solution letter by letter, discarding incomplete paths that no longer make sense. We start by taking the input digits and starts by checking if the input is empty, returning an empty list if so. It initializes an empty list ains to store the results and defines a recursive helper function, DFS, to perform depth first search, DFS. The DFS function tracks the current position in the digits string using start index and builds combinations step by step by adding letters to the path list. If the start index reaches the length of the digits, the current path is a complete combination, so it's joined into a string and added to ANDs. For each digit, the function iterates over its corresponding letters and recursively calls itself with the updated path and start index. If a letter is chosen, it adds the letter to the current combination and moves forward in the string. If it hits a dead-end, it backtracks by removing the last chosen letter and returns to the last valid state. This way, we avoid generating duplicate combinations and explore all possibilities efficiently. At the end of the DFS function, the result list contains all possible combinations of letters. Finally we return ANDs, which now holds

Model Averages for this Segment:
  gemma2-9b           : 0.2122
  llama3.1-8b         : 0.1046
  mistral-7b-v0.3     : 0.0000
  qwen2-7b            : 0.0507
  tinyllama           : 0.0000

====================================================================================================

--- SEGMENT 04 ---
Start Time: 1250s (20:50)
Transcript Length: 222 characters
Total Titles Evaluated: 50

Rank   Avg Score    Bert Score   Cosine Score   Penalty    Model                Title
----------------------------------------------------------------------------------------------------
1      0.5277       0.5277       0.0000         1.0000     mistral-7b-v0.3      Explore Dynamic Programming at Algo.Monster
2      0.5086       0.5086       0.0000         1.0000     mistral-7b-v0.3      Mastering Dynamic Programming: Resources on Algo.Monster
3      0.5041       0.5041       0.0000         1.0000     mistral-7b-v0.3      Exploring Dynamic Programming: Resources on Algo.Monster
4      0.4475       0.5286       0.0000         0.8465     mistral-7b-v0.3      Exploring Dynamic Programming: More Info at Algo.Monster
5      0.4319       0.4319       0.0000         1.0000     mistral-7b-v0.3      Deep Dive into Dynamic Programming
6      0.4274       0.4274       0.0000         1.0000     llama3.1-8b          Dynamic Programming Explained Beyond This Video
7      0.4241       0.4241       0.0000         1.0000     llama3.1-8b          Beyond This Video: Exploring Dynamic Programming
8      0.4233       0.4233       0.0000         1.0000     tinyllama            Dynamic Programming - A Complete Guide (2)
9      0.4142       0.4142       0.0000         1.0000     tinyllama            Dynamic Programming: A Brief Introduction
10     0.4112       0.4112       0.0000         1.0000     qwen2-7b             Master Dynamic Programming: Dive Deeper Online
11     0.4090       0.4090       0.0000         1.0000     mistral-7b-v0.3      Mastering Dynamic Programming: A Deep Dive
12     0.4086       0.4086       0.0000         1.0000     tinyllama            Dynamic Programming - A Brief Introduction
13     0.4029       0.4029       0.0000         1.0000     mistral-7b-v0.3      Exploring Dynamic Programming: More Details Await
14     0.3994       0.4718       0.0000         0.8465     mistral-7b-v0.3      Exploring Dynamic Programming: More on Our Website
15     0.3959       0.3959       0.0000         1.0000     llama3.1-8b          Mastering Dynamic Programming Made Easy
16     0.3931       0.3931       0.0000         1.0000     gemma2-9b            Mastering Dynamic Programming (10)
17     0.3925       0.3925       0.0000         1.0000     tinyllama            Understanding Dynamic Programming (2)
18     0.3893       0.3893       0.0000         1.0000     llama3.1-8b          Mastering Dynamic Programming Concepts
19     0.3883       0.3883       0.0000         1.0000     llama3.1-8b          Mastering Dynamic Programming Fundamentals
20     0.3880       0.3880       0.0000         1.0000     mistral-7b-v0.3      Deep Dive: Dynamically Programming Solutions
21     0.3838       0.3838       0.0000         1.0000     llama3.1-8b          Beyond What's Covered Here: DP Essentials
22     0.3809       0.3809       0.0000         1.0000     llama3.1-8b          Beyond This Video's Scope
23     0.3794       0.3794       0.0000         1.0000     llama3.1-8b          Exploring Dynamic Programming Fundamentals
24     0.3772       0.4456       0.0000         0.8465     mistral-7b-v0.3      Mastering Dynamic Programming: Check Out Our Resources
25     0.3734       0.3734       0.0000         1.0000     llama3.1-8b          (Link) Learn More About DP Now
26     0.3438       0.4062       0.0000         0.8465     tinyllama            Dynamic Programming: The Ultimate Tool for Algorithms
27     0.3197       0.4462       0.0000         0.7165     qwen2-7b             Master Dynamic Programming: Comprehensive Guide & Free Video (9)
28     0.3180       0.3180       0.0000         1.0000     llama3.1-8b          Exploring Advanced Algorithm Techniques
29     0.2877       0.4015       0.0000         0.7165     tinyllama            Dynamic Programming - The Ultimate Tool for Algorithms
30     0.2687       0.4430       0.0000         0.6065     tinyllama            Dynamic Programming - The Ultimate Guide to Understanding It
31     0.1588       0.4316       0.0000         0.3679     tinyllama            Dynamic Programming - A Complete Guide to Understanding and Implementing Dynamic Programs

Model Averages for this Segment:
  gemma2-9b           : 0.3931
  llama3.1-8b         : 0.3861
  mistral-7b-v0.3     : 0.4396
  qwen2-7b            : 0.3288
  tinyllama           : 0.3513

====================================================================================================

